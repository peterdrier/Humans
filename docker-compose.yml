services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=db;Database=humans;Username=humans;Password=${POSTGRES_PASSWORD:-humans}
      - Authentication__Google__ClientId=${GOOGLE_CLIENT_ID}
      - Authentication__Google__ClientSecret=${GOOGLE_CLIENT_SECRET}
      - GoogleMaps__ApiKey=${GOOGLE_MAPS_API_KEY}
    ports:
      - "5000:8080"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - humans-net
    restart: unless-stopped

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: humans
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-humans}
      POSTGRES_DB: humans
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - humans-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U humans"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  humans-net:
    driver: bridge
