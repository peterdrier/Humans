<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\..\src\Sections\Humans.Gdpr\Humans.Gdpr.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="NodaTime.Testing" />
  </ItemGroup>

  <!--
    No FrameworkReference: nothing here names an ASP.NET type. The section has no
    controller at all — the export download action stays on Shell's ProfileController —
    so the orchestrator test constructs GdprExportService over fake contributors and a
    FakeClock and nothing else (G5-SECTION-TEMPLATE.md step 8, Cantina's shape rather
    than Scanner's).

    No EF Core package either: Gdpr owns no tables, which is why tests/Directory.Build.props
    excludes this project from the shared TestDbContextFactory compile item — see the
    comment there.
  -->

</Project>
