# Baseline: only EventRepository may write to the seven event_* DbSets
# (Events, EventCategories, EventVenues, EventFavourites,
#  EventPreferences, EventGuideSettings, EventModerationActions).
# One locator per line: relative/path.cs:stable-key
# Stable keys only — line numbers are informational and stripped for diffing.
# Generated 2026-05-14 (section-align Events Phase 5).
#
# Empty baseline is the steady state: after Stage 3's cross-section strip,
# every event_* DbSet write goes through EventRepository. Test cleanup that
# previously seeded events directly through DbContext lives in test code,
# which is excluded from this scan via RatchetTestRunner.EnumerateSourceFiles.
