# Tests

Runs as a subagent, `opus low` (`doctor-reader`).

The invariant coverage matrix — every invariant, negative access rule and trigger in the target
mapped to a pinning test; redundant and asserting-the-mock tests.

Always build the matrix, including when the section looks well tested; the gaps it finds are the
invariants nobody thought to doubt.
