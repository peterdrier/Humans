# Section Doctor — Cantina — 2026-08-22

- **Invocation:** `/section-doctor` (no arguments), unattended scheduled run
- **Anchor:** `b68b35f3` (origin/main at branch point)
- **Branch:** `section-doctor/2026-08-22T183116Z`
- **Budget:** 2.5h default; started 18:30Z
- **PR:** [#1453](https://github.com/peterdrier/Humans/pull/1453)
- **Environment:** degraded — see `## Threads`. No .NET SDK in this container and the
  egress policy denies `builds.dotnet.microsoft.com` (403), so nothing compiled, ran or
  measured. No `gh` CLI either; GitHub work went through the GitHub MCP tools.
  **Followed up after the run** (Peter asked whether the SDK installs from Ubuntu): it does,
  and it is still not enough to build. See Needs Peter item 7 — the remaining blocker is
  narrower and different from what this run first assumed.

## Selection

Blocked set: empty — no open `section-doctor/*` PRs.

The selector subagent was not dispatched: its first two steps are `dotnet build` and
`reforge surface-score`, neither of which exists in this container, so it had no input its
brief is written around. Its checklist was worked on the main thread instead (**self-run**),
with **section LOC standing in for the reforge surface score** — reforge's compact output
carries `loc` as one of its fields, and it is the only component of the score obtainable
without a compiler. That substitution is a judgment call and is queued for Peter.

- Pool: 42 section projects, 3 previously doctored (Containers, Finance, Guide) → 39
  never-doctored, which outrank the re-doctor tier.
- Feature-active down-rank: the open GDPR right-to-erasure PR (#1446) alone touches 23
  sections, and #1442–#1448 add Expenses, SystemSettings, Shifts, Users, GoogleIntegration,
  Email, Monitor, Development and AuditLog. 26 of the 39 dropped to the bottom of the tier.
- Eligible, non-feature-active, never-doctored: 13. Ranked by the LOC proxy, the median is
  **Cantina** (34 source files, 1 991 lines) — genuinely middle-out.

## Assessment summary

Cantina is a read-only aggregator with no tables, no `DbContext`, no repository and no EF
reference. It answers two questions — "how many are here each day this week" and "who exactly
is here on this day, and what do they eat" — plus a CSV of each, behind an Admin/CantinaAdmin
policy, and it never touches medical data. That part is in good shape and well pinned.

What it does **not** do is describe itself accurately. The section's prose describes a
different product from the one that ships, and the code carries a behavioural gap that no test
doubts (finding 1; a second suspected one, finding 2, was later ruled out — see the resume
session below).

The target is now written at `src/Sections/Humans.Cantina/Docs/health.md`; this is the
section's first doctor pass, so there is no previous target to diff against.

### Findings, ranked

1. **[bug — queued] A human whose stored `DietaryPreference` is not one of the four canonical
   values disappears from the dietary breakdown.** `BuildDietaryBreakdown` increments
   `answered` for any non-empty preference but only buckets values it recognises, and
   `Unanswered` is then computed as `total − answered`. So an unknown value lands in no
   bucket at all and the breakdown no longer sums to the head count. The comment immediately
   above the branch states the intended rule — "Treat them as Unanswered for display purposes"
   — which is exactly what the code does not do. Both the weekly and the daily path. The write
   path filters against the canonical list, so the exposure is legacy or hand-edited rows;
   the code deliberately guards for that case and implements the guard wrongly.
2. **[not a defect — ruled 2026-08-23] Profile-less on-site humans render as "(unknown)".**
   As reported: `BuildProfileMap` keeps only users whose `UserInfo.Profile` is non-null and
   `ResolveBurnerName` reads `ProfileInfo.BurnerName`, so `UserInfo.BurnerName` — the field
   that carries the Users-side legacy fallback — is never consulted, while `Cantina.md`
   documented the opposite and two DTOs promised a `DisplayName` fallback no code performs.
   **The premise was wrong: there is no such thing as a profile-less human**, so the case
   cannot arise. The docs were corrected to describe what the service does, `"(unknown)"` is
   documented as a defensive default rather than a rendered case, and no code changed.
3. **[freshness — struck] The feature spec specified the inverse of the shipped UI**, on two
   points at once: it required a per-person table on the weekly page (removed when detail moved
   to the drill-down) and listed the per-day drill-down under "Out of scope (v1) … rejected as
   low-value" while its own Routes table shipped that page and its CSV. The drill-down had no
   user story at all.
4. **[delete — struck] 72 dead resx entries** — the 12 per-person-table column labels, orphaned
   in all six locale files when that table was removed. Referenced nowhere in the repository.
5. **[history — struck] The dated arrival/FODMAP design doc** (171 lines) narrated a merged
   branch through paths that have all moved (`src/Humans.Domain/`, `src/Humans.Application/`,
   `SharedResource.resx`, six line-number citations) and described a first-shift scan the code
   no longer performs. Its four live decisions moved into `health.md`.
6. **[freshness — struck] Names that no longer resolve**, across nine files: the service
   interface credited `IShiftManagementRepository`, `IProfileService` and `IUserService`;
   six DTO blocks sourced dietary from `VolunteerEventProfile`; the controller promised HTTP
   403 where the app redirects; `Humans.Domain`, `Humans.UI` and `Humans.Infrastructure`
   survived in four files after G5 deleted all three.
7. **[freshness — struck] The feature doc's freshness triggers watched the wrong section.**
   They pointed at the Shifts `VolunteerEventProfile` files — whose dietary columns are marked
   "RETAINED for prod-soak drop" — and not at `Humans.Users/Domain/Profile.cs` or
   `UserInfo.cs`, where the data the doc asserts about actually lives.
8. **[shape — queued] Four service methods over two questions.**
   `GetCurrentWeekStartOffsetForActiveEvent` / `GetCurrentDayOffsetForActiveEvent` exist only
   so the controller can resolve a default it immediately hands back to the service. Making the
   offset parameters nullable on the two async methods deletes 2 interface methods, 2 controller
   dependencies (`IBurnSettingsService`, `IClock`) and 2 private controller helpers.
9. **[efficiency — queued] The weekly path queries the same seven days twice.**
   `LoadWeeklyOnSiteUsersAsync` fetches the visible week, then
   `BuildFirstConfirmedOffsetByUserAsync` re-fetches every day from build start through the
   window's end, which includes all seven. The two scans are deliberately separate (see the
   load-bearing weirdness list) but the overlap is not deliberate.
10. **[tests — queued] Both defects sit in the two holes in the invariant matrix.** Every other
    invariant, negative access rule and trigger in the target has a pinning test; nothing pins
    unknown-value dietary bucketing or the burner-name fallback.

11. **[bug — queued, off-section] Account deletion leaves dietary and medical answers on the
    profile.** Surfaced by a review-bot finding on this PR's own GDPR wording, then verified:
    `UserRepository.AnonymizeProfileInternalAsync` clears seventeen profile fields and deletes
    the contact-field and volunteer-history rows, but never touches `DietaryPreference`,
    `Allergies`, `AllergyOtherText`, `Intolerances`, `IntoleranceOtherText` — or
    `MedicalConditions`, which is Art. 9 data. A deleted account keeps its dietary and medical
    answers attached to an anonymized profile. Users' code, not Cantina's, so queued rather
    than struck; the feature doc no longer claims the erasure guarantee it was inheriting.
    Possibly already in scope for the open right-to-erasure PR #1446.

12. **[delete — queued] Five members of the daily payload are computed every request and read
    by nothing.** `DailyMatrixDto.DietaryBreakdown`, `.AllergyRollup`, `.IntoleranceRollup`,
    `.AllergyOtherEntries` and `.IntoleranceOtherEntries` are consumed by no production code:
    `Day.cshtml` counts its own totals from the rendered rows, and
    `CantinaDailyMatrixCsvWriter` builds every row and total from `People`. Only tests assert
    them. `GetDailyRosterAsync` runs one `BuildDietaryBreakdown` and two `BuildRollup` passes
    over the day's cohort to produce them. Removing them takes the DTO members, the service
    work behind them and the test assertions with it — a compiler change, so queued.

**Independence check: pass.** Findings 1, 2, 3, 5 and 8 came from reading the section against
the target — spec-vs-reality deltas and a shape mismatch — not from any tool, score or grep;
findings 4, 6 and 7 came from mechanical name-resolution. No tool produced any finding on this
run, because no tool ran.

## Worked

- Deleted 12 orphaned resource keys across 6 locale files (72 entries), structure-aware, with
  every surviving key's value verified byte-identical after the edit.
- Rewrote the feature spec to describe the shipped product: weekly page is overview-only, the
  drill-down is real and now has US-36.5, the read map points at `Profile` rather than
  `VolunteerEventProfile`, and the freshness triggers watch the Users files.
- Retired the dated design doc, migrating its four still-live decisions into `health.md`.
- Swept nine files of names that no longer resolve (comments only in the `.cs` files).
- Wrote the section's target at `Docs/health.md` and linked it from `Cantina.md`.
- Applied the merged 2026-08-18 Finance run's sweep queue: 8 lessons into this skill, 2 debt
  items into the ledger inbox.

## Skipped, and why

- **Every code change** (findings 1, 2, 8, 9, 10) — *in the unattended run of 2026-08-22*. No
  compiler: the standing constraint is never to leave the branch red, and a C# edit that cannot
  be built or tested cannot honour it. All five went to `## Needs Peter` with the fix described.
  **The resume session of 2026-08-23 struck findings 8, 9 and 12 on a machine with a compiler**
  (see below); findings 1 and 11 moved to nobodies-collective/Humans#1113 and finding 2 was
  ruled out.
- **Sections passed over as blocked:** none — the blocked set was empty.
- **Phase 8 inline round:** unattended run, per the schedule's instruction.
- **The Phase 4.4 reviewer gate:** not obtained. The one non-mechanical deletion (the design
  doc) was **self-reviewed**, not reviewed — recorded here and in the PR body. The concept that
  improved: the section's rationale now lives in one place a reader looks, instead of being
  split between a live doc and a merged branch's note that had drifted from the code.

## Review round (post-PR)

Codex reviewed the PR and raised three findings against this run's own diff. All three were
verified against the code and all three were right — the run had written new claims the code
does not honour, which is precisely the failure it exists to remove:

1. The US-36.5 acceptance criteria I added described the *payload* rather than the *page*:
   `Day.cshtml` renders a two-number summary card and per-chip totals in the matrix footer, and
   renders the payload's `AllergyRollup` / `IntoleranceRollup` / `*OtherEntries` nowhere at all.
2. The GDPR retention line ("erased with the account") is false — finding 11 above.
3. "No medical fields cross the boundary" overstated the guarantee: the cached profile record
   Cantina receives does carry `MedicalConditions`; the exclusion is that the service never
   reads it and the output records have no such property. Swept across all three places the
   section said it.

### Second review round

Codex returned on the corrected text and was right twice more, both against edits this run had
just made:

- The VEP sweep missed `VEP` — the *abbreviation* — in the Aggregates table, so one stale claim
  survived a sweep that had also stopped watching the VEP files. Fixed, and the lesson is in the
  sweep queue.
- My replacement wording said the unused daily rollups were "carried for the CSV". They are not:
  the daily CSV counts from `People` like the view does. That is a second wrong rationale for the
  same passage, and the honest answer turned out to be better than either — the members are dead
  (finding 12). The doc now says so rather than explaining them away.

Two rounds of reshaped findings on one passage is the point at which to stop rewriting it and
name the underlying defect instead, which is what finding 12 does.

## Resume session — 2026-08-23

The whole Needs-Peter queue is answered and applied. Four commits, on a machine with a
compiler this time: full solution builds clean, whole suite green, CI green on every head.

**Struck**

- **The five unread daily-payload members are gone** (finding 12) — compiler-confirmed dead.
  `Day.cshtml` and `CantinaDailyMatrixCsvWriter` each count their own column totals from
  `People`; nothing outside the tests read `DietaryBreakdown`, either chip rollup or either
  other-text list. The test assertions moved onto `People`, counted the way the page counts them.
- **Each day's on-site cohort is fetched once per request** (finding 9). The weekly render
  loaded the visible seven days and then re-scanned the same range for arrivals; the daily
  render did the same for its one day. Both paths now share a per-request map from day offset
  to cohort, and the two scans stay logically separate.
- **The roster service resolves its own default offset** (finding 8). Both entry points take a
  nullable offset; `GetCurrentWeekStartOffsetForActiveEvent` and
  `GetCurrentDayOffsetForActiveEvent` leave the interface, and their two copies of the timezone
  lookup fold into the existing `GetEventTodayDate`. The controller loses `IBurnSettingsService`,
  `IClock` and both `ComputeDefault*Async` helpers, and stops fetching the active event twice per
  request. Routes, query parameters and behaviour unchanged; four tests now pin the default
  resolution, which had no coverage on either side of the move.
- **Burner-name resolution says what it does** (finding 2, ruled out). Docs fixed, no code change.

**Filed rather than fixed**

- **nobodies-collective/Humans#1112** — `IUserPart` fanout, so the profile page finds
  section-owned user info without Users reading it directly.
- **nobodies-collective/Humans#1113** — Cantina owns the food preferences end-to-end: its own
  table, `DbContext`, repository and cache; Users and Shifts stop reading food data entirely;
  `MedicalConditions` stays in Users as Art. 9 data. Carries **finding 1** (the dietary-count
  bug and its missing pinning test), **finding 11** (the GDPR erasure gap), the missing "Other"
  dietary preference, and write validation — deliberately not patched here, because the value
  model changes there.

**Closed unactioned**

- The cloud-environment build blocker (item 7). Diagnosis stands above; until
  `builds.dotnet.microsoft.com` is allowed, an SDK is baked into the image, or
  `Microsoft.CodeAnalysis.CSharp` is pinned down to Ubuntu's band, cloud runs stay docs-only.
  `.claude/bootstrap-dotnet.sh` (peterdrier/Humans#1454, merged) reports which case a run is in.

**Audit of this session** — the queue was ticked correctly in the run file but four narrative
restatements of the same findings went stale, in both this file and the PR body: the assessment
summary, `## Skipped`, `## Size`, and the PR body's defect list. Corrected above. The underlying
problem — one finding restated in five places, so a ruling must be applied five times — is
written up for the skill itself rather than patched again per-run.

Then the audit's own correction missed twice more, which is the same lesson landing harder:

- Peter's `cb30c05d` found a **sixth** copy the audit had not counted — the retention note in
  `daily-roster.md`, still pointing readers at this file's Needs-Peter queue for a gap that
  #1113 had taken over. The count of places a finding lives was itself guessed rather than
  derived; the honest number is "however many files mention it", found by grep.
- The corrected `## Size` was wrong too. It was re-measured against `be886917` — a commit on
  this branch, not the anchor — so the second set of figures was as wrong as the first, and the
  lesson recording the miss quoted the wrong number while recording it. Re-measured against
  `b68b35f3` and reconciled against GitHub's own diff totals for the PR.

- And that correction was wrong a third time, in the PR body: it carried the figures measured
  at `cb30c05d` under a `21cbada4` heading, because the commit doing the measuring had not
  been made yet when the measurement was taken. `## Size` no longer states any self-referential
  figure; the two that cannot be frozen are deferred to GitHub's own diff totals.

- Then a fourth review round found the finding-2 ruling had reached the prose and stopped
  there. `Cantina.md` and `daily-roster.md` were updated to call `"(unknown)"` a defensive
  default; **six DTO doc comments went on describing the profile-less case as a real, rendered
  one**, two of them promising such humans "still appear here with empty dietary fields". Codex
  reported one unrelated stale comment (`RollupItemDto` still advertising a day-cohort count
  this PR had deleted); grepping for that class of claim rather than stopping at the reported
  line found the other six. Fixed in `ea75040f`.

  So the copies were never five, or six. Counting them was the wrong move: only a grep for the
  claim knows how many there are, and the code comments were outside every list anyone drew.

## Threads

| Thread | Ran | Notes |
|---|---|---|
| Shape | yes (main) | Produced finding 8. |
| Behavior & bugs | yes (main) | Produced findings 1 and 2, both by walking flows against the target's invariants. |
| Freshness | yes (main) | Findings 3, 6, 7. Every name in every doc and comment checked against the tree. |
| Conformance | partial (main) | `section-conformance.yml` rows read by hand; the mechanical detectors are build-based and did not run. |
| Tests | partial (main) | Invariant matrix built by hand — it found the two gaps at finding 10. **Mutation score not measured:** Stryker needs `dotnet tool restore`, which failed (no SDK). |
| Prose & surface | partial (main) | Resource audit ran (dead keys, missing translations, `resource-key-prefix` — 32/32 keys correctly prefixed, no missing translations, all six locales in lockstep). **InspectCode did not run** (no SDK). Nav checked by reading `SectionAdminNav`. |
| History | yes (main) | Finding 5. |
| Comments | yes (main) | Every comment in the inventory read; the cuts are in finding 6 plus two pieces of rename narration. |
| Inbox | yes (main) | No `debt-ledger.yml` item is tagged Cantina; no open GitHub issue names the section. |
| **Reforge (tool)** | **no** | Not installed — `dotnet tool update --global Reforge` cannot run without an SDK, and the SDK's download host is denied by egress policy. Cost: the surface score, `loc`, `cogP95` and `cogMax` are all unmeasured this run, and the selector fell back to the LOC proxy above. |
| **Stryker (tool)** | **no** | Same cause. The mutation dimension is unmeasured. |
| **InspectCode (tool)** | **no** | Same cause. |
| **Runtime verification** | **no** | Same cause — nothing could be rendered. No UI-affecting change was made, so nothing this run needed it. |

## File coverage

- `src/Sections/Humans.Cantina/CantinaResource.ca.resx` — changed
- `src/Sections/Humans.Cantina/CantinaResource.cs` — reviewed
- `src/Sections/Humans.Cantina/CantinaResource.de.resx` — changed
- `src/Sections/Humans.Cantina/CantinaResource.es.resx` — changed
- `src/Sections/Humans.Cantina/CantinaResource.fr.resx` — changed
- `src/Sections/Humans.Cantina/CantinaResource.it.resx` — changed
- `src/Sections/Humans.Cantina/CantinaResource.resx` — changed
- `src/Sections/Humans.Cantina/Contracts/README.md` — changed
- `src/Sections/Humans.Cantina/Controllers/CantinaController.cs` — changed
- `src/Sections/Humans.Cantina/Docs/2026-06-24-cantina-arrival-and-fodmap-design.md` — deleted
- `src/Sections/Humans.Cantina/Docs/Cantina.md` — changed
- `src/Sections/Humans.Cantina/Docs/authorization.md` — reviewed
- `src/Sections/Humans.Cantina/Docs/data-access.md` — changed
- `src/Sections/Humans.Cantina/Docs/features/daily-roster.md` — changed
- `src/Sections/Humans.Cantina/Docs/health.md` — changed
- `src/Sections/Humans.Cantina/Humans.Cantina.csproj` — changed
- `src/Sections/Humans.Cantina/Models/CantinaDailyMatrixCsvWriter.cs` — reviewed
- `src/Sections/Humans.Cantina/Models/CantinaRosterAssembler.cs` — reviewed
- `src/Sections/Humans.Cantina/Models/CantinaRosterCsvWriter.cs` — reviewed
- `src/Sections/Humans.Cantina/Properties/AssemblyInfo.cs` — reviewed
- `src/Sections/Humans.Cantina/Section.cs` — reviewed
- `src/Sections/Humans.Cantina/SectionAdminNav.cs` — reviewed
- `src/Sections/Humans.Cantina/SectionPolicies.cs` — reviewed
- `src/Sections/Humans.Cantina/Services/CantinaRosterService.cs` — reviewed
- `src/Sections/Humans.Cantina/Services/Dtos/DailyMatrixDto.cs` — changed
- `src/Sections/Humans.Cantina/Services/Dtos/DailyPersonRowDto.cs` — changed
- `src/Sections/Humans.Cantina/Services/Dtos/DayRosterSummaryDto.cs` — changed
- `src/Sections/Humans.Cantina/Services/Dtos/RollupItemDto.cs` — changed
- `src/Sections/Humans.Cantina/Services/Dtos/RosterPersonDto.cs` — changed
- `src/Sections/Humans.Cantina/Services/Dtos/WeeklyRosterDto.cs` — changed
- `src/Sections/Humans.Cantina/Services/ICantinaRosterService.cs` — changed
- `src/Sections/Humans.Cantina/Views/Cantina/Day.cshtml` — reviewed
- `src/Sections/Humans.Cantina/Views/Cantina/Roster.cshtml` — reviewed
- `src/Sections/Humans.Cantina/Views/Cantina/_ViewStart.cshtml` — reviewed
- `src/Sections/Humans.Cantina/Views/_ViewImports.cshtml` — changed
- `tests/Humans.Cantina.Tests/CantinaArchitectureTests.cs` — reviewed
- `tests/Humans.Cantina.Tests/Humans.Cantina.Tests.csproj` — reviewed
- `tests/Humans.Cantina.Tests/Models/CantinaCsvWritersTests.cs` — reviewed
- `tests/Humans.Cantina.Tests/Models/CantinaRosterAssemblerTests.cs` — reviewed
- `tests/Humans.Cantina.Tests/Services/CantinaDailyRosterServiceTests.cs` — reviewed
- `tests/Humans.Cantina.Tests/Services/CantinaRosterServiceTests.cs` — reviewed

## Size

Measured twice: once for the unattended run, once for the branch after the resume session
struck the queued code items.

| Measure | Unattended run (2026-08-22) | Branch, resume session |
|---|---|---|
| Section, against base `b68b35f3` | +220 / −314 → net −94 | **+350 / −478 → net −128** |
| Tests | +0 / −0 | +61 / −38 |
| Shared sweep files (this skill + the debt ledger) | +34 / −0 | +34 / −0 |
| Sections touched other than Cantina | none | none |
| Reforge, Cantina | **unmeasured** — reforge unavailable, see `## Threads` | **score 72 → 64; `locProd` −49; `cogP95` −3** |

These rows are current as of `ea75040f`, the last commit to touch anything outside this file.

**This file's own line count, and therefore the whole-branch total, are deliberately not
given here.** They cannot be: the commit that writes the number is a commit the number has to
count. Two successive corrections to this block were wrong for exactly that reason — the
second one carried figures measured at the previous head under the new head's name. GitHub's
`additions`/`deletions` on peterdrier/Humans#1453 settles the branch total on demand and is
never stale; the PR body quotes it. A self-referential figure belongs where it can be
recomputed after the fact, not frozen into the artefact it measures.

The rows above did not always reconcile. The first version of this table was measured against
`be886917`, a commit on this branch rather than the base, and no row cross-checked any other.
The standing check, which stays true however long this file grows: the branch's insertions
minus this file's own line count must equal **445** (= 350 + 61 + 34), and its deletions must
equal **516** (= 478 + 38).

**That check has already earned its place.** The previous version of this block called the
rows "exact and final — no commit after `cb30c05d` touches code", and `ea75040f` then rewrote
seven doc comments in `.cs` files. The claim was false within the hour. Prose noticed nothing;
the arithmetic did, on its first opportunity — GitHub reported 516 deletions against a stated
515. This is the only piece of the run's bookkeeping that has ever caught its own error.

The same commit also moved `locProd` from −56 to −49, because a doc comment on production code
is production LOC and reforge counts it. Caught by the surface report, which recomputes on
every push. Both catches came from a figure that is derived rather than asserted, which is the
whole of the difference: five rounds of prose review found nothing, and the two mechanisms that
recompute found one each within an hour.

Rule deltas: `cognitiveComplexity` −3, `longMethod` −3, `crossSectionReadInterface`
−2 — the last of those is finding 8's two interface methods actually leaving the surface.

Growth: none to explain. The section shrank without losing a behaviour; every deletion was a
dead resource key, a doc claim that contradicted the code, or narration of a state the tree no
longer has.

## Cost

| Component | Fresh in | Out | Cache write | Cache read | ~$ |
|---|---|---|---|---|---|
| main:phase2 | 36 | 21,633 | 53,442 | 1,688,103 | 1.72 |
| main:phase3 | 178 | 117,876 | 338,936 | 16,530,365 | 13.33 |
| main:phase5 | 44 | 21,091 | 43,538 | 5,331,051 | 3.47 |
| main:phase7 | 6 | 1,131 | 2,329 | 755,532 | 0.42 |
| **total** | 264 | 161,731 | 438,245 | 24,305,051 | **18.94** |

API-equivalent $, list rates; run under subscription quota. Measured Phase 1 to PR creation;
PR create/backfill excluded. The cloud-environment transcript layout the skill flags as
unverified worked without the `unmeasured` fallback.

## Needs Peter

Answered in the 2026-08-22 `resume` session; ticked items are applied on this branch.

1. [x] **Unknown dietary value — bucket as Unanswered, or count it separately?** (Finding 1.)
   — **Deferred to nobodies-collective/Humans#1113, fixed when the data moves to Cantina.**
   `DietaryPreference` is an unvalidated `string?` column, and unlike the allergy and
   intolerance chips it has no `Other` option at all — so the preference list gains one, the
   write gets validated against the list, and the count is fixed against the new shape rather
   than the old one. Nothing changed here: fixing the arithmetic against a value model that is
   about to gain a fifth canonical value would only have to be redone.
2. [x] **Burner name — is the `UserInfo.BurnerName` fallback meant to apply here?** (Finding 2.)
   — **There is no such thing as a profile-less human.** No code change; `Cantina.md`, the
   daily-roster spec and `health.md` now describe what the service does, and `"(unknown)"` is
   documented as a defensive default rather than a rendered case.
3. [x] **Collapse the roster service from 4 methods to 2** (Finding 8.) — Done. Both entry
   points take a nullable offset and resolve the default from the event they already load.
   `GetCurrentWeekStartOffsetForActiveEvent` and `GetCurrentDayOffsetForActiveEvent` are gone
   from the interface (private now, and the two copies of the timezone lookup they carried
   collapsed into the existing `GetEventTodayDate`); the controller loses
   `IBurnSettingsService`, `IClock` and both `ComputeDefault*Async` helpers, and no longer
   fetches the active event a second time per request. Routes, query parameters and the
   default-to-today behaviour are unchanged.
4. [x] **De-duplicate the weekly day scan?** (Finding 9.) — Done. Both entry points share one
   per-request map from day offset to on-site cohort; the two scans stay logically separate.
5. [x] **Two pinning tests** for the invariants behind findings 1 and 2 — finding 2 needs none
   (no such case) and finding 1 moved to nobodies-collective/Humans#1113 with its fix. Four
   tests landed instead on the default resolution item 3 moved into the service, which had no
   coverage at all before: with and without an active event, weekly and daily.
6. [x] **Was the LOC-for-reforge-score substitution the right fallback** for selection when the
   score cannot be computed? — Accepted, and moot going forward: peterdrier/Humans#1454 gives
   cloud runs a real toolchain check.
7. [x] **Environment — the blocker is narrower than this run first reported.** Closed unactioned
   at Peter's direction ("don't worry"). The diagnosis below stands for whoever picks it up;
   until one of the three fixes lands, cloud runs stay docs-only. Investigated after the run:

   - **The SDK does install from Ubuntu.** `apt-get install -y dotnet-sdk-10.0` gives 10.0.111
     from `archive.ubuntu.com`, which satisfies `global.json` (10.0.100 + `rollForward:
     latestFeature`). `dotnet tool restore` (Stryker 4.16.0) and `dotnet tool update --global
     Reforge` (0.29.0) then both succeed. The blocked host is not needed for any of that.
   - **It still cannot build this repo.** Every project fails `CS9057`:
     `src/Humans.Analyzers` is built against `Microsoft.CodeAnalysis.CSharp` **5.3.0**
     (`Directory.Packages.props`), and Ubuntu's 10.0.1xx band ships Roslyn **5.0.0.0**.
     `-p:RunAnalyzers=false` does not bypass it — the analyzer assembly is still loaded and
     version-checked before it is asked to run.
   - **There is no second source for the newer band.** `packages.microsoft.com` is reachable
     (200) but carries no .NET 10 SDK for noble — Microsoft defers to Canonical on 24.04 — and
     `apt-cache madison` shows 10.0.111 as the only version in any Ubuntu pocket. The band that
     carries Roslyn 5.3 comes from `builds.dotnet.microsoft.com`, which is 403.

   **So the fix is one of:** allow `builds.dotnet.microsoft.com` for this environment; bake a
   10.0.3xx/4xx SDK into the image; or pin `Microsoft.CodeAnalysis.CSharp` down to the band
   Ubuntu ships. The first is the smallest change and also restores `dotnet-install.sh`.

   **Either way the daily routine's setup block is now wrong** — it opens with `dotnet tool
   restore` on a container where `dotnet` does not exist. I could not edit the routine myself
   (`section-doctor-daily`, `trig_01DaFsf6UCrv7NiCAFxgExFr`, created via the HTTP API, so agents
   cannot update it). Replacement text is in the PR body under this item; it installs the SDK
   first, names the denied host so no future run burns time rediscovering it, and tells the run
   to degrade to docs-only rather than editing C# it cannot compile.

8. [x] **Should account deletion clear the dietary and medical profile fields?** (Finding 11.)
   — **Yes, and through the GDPR contributor seam, not by extending the anonymize field list.**
   Whichever section owns the dietary data implements the export half (already done for these
   fields, in `UserService.ContributeForUserAsync`) *and* an erasure half, so the answers are
   handed back to the subject and cleared on deletion from one place. Users' code, tracked
   against peterdrier/Humans#1446 — not this run's to write. The fanout seam is
   nobodies-collective/Humans#1112; the food data moves to Cantina, which then carries the
   erasure contribution, in nobodies-collective/Humans#1113.

9. [x] **Delete the five unused daily-payload members?** (Finding 12.) — Done, compiler-confirmed:
   `DailyMatrixDto.DietaryBreakdown`, both chip rollups and both other-text lists are gone.
   `Day.cshtml` and `CantinaDailyMatrixCsvWriter` each count their own column totals from
   `People`; the test assertions moved onto `People`, counted the way the page counts them.

## Sweep queue

- `lesson: 2026-08-22 — check a repo-relative path with the repo-relative path. A bare test on the basename "G5-SECTION-TEMPLATE.md" reported the live template missing and nearly produced a 65-file "fix"; the file was at docs/sections/. Resolve every asserted path from the worktree root.`
- `lesson: 2026-08-22 — write run files with a quoted heredoc or a file-write tool, never an unquoted one: the run file's own sweep queue lost a code span to command substitution because backticks inside an unquoted heredoc are executed.`
- `lesson: 2026-08-22 — when a doc and the code disagree and the code looks wrong, change neither. Fixing the doc to match a suspected defect cements it; the pair belongs in Needs-Peter together. Only sweep a claim when the code is the side that is right.`
- `lesson: 2026-08-22 — with no compiler, C# doc-comment edits are still safe if they add no <see cref> and the run verifies tag balance by parsing each /// block as XML. TreatWarningsAsErrors is on and CS1591 is suppressed but CS1574 is not, so a broken cref would break the build.`
- `lesson: 2026-08-22 — a feature doc's "Out of scope" list ages worse than its body: the drill-down Cantina ships was sitting under "rejected as low-value" in the same file that documented its routes. Read the out-of-scope list against the route table every time.`
- `lesson: 2026-08-22 — dead resource keys cluster where UI was removed, and they are the cheapest full-coverage signal available without a compiler: diff the resx key set against the keys the section's views actually reference.`
- `lesson: 2026-08-22 — a freshness trigger that resolves is not a freshness trigger that works. Cantina's pointed at a Shifts interface file that exists and contains none of the code the doc asserts about; check that each trigger path actually carries the claim, not merely that the path is live.`
- `lesson: 2026-08-22 — sweep a renamed concept by its abbreviations too. The VolunteerEventProfile sweep cleared every full-name hit and left "VEP" standing in the same file, while also dropping the freshness trigger that would have caught it later.`
- `lesson: 2026-08-22 — when a doc explains why an unused member exists, check that the explanation is true before writing it. Two rounds were spent inventing rationales ("for the CSV") for daily payload members that nothing reads; "nothing reads these" was both the correct answer and a finding.`
- `lesson: 2026-08-22 — when rewriting a spec to match reality, read the view, not the DTO. This run's new acceptance criteria listed aggregates the payload carries and the page does not render; a reviewer caught it, the run did not.`
- `lesson: 2026-08-22 — "never crosses the boundary" is almost always an overclaim for a section reading a shared read-model. The honest form is "the field is carried, this code never reads it, and the output record has no such property" — check which the code implements before repeating the claim.`
- SUPERSEDED by peterdrier/Humans#1454, which lands `memory/process/cloud-run-dotnet-bootstrap.md` covering both memory items below. A later sweep should skip them rather than create a second atom. `memory: process/cloud-run-dotnet-sdk — cloud containers ship no .NET SDK; install it with apt-get install -y dotnet-sdk-10.0 from Ubuntu's own archive (builds.dotnet.microsoft.com is denied by egress policy, and packages.microsoft.com has no .NET 10 for noble). Ubuntu's 10.0.1xx band ships Roslyn 5.0 and cannot build this repo while Humans.Analyzers targets Microsoft.CodeAnalysis 5.3.0 — CS9057, not bypassable with -p:RunAnalyzers=false.`
- `lesson: 2026-08-23 — a Needs-Peter ruling is a state change to a finding, and a finding is restated in the findings list, the assessment summary, Skipped, Size and the PR body. Ticking is the cheap half; propagating the changed status is the half that gets skipped. Two passes over one queue both ticked correctly and both left four restatements stale.`
- `lesson: 2026-08-23 — resume must re-derive ## Size before it finishes: the block is written at PR time and every struck item invalidates it. Cantina's read net -94 against an actual net -135.`
- `lesson: 2026-08-23 — measure ## Size against the PR's base sha, and name that sha in the table header. The re-measurement meant to fix a stale Size block was itself taken against a commit on the branch, so the corrected figures were also wrong; GitHub's own additions/deletions on the PR is the free cross-check.`
- `lesson: 2026-08-23 — make ## Size reconcile: component rows summing to the whole-branch row turns a silent wrong number into a visible one. Neither wrong version of Cantina's table added up, and nothing noticed until the rows were made to.`
- `lesson: 2026-08-23 — never freeze a figure that counts the commit writing it. A run file's own line count, and the branch total containing it, are stale the instant they are typed; three successive corrections to Cantina's Size block failed on this. State the stable rows, and defer the self-referential ones to the PR's own additions/deletions.`
- `lesson: 2026-08-23 — apply a ruling to the code comments, not just the prose docs. Finding 2's ruling reached Cantina.md and daily-roster.md and left six DTO doc comments describing the abolished case as real. Doc comments are documentation and drift like it; grep the section's .cs files for the claim, not only its .md files.`
- `lesson: 2026-08-23 — a reconciling Size table is the only bookkeeping in this run that ever caught its own error, flagging a stated 515 deletions against an actual 516 within an hour of a commit that invalidated an "exact and final" claim. Prose caught nothing, four times running. Make the rows sum, and never write "final" about a branch that is still moving.`
- `lesson: 2026-08-23 — a comment-only edit to a .cs file is not score-neutral. Rewriting seven DTO doc comments moved Cantina's locProd from -56 to -49 and the branch's deletions from 515 to 516; a run that calls such a change "docs only" and leaves its Size and reforge rows alone will misreport both.`
- `lesson: 2026-08-23 — when a review bot reports one stale claim, grep for its class before fixing the line. Codex's RollupItemDto finding was one of seven instances of the same overclaim; fixing only the reported line would have left six and looked resolved.`
- `lesson: 2026-08-23 — an environment caveat belongs to the session, not the run. "This run had no compiler" was true when written and false an hour later, sitting above a section describing compiler-confirmed strikes.`
- `memory: process/section-doctor-no-sdk — an unattended run that cannot build still runs its reading threads and doc strikes; it must record every compiler-dependent thread as skipped-with-reason and queue every code finding rather than editing C# it cannot build.`
