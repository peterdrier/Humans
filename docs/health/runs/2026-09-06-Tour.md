# Tour — section doctor, 2026-09-06

- **Invocation:** unattended daily run, no arguments. Phase 8 (inline round) skipped.
- **Anchor commit:** `10199a23` (`origin/main`)
- **Branch:** `section-doctor/2026-09-06T121559Z` (cloud run, repo root — no worktree)
- **Budget:** 2.5h, single PR.
- **PR:** peterdrier/Humans#1611

## Assessment summary

First doctor pass over Tour, the smallest section in the tree: one anonymous controller, one
view, its own layout, a stylesheet, a script, five images, and no tables, services or resx.
reforge scores nothing in it (finding 11), so the run steered by reading every non-image file
in full and by rendering the page live in this container (finding 12). The target shape
([`health.md`](../../../src/Sections/Humans.Tour/Docs/health.md), written this run before
any scan) finds the code right as built: the page does what its copy claims (finding 13), the
stylesheet and script carry nothing dead (finding 14), and the one thing that could break
silently — the CSP nonce on the layout's inline script — is held by a `_ViewImports` line
that looked unused (finding 7).

No behavior bugs. What the section had instead: no test that CI ever runs (finding 8 — its
only tests sit in the local-only integration suite), a section doc that narrated its own
birth and never said how a visitor reaches the page (finding 3), and comments across the
csproj, `Section.cs`, `SectionNav.cs` and the integration test that told the migration story
instead of describing the code (findings 2, 4, 5, 6). One claim outside the section had gone
false (finding 1). Two out-of-section references to Tour are strings the activation scan
cannot see (findings 9, 10).

## Ranked findings

Value = bug surface removed, then concepts removed, then words removed.

| # | Finding | Value | Disposition |
|---|---|---|---|
| 1 | `tests/Humans.Integration.Tests/Controllers/AdminLayoutRenderTests.cs` claimed cross-part layout resolution "was already proven by `Humans.Tour/Views/Tour/_ViewStart.cshtml`, which resolves `_Layout` out of `Humans.Web`" — false since the landing-page redesign; Tour's `_ViewStart` names its own `_TourLayout`. Reworded to the section views that do set `Layout = "_Layout"` inline (CityPlanning's ContainerMap, Debug's ColorPalette and WidgetGallery). | med | **worked** |
| 2 | `Humans.Tour.csproj` comments named `Humans.Infrastructure` (no such project), "design §5" / "§7a" (Decorator Caching and Audit — unrelated), and "the Scanner shape, G5-SECTION-TEMPLATE.md preconditions". Kept the two facts the code cannot say (why the Usings; why the InternalsVisibleTo), dropped the dead names. | low | **worked** |
| 3 | `Docs/Tour.md`: the Status line narrated history ("Migrated — born in … burn-demo PR, 2026-08-12"); Concepts never named how anyone reaches the page though `flag-on-change` asks for reachability review; the invariants cited `TourPageRenderTests` without saying it is local-only and never runs in CI; triggers watched only the section while the doc asserts about Shell's dashboard and Onboarding's Welcome page. Rewritten end to end against the code, triggers widened. | med | **worked** |
| 4 | `SectionNav.cs` summary narrated the pre-nobodies-collective/Humans#1090 state ("was the anonymous-only Tour link Shell's `_Layout.cshtml` named…"). Kept the design reason: signed-out only because the signed-in nav has no Tour slot; members get the dashboard card; the weight sorts it last, beside Legal. | low | **worked** |
| 5 | `Section.cs`: the summary restated the convention ("at the project root by convention. Discovered by Shell"), carried "(the Scanner shape)", and the method body comment restated the summary. Trimmed to the one thing the code cannot say: an empty `Register` still matters because implementing `ISection` is what makes the assembly a section. | low | **worked** |
| 6 | `TourPageRenderTests.cs`: "Tour is the first section created directly in src/Sections (never lived in Shell)" and "(Peter, 2026-08-13)" narrated history; the second test's name claims the nav but its body cannot tell the nav link from the body link on `/Welcome` (both are `href="/Tour"`). Narration cut; the nav rule gets its CI pin under finding 8. | low | **worked** |
| 7 | The layout's inline `tour-js` script is only CSP-legal because `_ViewImports.cshtml`'s `@addTagHelper *, Humans.Base` brings `NonceTagHelper`, which stamps the nonce on every `<script>`. Nothing in the section said so, and the line looks unused (Tour names no Base tag helper). Verified at runtime this session: both script tags carry the nonce. Recorded where the line is, and in `health.md`'s load-bearing weirdness. | med | **worked** |
| 8 | No CI-run test pinned any Tour invariant: `TourPageRenderTests` is in the local-only integration suite (build.yml excludes it), and `EndpointAuthorizationTests` proves only that the controller carries *some* auth attribute. New `tests/Humans.Tour.Tests` pins the anonymous route and the signed-out-only nav item. | high | **worked** |
| 9 | Shell's `Views/Home/Dashboard.cshtml` builds the Tour tile by string (`Controller = "Tour"`), the reference class the activation scan cannot see; the `ISectionMemberDashboard` seam exists (Tickets, Governance, Shifts, GoogleIntegration use it). Contributing the tile from Tour moves it out of the "Your stuff" grid into the contributed area above it — a placement choice. | low | **Needs Peter** |
| 10 | Onboarding's `Views/Welcome/Index.cshtml` links `/Tour` by URL — the same class of invisible reference; the fix sits in Onboarding. | low | sweep queue |
| 11 | reforge scores no Tour entry ("every type in that assembly is unscored"), so the selector ranks it last and a run steers by reading alone. Already filed as peterdrier/Humans#1601. | — | no change |
| 12 | Skill lesson, Phase 4: runtime verification is achievable in this container. A Postgres 16 cluster is installed (`pg_ctlcluster 16 main start`; user and database `humans`), and `dotnet run --project src/Humans.Web --no-build` boots once `Authentication__Google__ClientId` / `ClientSecret` hold placeholder values (`DevAuth:Enabled` is already true in Development; the app listens on http://localhost:7101; `GET /dev/login/volunteer` signs in). This run rendered `/Tour`, `/Welcome` and `/` anonymous and signed in. Proposed edit: Phase 4 names the recipe; peterdrier/Humans#1609 assumes it cannot be met. | — | **Needs Peter** |
| 13 | Copy claims checked against the section list: every chapter's feature names a shipped section (People → Users/Consent/Gdpr/Onboarding; Organize → Teams/Shifts/Camps/CityPlanning/Guide; Money → Store/Expenses/Budget/Holded; Govern → Governance/AuditLog; Communicate → Email/Campaigns/MailerLite/Notifications; At the gate → Tickets/TicketTailor/Gate/Scanner/EarlyEntry); "mutation testing" has four Stryker configs under `tests/`. | — | no change |
| 14 | `tour.css` and `tour.js`: every class the stylesheet defines is used by the views or the script and vice versa (scripted diff); the animation-frame guard, reduced-motion gates and lazy slide loading read correct. | — | no change |
| 15 | Out-of-section files naming Tour all resolve (`docs/README.md`, `section-activation.md`, the `_Layout` / `Dashboard` / `Welcome` comments, `ticket-vendor-integration.md`, the two section templates) except `memory/process/scoped-inner-loop-tests.md`, whose "a section with no test project (e.g. Tour)" finding 8 makes false. Corrected in the sweep commit, the one place a run may touch `memory/`. | low | **worked** (sweep) |

## Worked

Findings 1–8 and 15, one commit per strike:

- `doctor(Tour): add the target shape` — `Docs/health.md`, written before any scan.
- `doctor(Tour): rewrite the section doc against the code` — finding 3.
- `doctor(Tour): cut comments that narrate history or restate the code; fix the stale layout claim` — findings 1, 2, 4, 5, 6, 7.
- `doctor(Tour): pin the anonymous route and the signed-out-only nav link` — finding 8.
- the sweep commit — finding 15.

Surfaces hit: **localization** — none; Tour is deliberately English-only and no user-facing
string changed. **Authorization** — no behavior change; finding 8 pins the existing
`[AllowAnonymous]`. **Audit** — n/a; the section performs no actions. **GDPR** — n/a; no
personal data. **Invariant doc** — rewritten (finding 3) and consistent with the struck code.
**Migrations** — none; no schema. **Navigation** — unchanged; the three entry points were
rendered live and match the doc. **Tests** — 5 passing in the new `tests/Humans.Tour.Tests`;
the local-only `TourPageRenderTests` compiles but did not run here. Views untouched apart
from the `_ViewImports` comment; the page was rendered live during assessment.

## Skipped

Findings 9–14 (dispositions above; 9 and 12 to Needs Peter, 10 to the sweep queue, 11, 13,
14 need no change).

Sections passed over as blocked (open doctor PRs on peterdrier/Humans): Auth #1575,
Backdoor #1586, Budget #1565, Calendar #1578, Campaigns #1564, Camps #1561, Consent #1572,
Debug #1598, EarlyEntry #1593, Email #1587, Feedback #1566, Gate #1574,
GoogleIntegration #1599, Governance #1580, Holded #1583, Monitor #1582, Scanner #1608,
Search #1602, Shifts #1596, Stripe #1588, Teams #1594, TicketTailor #1595, Tickets #1589,
Users #1603. Feature-active and set aside: AuditLog, Gdpr, Notifications, Rideshare.

## Threads

Raw per-thread finding counts before consolidation into the ranked list above. Every
non-image file in the inventory was read in full on the main thread during 3b, before the
target was written, so the reading threads ran on main (self-run): dispatching would have
re-read a twelve-file inventory already in context for no saving.

| Thread | How it ran | Model | Findings |
|---|---|---|---|
| Shape | main | session default (see cost comment) | 3 → findings 9, 10, 11 |
| Behavior & bugs | main; the page rendered live in this container (finding 12's recipe) | session default (see cost comment) | 2 → findings 7 (non-bug, recorded as a landmine), 13; nav, tile and header-bar invariants confirmed at runtime |
| Freshness | main (self-run) | session default (see cost comment) | 4 → findings 1, 2, 3, 15 |
| Conformance | main, detectors run by hand | session default (see cost comment) | 0 — `section-file-layout` clean; `resource-key-prefix` n/a (no resx) |
| Tests | main (self-run) | session default (see cost comment) | 2 → findings 6, 8 |
| Prose & surface | main; CSS-vs-view class diff scripted; razor-lint on both views (0 warnings; 12 terminology infos on marketing copy); InspectCode not in this container | session default (see cost comment) | 1 → finding 14 |
| History | main (self-run) | session default (see cost comment) | 3 → findings 3, 4, 6 |
| Comments | main (self-run) | session default (see cost comment) | 3 → findings 2, 5, 7 |
| Inbox | main; fork-only scope | session default (see cost comment) | 0 — no open Tour issues on peterdrier/Humans (list + search); no Tour rows in `debt-ledger.yml`; no `Docs/debt.yml`; in-app issues not reachable (empty local database) |
| Second opinion (comment/claim batch, findings 1, 2, 4, 5, 6, 7) | subagent | session default (see cost comment) | 1 reject — the csproj change carried an `InternalsVisibleTo` for a test project that did not exist yet; moved into finding 8's commit, where the project lands |

Independence check: pass — 7, 8, 9, 10 came from the target (its invariants and seams);
1–6 from the reads.

## Retro

**What the selector/rubric got wrong:** the pick was right but the mechanism was degenerate:
Tour was the last never-doctored section, unscored, so "median of the tier" was a tier of one
and the selector's own `WARNING: no score … ranked last` contradicted its pick. After this run
the never-doctored tier is empty and `UPCOMING` says only "previously-doctored (judgment)" —
finding 16, below.

**Wasted motion:** the first app boot returned 500 on an empty Google `ClientId`; the time
went to reading `Program.cs` before the placeholder env vars were tried. And the reviewer's
one reject was self-inflicted: the "comment-only" csproj batch had an `InternalsVisibleTo`
for a project that did not exist yet, which the reviewer rightly refused to approve on a
promise. Ordering the strikes so the attribute rides with the project would have avoided the
resubmit.

**What the assessment missed that striking revealed:** the nav-rule test in the integration
suite (finding 6) was reading `href="/Tour"` off a page that carries two of them; only writing
the unit test made it obvious that the existing test could not tell them apart. And finding
15 — the memory atom that names Tour as the section without a test project — surfaced from
finding 8, not from the freshness read: an out-of-section grep for the section name is worth
re-running after the strikes, not only before.

**Target diff:** none possible — this is the section's first doctor pass and `health.md` was
written this run. Next run gets the first real diff.

One auto-compaction occurred mid-Phase 4; Phase 5's mandatory re-read of Phases 5–7 was
applied after it.

- 16 — Phase 2: with the never-doctored tier empty, the selector's previously-doctored path is
  marked "(judgment)". Proposed edit: name the rule — oldest run date first, ties by lowest
  reforge score — so the next unattended run does not have to invent one.

## Needs Peter

- [ ] 9 — move the dashboard Tour tile behind `ISectionMemberDashboard` (out of the "Your stuff" grid), or keep Shell naming Tour by string?
- [ ] 12 — Phase 4: name the Postgres + placeholder-OAuth boot recipe in the skill, and close peterdrier/Humans#1609 as answered?
- [ ] 16 — Phase 2: previously-doctored pick rule — oldest run date, ties by lowest score?

## Sweep queue

- debt: Onboarding — `src/Sections/Humans.Onboarding/Views/Welcome/Index.cshtml` links `/Tour` by raw URL; a route reference the section-activation scan cannot see. Use `asp-controller="Tour"` / `asp-action="Index"` (finding 10, /section-doctor on Tour 2026-09-06).
- debt: Scanner — `tests/Humans.Scanner.Tests/Humans.Scanner.Tests.csproj`'s trailing comment says `tests/Directory.Build.props` excludes the project from shared `TestDbContextFactory` / `CapturingLogger` compile items; no such exclusion exists in that props file (found by /section-doctor on Tour 2026-09-06 while modelling the Tour test project on it).

## File coverage

`generated` = excluded from review per the skill.

**Changed:**

- `src/Sections/Humans.Tour/Docs/Tour.md` — changed
- `src/Sections/Humans.Tour/Docs/health.md` — changed (new)
- `src/Sections/Humans.Tour/Humans.Tour.csproj` — changed
- `src/Sections/Humans.Tour/Section.cs` — changed
- `src/Sections/Humans.Tour/SectionNav.cs` — changed
- `src/Sections/Humans.Tour/Views/_ViewImports.cshtml` — changed
- `tests/Humans.Integration.Tests/Controllers/TourPageRenderTests.cs` — changed
- `tests/Humans.Integration.Tests/Controllers/AdminLayoutRenderTests.cs` — changed (outside the inventory; finding 1)
- `tests/Humans.Tour.Tests/Humans.Tour.Tests.csproj` — changed (new)
- `tests/Humans.Tour.Tests/TourControllerTests.cs` — changed (new)
- `tests/Humans.Tour.Tests/SectionNavTests.cs` — changed (new)
- `Humans.slnx` — changed (test project entry)
- `memory/process/scoped-inner-loop-tests.md` — changed (sweep commit; finding 15)

**Reviewed, unchanged:**

- `src/Sections/Humans.Tour/Controllers/TourController.cs` — reviewed
- `src/Sections/Humans.Tour/Docs/authorization.md` — reviewed
- `src/Sections/Humans.Tour/Views/Shared/_TourLayout.cshtml` — reviewed
- `src/Sections/Humans.Tour/Views/Tour/Index.cshtml` — reviewed
- `src/Sections/Humans.Tour/Views/Tour/_ViewStart.cshtml` — reviewed
- `src/Sections/Humans.Tour/wwwroot/css/tour.css` — reviewed
- `src/Sections/Humans.Tour/wwwroot/js/tour.js` — reviewed
- `src/Sections/Humans.Tour/wwwroot/img/hero-3.webp` — reviewed
- `src/Sections/Humans.Tour/wwwroot/img/hero-10.webp` — reviewed
- `src/Sections/Humans.Tour/wwwroot/img/hero-13.webp` — reviewed
- `src/Sections/Humans.Tour/wwwroot/img/whatis-2.jpg` — reviewed
- `src/Sections/Humans.Tour/wwwroot/img/whatis-3.jpg` — reviewed
