# Budget — section doctor, 2026-08-30

- **Invocation:** unattended daily run, no arguments. Phase 8 (inline round) skipped.
- **Anchor commit:** `443fe373` (`fork/main` = peterdrier/Humans)
- **Branch:** `section-doctor/2026-08-30T191541Z` (cloud run, repo root — no worktree)
- **Budget:** 2.5h, single PR.
- **PR:** peterdrier/Humans#1565

## Assessment summary

First doctor pass over Budget, the never-doctored-tier median pick (reforge 436,
loc=4773, cog p95=7, max=20 at `BudgetRepository.MaterializeProjectedWeeks`,
maxClassLoc=1096 `BudgetRepository`). The section is structurally sound: clean
G5 layout, a tight `IBudgetServiceRead` contract, perfect resx parity,
sound navigation, an immutable baseline migration. What it had instead was
**doc drift** (claims about audit methods, architecture tests and features that
were never built — findings 1, 9), **unpinned invariants** in exactly the
places most likely to lose real data or money (findings 2–4), the **VAT math
duplicated across the service and both category views** (finding 7), and genuine
gaps the run does not decide on its own: a closed-year gate the doc promises
and the ticketing mutation paths don't enforce (finding 5), and an
authorization hole masked by today's data shape (finding 6).

Independence check: pass — the target-derived items (the closed-year sync gate,
the ticketing-authorization gap, the RestoreYear fork, the VAT-ownership dedup)
came from reading the 3c target and health.md invariants against the code, not
from the scan output.

## Ranked findings

Value = bug surface removed, then concepts removed, then words removed.

| # | Finding | Value | Disposition |
|---|---|---|---|
| 1 | **Section docs asserted things the code never did.** `Budget.md` claimed an `AddAuditLogAsync` repository method and an architecture test that don't exist (the real audit surface is three read methods), named `IUserService` where the code injects `IUserServiceRead`, and carried #1188 nav-removal and ruling-43 as future work already shipped. `Budget-feature.md` documented never-built VAT-line-item auto-generation, donations handling and quarter-split behavior, plus a `TicketingBudgetService.GetProjectionsAsync` description and a then-`FinanceController` home that both moved. `data-access.md` named the wrong Teams interface twice; `Tickets.md:244` pointed at tests that had moved; `health.md` listed a closed seam as open; `debt.yml` carried a resolved item. All corrected. | high | **worked** (commit 1) |
| 2 | **"Sync never touches hand-entered line items" — the invariant most likely to cause real data loss — had no test in any project.** Every `SyncTicketingActualsAsync` test asserted generated items appear; none seeded a manual line item and asserted it survives. Pinned: manual and `Projected:`-prefixed-but-manual items survive a sync byte-identical. | high | **worked** (commit 5) |
| 3 | **The section's only pure logic — `ComputeBudgetSummary` and the VAT breakout — had zero tests.** Pinned: cashflow-only items excluded from totals, VAT split out into its own income/expense slices, net balances to zero on a symmetric fixture. | high | **worked** (commit 5) |
| 4 | **The closed-year gate was pinned on exactly one of ~14 repository mutations.** Now a 12-case theory over create/update/delete of group, category and line item, department sync, ticketing-group ensure and projection update — every one refuses on a closed year. | high | **worked** (commit 5) |
| 5 | **`health.md` says every mutation gates on closed years; `SyncTicketingActualsAsync` and `RefreshTicketingProjectionsAsync` don't.** The nightly job and the admin refresh can mutate a closed year's ticketing items. Doc-vs-code where the code may be the wrong side — changed neither, per the skip-and-queue rule. | high | **Needs Peter** |
| 6 | **`BudgetAuthorizationHandler` has no `IsTicketingGroup` check.** A ticketing category carrying a department TeamId the user coordinates would pass the edit gate, and no test would notice — `IsTicketingGroup` is hardcoded `false` in all 15 authorization theory cases. Masked today only because scaffolded ticketing categories have null TeamId. Privilege change — skipped, queued. | high | **Needs Peter** |
| 7 | **The VAT math lived in the service plus near-identical `@functions` blocks in both `Views/Budget/CategoryDetail.cshtml` and `Views/BudgetAdmin/CategoryDetail.cshtml`.** Now one home — `BudgetService.ComputeVatPortion` / `ComputeVatSettlementDate` — with `VatProjection.ForLineItems` doing view-model shaping only; both `@functions` blocks deleted. Reviewer-approved after one rework (v1 had moved the tax math into Models, contradicting health.md's ownership claim). | med | **worked** (commit 4) |
| 8 | **`BudgetAuthorizationHandler` injected write-capable `IBudgetService` for one read call** (`GetEffectiveCoordinatorTeamIdsAsync`) — reforge's `writeCapableInterfaceUsedReadOnly`. Narrowed to `IBudgetServiceRead`; test substitute narrowed to match. Reviewer-approved. | med | **worked** (commit 3) |
| 9 | **~40 sites of comment drift:** a dangling cref, boxed ASCII region banners, issue references as load-bearing rationale, comments naming types that moved (`UsersDbContext`, `ITeamServiceRead`), a Codex-P1 war story where an invariant statement belongs, doc-comment padding across Section/nav/job files, and four stale view comments. Fixed or deleted. | med | **worked** (commit 2) |
| 10 | **Two mock-assertion delegation tests tested NSubstitute, not behavior**, and the surviving ordering test (`UpdateProjectionAndRefreshAsync_SavesParametersThenRefreshes`) never asserted the order its name claims — two unordered `Received(1)` calls pass with the calls reversed. Trimmed the two; upgraded the survivor to `Received.InOrder`. Reviewer-approved. | med | **worked** (commit 6) |
| 11 | **`RestoreYearAsync` is unreachable from the UI.** It exists on the repository and service, but archived years render no Restore control anywhere; the only caller is `DevelopmentBudgetSeeder`. Fork: add the button, or drop the API. | med | **Needs Peter** |
| 12 | **Member-facing views hardcode English strings throughout** (`Views/Budget/Index.cshtml`, member `CategoryDetail.cshtml`, `Summary.cshtml`) against a resx set with perfect 6-culture parity for the keys it does carry. Backfill is ×6 cultures and a product decision — queued, recorded in the section's `debt.yml`. | med | **Needs Peter** |
| 13 | **Chart palette/loader duplicated** across `Views/BudgetAdmin/YearDetail.cshtml` (hardcoded hex + `'#fff'`) and `Views/Budget/Summary.cshtml` (CSS variables), each loading Chart.js separately. Recorded in the section's `debt.yml`. | low | **skipped → debt.yml** |
| 14 | **Test gaps left open from the invariant matrix:** the GDPR export contributor has never executed under test (merge-chain path stubbed empty in the ctor), restricted-group drill-in only covers the not-a-coordinator case, ticketing-group hiding and the group/category/line-item audit-row writes are unpinned, `GetActualTicketsSold`'s Notes round-trip unpinned. Recorded in the section's `debt.yml`. | med | **skipped → debt.yml** |
| 15 | **Unused `Humans.Shifts.Contracts` project reference** in `Humans.Budget.Tests.csproj` — no test names a Shifts type. Removed. | low | **worked** (commit 5) |
| 16 | **`IsoMonday` helper duplicated ×3** across service/repository week loops. Considered and rejected: below the value bar, and health.md already carries the week-loop duplication as an accepted cost. | low | **rejected** |
| 17 | **Round-trip trivia tests** (`CreateLineItemAsync_PersistsLineItem`, `UpdateLineItemAsync_PersistsChanges`) pin no doc invariant. Harmless; left in place. | low | **skipped** |
| 18 | **`EnumStringStabilityTests` is weaker than its docstring** — `Contain` passes on a rename plus re-add; it does catch a plain rename. Left in place. | low | **skipped** |
| 19 | **Carried from 2026-08-28-Agent's sweep queue:** `memory/process/debt-ledger-additions`'s routing table sends `tests/` fixes to the central ledger, but section-doctor practice (Agent, this run) records a section's own test-project gaps in the section's `debt.yml`. The item asks which reading is intended — question-shaped, so this run's sweep did not self-answer it by editing the atom. | — | **Needs Peter** |
| 20 | **Proposed amendment (Phase 3e):** write the ranked findings list to `$RUNDIR/findings.md` at 3e time. This run's list existed only in working context when a mid-run compaction hit; the numbering had to be reconstructed from the thread transcripts before Phase 5 could write it durably. One file write at 3e removes the risk. | — | **Needs Peter** |
| 21 | **Proposed amendment (Phase 3d/threads):** have each reading thread state key counts it derives (resx keys, theory cases) as a command it ran, not a number it believes. This run's Conformance thread reported 45 resx keys where the file has 21 (verified by parsing the resx); its CLEAN verdict survived, but the number would have poisoned any doc that copied it. | — | **Needs Peter** |
| 22 | **The ticketing sync paths write no audit rows** (raised by Codex round 2 on peterdrier/Humans#1565, verified: every `AddFieldAudit`/`AddDescriptionAudit` call site precedes them). `SyncTicketingActualsAsync` and `RefreshTicketingProjectionsAsync` mutate line items and learned projection parameters with no `BudgetAuditLog` entry — automation acting invisibly. Third missing guarantee on the same two methods (with findings 5, 6): the class is "ticketing paths exempt from the section's cross-cutting guarantees", and one ruling can cover it. Recorded as a seam; neither side changed. | high | **Needs Peter** |
| 23 | **`UpdateYearAsync` skips the closed-year gate** (raised by Codex round 4 on peterdrier/Humans#1565, verified: no `EnsureYearNotClosedAsync` call in the method, and the Edit Year form in `Admin.cshtml` renders outside the status conditionals, for Closed years too). A finance admin can rename a Closed year's identifier and name — audited, but ungated. Same decision class as finding 5: gate it, or exempt year-metadata edits in the invariant. Recorded as a seam; neither side changed. | medium | **Needs Peter** |

## Worked

Findings 1–4, 7–10, 15 — six commits, each `doctor(Budget):`-prefixed; strikes 3, 4
and 10 (non-mechanical) carry the second-opinion reviewer's approval, with one
rework on finding 7. Budget tests green after every commit (61 pass at head).

## Skipped + why

- Findings 5, 6, 11, 12 — the four skip-and-queue classes this run hit
  (doc-vs-code-where-code-may-be-wrong, privilege change, public-surface/feature
  fork, product-scoped localization backfill). Each is one Needs-Peter line below.
- Findings 13, 14 — real but below this run's value bar; recorded in the
  section's `debt.yml` so `/debt-sweep` serves them.
- Findings 16–18 — considered, rejected or left as harmless; reasons in the table.
- **Sections passed over as blocked:** Campaigns (peterdrier/Humans#1564),
  Camps (peterdrier/Humans#1561).

## Retro

- **What did the selector/rubric get wrong?** Nothing observed — build ok, score
  source clean, and the median never-doctored pick surfaced high-value findings
  (two Needs-Peter items are genuine behavior questions, not doc trivia).
- **Wasted motion:** the History thread asserted `Docs/features/Budget-feature.md`
  does not exist while the Freshness thread was reviewing it; verifying the
  contradiction cost a round. The post-compaction reconstruction of the 3e
  ranked list (finding 20) was the other avoidable cost. A first
  `Received.InOrder` attempt tripped MA0134 (async results unobserved in the
  lambda) — one build round; `_ =` discards fixed it.
- **What did the assessment miss that striking revealed?** The reviewer's reject
  of VAT-dedup v1 caught two things the strike had missed: an out-of-scope
  comment deletion, and that moving the tax math into Models contradicted the
  ownership claim health.md makes. The score-blind gate earned its keep.
- **What does the target diff say?** Nothing — Budget is never-doctored; the
  target was written fresh this run, so there is no previous target to diff.
- Two mid-run compactions occurred (the cost report places them in the assess
  phase and the commit-5 strike); Phase 5's mandated re-read of Phases 5–7
  re-anchored the bookkeeping (the rule worked as designed).

## Needs Peter

- [ ] 5 — closed-year gate on `SyncTicketingActualsAsync`/`RefreshTicketingProjectionsAsync`: gate them, or amend the doc?
- [ ] 23 — closed-year gate on `UpdateYearAsync` (Edit Year rename, renders for Closed years): gate it, or exempt year-metadata edits? Same ruling class as 5.
- [ ] 22 — same two methods write no audit rows: audit them (one ruling with 5 covers the ticketing-exemption class), or exempt them in the invariants?
- [ ] 6 — should `BudgetAuthorizationHandler` deny coordinator edits in ticketing groups (`IsTicketingGroup` check + theory case)?
- [ ] 11 — `RestoreYearAsync`: add a Restore control for archived years, or drop the API?
- [ ] 12 — member-view localization backfill (all six cultures): do it, or accept as admin-adjacent?
- [ ] 19 — `debt-ledger-additions` routing for `tests/Humans.<X>.Tests`: section `debt.yml` or central ledger?
- [ ] 20 — amend Phase 3e to persist the ranked list to `$RUNDIR`?
- [ ] 21 — amend the thread prompts to require derived counts be command-produced?

## Sweep queue

None — this run's debt items were written directly to their ledgers in this PR
(Budget's `debt.yml`; the sweep commit applied 2026-08-28-Agent's and
2026-08-28-Settings's queued items to Humans.Agent's `debt.yml` and the central
ledger, skipping Agent's `memory:` item as question-shaped → finding 19).

## Threads

| Thread | How | Model | Findings |
|---|---|---|---|
| Freshness | subagent | claude-opus-5 | 17 |
| History | subagent | claude-opus-5 | 27 (one wrong: claimed `Budget-feature.md` absent while Freshness reviewed it) |
| Tests | subagent | claude-opus-5 | invariant matrix + 8 |
| Comments | subagent | claude-opus-5 | 17 |
| Prose & surface | subagent | claude-haiku-4-5 | 7 |
| Conformance | subagent | claude-haiku-4-5 | 0 (clean; misreported the resx key count 45 vs the actual 21 — verdict unaffected) |
| Inbox | main thread, inline | claude-fable-5 | 3 items: section debt.yml item resolved → struck; fork issues #1562/#1554 off-section → keep, no action |

## File coverage

`src/Sections/Humans.Budget.Contracts/`:

- `BudgetYearStatus.cs` — reviewed
- `ExpenditureType.cs` — reviewed
- `Humans.Budget.Contracts.csproj` — changed
- `IBudgetDemoSeeder.cs` — reviewed
- `IBudgetServiceRead.cs` — changed

`src/Sections/Humans.Budget/`:

- `Authorization/BudgetAuthorizationHandler.cs` — changed
- `Authorization/BudgetOperationRequirement.cs` — reviewed
- `BudgetResource.ca.resx` — reviewed
- `BudgetResource.cs` — reviewed
- `BudgetResource.de.resx` — reviewed
- `BudgetResource.es.resx` — reviewed
- `BudgetResource.fr.resx` — reviewed
- `BudgetResource.it.resx` — reviewed
- `BudgetResource.resx` — reviewed
- `Controllers/BudgetAdminController.cs` — reviewed
- `Controllers/BudgetController.cs` — reviewed
- `Data/BudgetDbContext.cs` — changed
- `Data/BudgetDbContextFactory.cs` — reviewed
- `Data/BudgetRepository.cs` — changed
- `Data/Configurations/BudgetAuditLogConfiguration.cs` — changed
- `Data/Configurations/BudgetCategoryConfiguration.cs` — reviewed
- `Data/Configurations/BudgetGroupConfiguration.cs` — reviewed
- `Data/Configurations/BudgetLineItemConfiguration.cs` — reviewed
- `Data/Configurations/BudgetYearConfiguration.cs` — reviewed
- `Data/Configurations/TicketingProjectionConfiguration.cs` — reviewed
- `Data/IBudgetRepository.cs` — changed
- `Data/Migrations/20260809142337_BaselineBudget.Designer.cs` — reviewed (immutable)
- `Data/Migrations/20260809142337_BaselineBudget.cs` — reviewed (immutable)
- `Data/Migrations/BudgetDbContextModelSnapshot.cs` — reviewed (generated)
- `Docs/Budget.md` — changed
- `Docs/authorization.md` — reviewed
- `Docs/data-access.md` — changed
- `Docs/debt.yml` — changed
- `Docs/features/Budget-feature.md` — changed
- `Docs/health.md` — changed (regenerated 3c + history row)
- `Domain/BudgetAuditLog.cs` — reviewed
- `Domain/BudgetCategory.cs` — reviewed
- `Domain/BudgetGroup.cs` — reviewed
- `Domain/BudgetLineItem.cs` — reviewed
- `Domain/BudgetYear.cs` — reviewed
- `Domain/TicketingProjection.cs` — reviewed
- `Humans.Budget.csproj` — changed
- `Jobs/TicketingBudgetSyncJob.cs` — changed
- `Models/BudgetViewModels.cs` — changed
- `Models/StatusBadgeExtensions.cs` — changed
- `Properties/AssemblyInfo.cs` — reviewed
- `Section.cs` — changed
- `SectionAdminNav.cs` — changed
- `SectionJobs.cs` — changed
- `SectionNav.cs` — changed
- `Services/BudgetService.cs` — changed
- `Services/DevelopmentBudgetSeeder.cs` — changed
- `Services/Dtos/BudgetDtos.cs` — reviewed
- `Services/IBudgetService.cs` — reviewed
- `Services/ITicketingBudgetService.cs` — changed
- `Services/TicketingBudgetService.cs` — reviewed
- `Views/Budget/CategoryDetail.cshtml` — changed
- `Views/Budget/Index.cshtml` — reviewed
- `Views/Budget/NoActiveBudget.cshtml` — reviewed
- `Views/Budget/Summary.cshtml` — reviewed
- `Views/BudgetAdmin/Admin.cshtml` — reviewed
- `Views/BudgetAdmin/AuditLog.cshtml` — reviewed
- `Views/BudgetAdmin/CashFlow.cshtml` — reviewed
- `Views/BudgetAdmin/CategoryDetail.cshtml` — changed
- `Views/BudgetAdmin/NoActiveYear.cshtml` — reviewed
- `Views/BudgetAdmin/YearDetail.cshtml` — changed
- `Views/BudgetAdmin/_ViewStart.cshtml` — reviewed
- `Views/_ViewImports.cshtml` — changed

`tests/Humans.Budget.Tests/`:

- `Authorization/BudgetAuthorizationHandlerTests.cs` — changed
- `Enums/EnumStringStabilityTests.cs` — reviewed
- `Humans.Budget.Tests.csproj` — changed
- `Services/BudgetServiceTests.cs` — changed
- `Services/TicketingBudgetServiceTests.cs` — changed

Elsewhere:

- `docs/guide/Budget.md` — reviewed
- `src/Sections/Humans.Tickets/Docs/Tickets.md` — changed (one stale moved-tests claim, finding 1)
