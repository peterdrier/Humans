# Finance — section-doctor run, 2026-08-18

- **Invocation:** `/section-doctor --section=Finance` (interactive; plan skipped by `--section`)
- **Anchor:** `41fd7374d` (`origin/main` at branch point)
- **Branch:** `section-doctor/2026-08-18T161402Z`
- **Budget:** 2.5 h standard; started 16:14 UTC
- **PR:** [#1374](https://github.com/peterdrier/Humans/pull/1374)

**Benchmark run.** Peter ran this as a measurement of the rewritten Phase 3 (#1373, merged as
`41fd7374d`) against a section a previous run had already worked, with that previous run's output
blindfolded. Per the instructions the blocked set was taken as empty and Finance was worked
directly; no `gh pr list` was run and no section-doctor branch, PR, worktree or diff was read.
Every Glob/Grep/`git ls-files` was scoped to this run's worktree by explicit path.

**Blindfold breach, disclosed.** Late in the run — after Peter answered the open Needs-Peter item
with "drop both columns" — I ran `git log --all -S 'RawPayload'` to verify the column had never
held real data before dropping it irreversibly. `--all` includes remote branches, and the output
carried one commit subject from the blocked set: `doctor(finance): drop the RawPayload column`.
Subject line only; no diff, body, PR, or file was opened, and nothing else from that branch was
seen before or after. What it tells me is that the other run also dropped this column.

**What it does and does not contaminate.** The finding that `RawPayload` is dead came from reading
the section at 3b, hours earlier and before any tool ran; the decision to drop it is Peter's, made
just now. So the substance is independent. What is no longer clean is the *claim* of a sealed
benchmark: for this one fact, the two runs are known to agree, and that agreement is no longer
evidence. Read the RawPayload drop as corroborated rather than independently reproduced.

**Lesson:** `git log --all` is not blindfold-safe. A scoped history check wants
`git log <branch> -S …`, not `--all`.

## Assessment summary

Finance is the treasurer's side of the money story: Holded purchase invoices attributed to budget
categories, the Holded expense accounts those categories are attributed to, and the creditor
accounts the organisation owes its members on. Its contract methods group into **four question
shapes** (target §2). The file structure already matches what those shapes imply — the work was inside the
files, not between them.

What the target surfaced that no scan reported:

- **The section's own doc led with 23 routes it does not serve.** Finance.md's Routing section was
  majority `BudgetAdminController`'s, which the same doc said two sections further down, plus a
  `POST /Finance/Creditors/Resync` route deleted in #1267.
- **Two claims stated but never built.** Nothing in the section ever sets
  `HoldedCategoryMap.IsActive = false` or writes `ArchivedAt`, and `MapDoc` hard-codes
  `RawPayload` to `"{}"` where the data model promised the full Holded JSON.
- **A shape mismatch inside one method.** `GetProvisioningPlanAsync` seeded its number-collision
  set from the whole map up front but built its *tag*-collision set as the walk encountered rows.
  A ToAdd category sorting ahead of a mapped one whose normalized tag it collides with was handed
  that tag verbatim — two active rows sharing a tag, and tag attribution then arbitrary. Fixed;
  the new test fails without the fix.
- **A published type with no consumer.** `HoldedPaymentInfo` sat on the contracts leaf; nothing
  outside `Service.cs` referenced it, and inside it only two aggregates were ever read.
- **One row shape, two view models.** `CreditorAccountBindingVm` and
  `UnresolvedCreditorBindingVm` were the same three fields, one per half of one page.

What the scans added: `Service.cs` at 54% mutation score, `FinanceController` and `Repository` at
0%, three surviving `ReasonFor` conditions that turned out to be unreachable, and one unread
`HoldedMatchEntry.AccountNum`.

**Independence check: pass.** Target-derived: the provisioning tag-collision bug (a shape mismatch
between how the method seeds its two collision sets), the two stated-but-unbuilt claims
(`IsActive`/`ArchivedAt`, `RawPayload`), `HoldedPaymentInfo` as an abstraction covering only part of
its domain, and the duplicate binding view model. Scan-added: the mutation-score gaps on
`Service.cs`, `FinanceController` and `Repository`, the three unreachable `ReasonFor` conditions, and
the unread `HoldedMatchEntry.AccountNum`. Neither failure symptom applies — the list does not trace
wholly to tools, and it does cite shape mismatches and spec-vs-reality deltas.

**Conformance:** `docs/architecture/section-conformance.yml` has one rule (`section-file-layout`).
All three of its detectors pass for Finance — no top-level entry outside the allowed set, `Docs/`
present, migrations under `Data/Migrations/`. No new rule is proposed.

## Worked

1. **Derived the target** into `src/Sections/Humans.Finance/Docs/health.md` — Finance's first, so
   nothing to diff against.
2. **Finance.md freshness** — dropped the 23 Budget routes for a pointer to `Budget.md`, removed
   the deleted `Creditors/Resync` route and swept that claim to
   `docs/features/global/background-jobs.md`, corrected the archive and `RawPayload` claims, and
   added the two freshness triggers the stalest block needed (Holded's `Service.cs`, Budget's
   `BudgetAdminController`).
3. **Deletions** — `HoldedPaymentInfo` and the `LedgerPayments` helper (aggregates inlined),
   the duplicate binding view model, `HoldedMatchEntry.AccountNum`, two unreachable `ReasonFor`
   conditions, a shadowed local, and a repeated Madrid-zone lookup.
4. **Provisioning tag collisions** — `usedTags` now seeded from every active map row, like the
   numbers.
5. **Comment mass** — 340 comment lines against 1269 of code, most of it decision histories
   re-derived in prose at 10–25 lines a block, against a 1–3 line rule. Trimmed to their
   load-bearing lines; the decisions themselves now live once in `health.md`. Fixed a stale claim
   two of them carried (the Holded connector is not "Base and stays there").
6. **Tests** — 55 → 88. `FinanceController` had none at all; `Repository` had none at all. Built
   the invariant matrix and closed its three gaps (`ProvisionAsync`, the creditor-block bounds,
   the "sync never deletes" negative rule), then re-checked the boundary test and found it did not
   actually discriminate — a row reached the list through either filter — and tightened it.
7. **Stryker scoped** to exclude `Data/Migrations`: 292 of 599 surviving mutants came from
   migration bodies no test can or should kill.

## Skipped, and why

- **Plan rows passed over as blocked:** none — `--section` skipped plan selection, and the blocked
  set was taken as empty per the run instructions.
- **`FinanceController`'s remaining 43 mutants** are almost entirely `SetSuccess`/`SetError`/
  `logger.LogError` message strings on the POST actions. Pinning them means asserting flash text,
  which is asserting the mock. Left.
- **EF configurations, `FinanceDbContext`, `Section.cs`, `FinanceDbContextFactory`** (~59 mutants)
  are declarative wiring; mutation testing has nothing useful to say about them.
- **`[ExternalWrite]` on the Holded surface** (`debt-ledger.yml`, added 2026-08-05) covers
  `EnsureCreditorContactAsync` and `CreateExpenseAccountAsync` here *and* several
  `ExpenseReportService` transitions. Marking only the Finance half leaves HUM0033 enforcing
  inconsistently, so it stays a cross-section item. Ranked, not worked.
- **Schema and public-surface items** → Needs Peter below.

## Post-PR rounds

Two rounds landed after the PR opened, both recorded here rather than folded into the narrative
above:

1. **Peter's inline answers** — the superseded design doc deleted, the creditor DTOs narrowed.
2. **An external audit against the anchor** — it verified every finding, the size table and three of
   the new tests (by breaking the implementation), and returned twelve things still open. All twelve
   are fixed: the stale tail of Finance.md that the headline fix left behind (its Feature-1 class
   names, the budget-mutation invariant, Negative Access, Triggers, Current-violations,
   Touch-and-clean, and a Cross-Section Dependencies block naming a Tickets dependency the section
   does not have — rebuilt from the csproj); "stayed in Shell as BudgetAdminController" in two
   places; `HoldedSyncJob`'s location in two more; the last two `Creditors/Resync` strings (both
   since superseded by main — #1371 deleted `controller-architecture-audit.md` outright and replaced
   `authorization-inventory.md`'s sweep-changelog paragraph, which is where both strings lived, so
   the merge took main's side and this run's edits to those two files are gone); the
   statement's false 12-month window in both the view and the contract; the Creditors empty state
   pointing at the wrong sync; the missing `holded_creditor_contacts` data-model entry; an unused
   test-project reference; the missing 3e verdict line; two self-counts; the sharp edge on the open
   Needs-Peter item; and four tests for the GDPR export slice and `GetDocSyncInfoAsync`.

**What that says about the run.** The audit's largest finding is the shape of my own miss: I fixed
the *headline* of a stale doc — the 23-route table — and stopped, leaving eight smaller claims in
the same file, in sections I had read. Freshness work is not done when the worst claim is fixed; it
is done when the file is consistent with itself. The `IBudgetServiceRead` split had shipped and four
separate paragraphs still called it future work, which is exactly the kind of sediment this process
exists to remove.

## Retro

**What the rubric got wrong.** Nothing in the replan rubric would have picked Finance: it scores
254, mid-pack, and its structure is fine. Every finding above came from reading behaviour against
the docs, which is what the inverted Phase 3 is for. This is the same lesson Guide taught on
2026-08-17, now confirmed on a second section.

**Wasted motion.** Two Stryker runs were thrown away — the first because
`--coverage-analysis` is not a CLI flag (it is config-only), the second because migrations
dominated the score and made it unreadable. Both were cheap, but writing the config first would
have saved a round.

**What striking revealed that the assessment missed.** The boundary test written in the strike
phase passed for the wrong reason: the list unions three sources, so a row arrived through the
contact filter even with the mirror filter's bound wrong. The assessment had the invariant right
and the test wrong, and only re-running Stryker against the new test caught it. **A new test that
does not move the mutants it was written for is not passing — it is not discriminating.**

**What the target diff says.** Nothing to say this run: Finance had no `health.md` on `origin/main`,
so this is the first target and there is no previous one to diff. The next run on Finance gets the
first real signal.

**The reviewer gate was not obtained.** Phase 4.4 calls for a second-opinion subagent on
non-mechanical deletions. This session carries a standing instruction not to call the Agent tool
unless asked, and it was not asked, so no reviewer was dispatched. Following the skill's own
2026-08-17 lesson, the checklist was worked on the main thread and the affected commits say
**self-reviewed** in their message. No review happened; do not read one into this run.

## File coverage

Every path in the 3a inventory, with its disposition. `generated` is the two exempt kinds,
`*.Designer.cs` and the model snapshot; migration `.cs` bodies are not exempt and were read.

- `src/Sections/Humans.Finance.Contracts/CreditorContactSource.cs` — reviewed
- `src/Sections/Humans.Finance.Contracts/CreditorLedgerLine.cs` — changed
- `src/Sections/Humans.Finance.Contracts/HoldedCreditorAdminDtos.cs` — changed
- `src/Sections/Humans.Finance.Contracts/HoldedCreditorStatus.cs` — changed
- `src/Sections/Humans.Finance.Contracts/HoldedDtos.cs` — reviewed
- `src/Sections/Humans.Finance.Contracts/HoldedPaymentInfo.cs` — deleted
- `src/Sections/Humans.Finance.Contracts/Humans.Finance.Contracts.csproj` — changed
- `src/Sections/Humans.Finance.Contracts/IHoldedFinanceService.cs` — changed
- `src/Sections/Humans.Finance/Controllers/FinanceController.cs` — changed
- `src/Sections/Humans.Finance/Data/Configurations/HoldedCategoryMapConfiguration.cs` — reviewed
- `src/Sections/Humans.Finance/Data/Configurations/HoldedCreditorContactConfiguration.cs` — reviewed
- `src/Sections/Humans.Finance/Data/Configurations/HoldedDocSyncStateConfiguration.cs` — reviewed
- `src/Sections/Humans.Finance/Data/Configurations/HoldedExpenseDocConfiguration.cs` — reviewed
- `src/Sections/Humans.Finance/Data/FinanceDbContext.cs` — changed
- `src/Sections/Humans.Finance/Data/FinanceDbContextFactory.cs` — reviewed
- `src/Sections/Humans.Finance/Data/IHoldedRepository.cs` — reviewed
- `src/Sections/Humans.Finance/Data/Migrations/20260715103643_BaselineFinance.Designer.cs` — generated
- `src/Sections/Humans.Finance/Data/Migrations/20260715103643_BaselineFinance.cs` — reviewed
- `src/Sections/Humans.Finance/Data/Migrations/20260810195350_HoldedExpenseDocIsApproved.Designer.cs` — generated
- `src/Sections/Humans.Finance/Data/Migrations/20260810195350_HoldedExpenseDocIsApproved.cs` — reviewed
- `src/Sections/Humans.Finance/Data/Migrations/20260810204942_HoldedMirrorMovesToHoldedSection.Designer.cs` — generated
- `src/Sections/Humans.Finance/Data/Migrations/20260810204942_HoldedMirrorMovesToHoldedSection.cs` — reviewed
- `src/Sections/Humans.Finance/Data/Migrations/FinanceDbContextModelSnapshot.cs` — generated
- `src/Sections/Humans.Finance/Data/Repository.cs` — reviewed
- `src/Sections/Humans.Finance/Docs/2026-06-15-holded-ledger-single-source-design.md` — deleted
- `src/Sections/Humans.Finance/Docs/Finance.md` — changed
- `src/Sections/Humans.Finance/Docs/health.md` — changed
- `src/Sections/Humans.Finance/Domain/HoldedCategoryMap.cs` — reviewed
- `src/Sections/Humans.Finance/Domain/HoldedCreditorContact.cs` — reviewed
- `src/Sections/Humans.Finance/Domain/HoldedDocSyncState.cs` — reviewed
- `src/Sections/Humans.Finance/Domain/HoldedExpenseDoc.cs` — reviewed
- `src/Sections/Humans.Finance/Domain/HoldedMatchSource.cs` — reviewed
- `src/Sections/Humans.Finance/Domain/HoldedMatchStatus.cs` — reviewed
- `src/Sections/Humans.Finance/Humans.Finance.csproj` — reviewed
- `src/Sections/Humans.Finance/Models/CreditorAccountRowVm.cs` — changed
- `src/Sections/Humans.Finance/Properties/AssemblyInfo.cs` — reviewed
- `src/Sections/Humans.Finance/Section.cs` — changed
- `src/Sections/Humans.Finance/Services/HoldedMatcher.cs` — changed
- `src/Sections/Humans.Finance/Services/Service.cs` — changed
- `src/Sections/Humans.Finance/Views/Finance/CreditorStatement.cshtml` — changed
- `src/Sections/Humans.Finance/Views/Finance/Creditors.cshtml` — changed
- `src/Sections/Humans.Finance/Views/Finance/HoldedAccounts.cshtml` — reviewed
- `src/Sections/Humans.Finance/Views/Finance/HoldedUnmatched.cshtml` — reviewed
- `src/Sections/Humans.Finance/Views/Finance/_ViewStart.cshtml` — reviewed
- `src/Sections/Humans.Finance/Views/_ViewImports.cshtml` — reviewed
- `tests/Humans.Finance.Tests/FinanceArchitectureTests.cs` — reviewed
- `tests/Humans.Finance.Tests/FinanceControllerTests.cs` — changed
- `tests/Humans.Finance.Tests/HoldedMatcherTests.cs` — changed
- `tests/Humans.Finance.Tests/Humans.Finance.Tests.csproj` — changed
- `tests/Humans.Finance.Tests/RepositoryTests.cs` — changed
- `tests/Humans.Finance.Tests/ServiceTests.cs` — changed
- `tests/Humans.Finance.Tests/stryker-config.json` — changed

## Threads

| Thread | Ran | Notes |
|---|---|---|
| Shape | yes | main thread; source of items 3–5 |
| Behavior & bugs | yes | main thread; source of the provisioning fix and both unbuilt claims |
| Freshness | yes | main thread; doc-vs-code, trigger globs (both resolve), claim sweep |
| Conformance | yes | all three `section-file-layout` detectors, Finance-scoped: clean |
| Tests | yes | Stryker ×4 + the invariant matrix |
| Prose & surface | yes | InspectCode (Finance-scoped) + comment-density measurement + nav check |
| Inbox | yes | `debt-ledger.yml` (one Finance-touching item, ranked), open upstream issues (none Finance-scoped) |

No thread was skipped.

**Real-content pass.** The 2026-08-17 lesson says to run the section's real shipped content through
its real pipeline. Finance's input is the Holded API, not a checked-in content set, so there is no
offline equivalent to run — the nearest thing, the shape of Holded's JSON, is already pinned by the
Holded section's own parser tests and by the `supplierRecord: []` rule in the invariants. Recorded
rather than silently skipped.

**Nav check.** `/Finance/HoldedAccounts`, `/Finance/HoldedUnmatched` and `/Finance/Creditors` are
reachable only from Budget's `YearDetail` page, and each has a "Back to Finance" link to
`BudgetAdmin/Index`. With no active budget year, `NoActiveYear.cshtml` renders and the three pages
have no nav path at all. That view is Budget's, so it is a sweep-queue item, not a strike here.

## Size

Line counts against the anchor `41fd7374d`. Finance is the only section touched; the one
out-of-section edit is a single corrected line in `docs/features/global/background-jobs.md`.

| Bucket | Anchor | Now | Δ |
|---|---:|---:|---:|
| Production code (`Humans.Finance` + `.Contracts`, excl. `Docs/`, `Migrations/`) | 2388 | 2275 | **−113** |
| Migrations (untouched) | 1363 | 1363 | 0 |
| Section docs | 357 | 452 | +95 |
| Tests | 1419 | 2319 | +900 |
| **Total** | **5527** | **6409** | **+882** |

Includes Peter's answers from the inline round (superseded design doc deleted, creditor DTOs
narrowed) and the external audit's twelve findings.

**The section's code got smaller; the run grew the total, deliberately, in one place.** Production
code is down 113 lines with no behaviour removed — deletions, one collapse, and the comment trim,
against two lines added back by a corrected doc comment. Docs are up 95 on balance: `health.md` is
new, `Finance.md` lost its stale route table and gained the `holded_creditor_contacts` entry it
never had, and the superseded design doc is gone. Tests are up 900, which is the point of the run:
two of the section's five source files had *no test at all*, and mutation coverage went from 34.4%
to 57.9% on a like-for-like basis. No consolidation into or out of another section, so there is no
cross-section trade to state.

**Mutation score** (migrations excluded, like-for-like against the anchor):

| | Anchor | Now |
|---|---:|---:|
| Section | 34.4% | **57.9%** |
| `Services/Service.cs` | 54.2% | **71.8%** |
| `Controllers/FinanceController.cs` | 0% | **41.1%** |
| `Data/Repository.cs` | 0% | **80.6%** |
| `Services/HoldedMatcher.cs` | 86.7% | 86.7% |

Tests: 55 → 92, all green. `dotnet build Humans.slnx`, `dotnet test Humans.slnx` and
`dotnet format whitespace --verify-no-changes` all clean.

## Needs Peter

Answered inline 2026-08-18; applied items are ticked.

- [x] **1. Delete `Docs/2026-06-15-holded-ledger-single-source-design.md`.** Peter: yes. Deleted —
  83 lines, header said Superseded, successor is Holded's `2026-08-10-holded-v2-migration-design.md`,
  git keeps the history.
- [x] **5. `Balance` narrowed and the duplicate name dropped.** Peter: "decimal should be fine. Names
  can get confusing, Contact.name?" Applied: `HoldedCreditorStatus.Balance` and
  `HoldedCreditorLedger.Balance` are now `decimal` (both methods return null wholesale instead), and
  `HoldedCreditorStatus.SupplierAccountNum` is `int` for the same reason.
  `HoldedCreditorLedger.Name` was exactly `Contact?.Name`, so it is gone and the statement header
  reads through the contact. One caller updated (`ExpenseReportService`'s paid check), one view, four
  tests.

Still open — Peter asked back rather than deciding:

- [x] **2. `ArchivedAt` / `RawPayload` — answered: drop both columns.** Peter, 2026-08-18. **Done,
  but not on this branch** — see the blocker below.  Original question and provenance: Answered from #783's own history:
  `IsActive`/`ArchivedAt` are "retire-by-archive, since Holded account delete is unconfirmed" — a
  category deleted in Budget would archive its map row rather than delete a Holded account. The write
  path (`ArchiveCategoryMapAsync`) was cut in that same PR's review as having zero callers, "re-add
  with sign-off when archiving is wired up", and nobody ever did. `RawPayload` was "full Holded JSON
  for debugging + future field needs" — an escape hatch so a new field could be read without a
  re-pull. Neither has ever held a value. Decision still needed: wire archiving up, start writing the
  payload, or drop both columns.

  **Sharp edge for whoever implements it:** `holded_category_map` has a **unique** index on
  `BudgetCategoryId`. Archiving as documented keeps the archived row, so re-provisioning that
  category sends `AddCategoryMapAsync` into a second insert for the same `BudgetCategoryId` and the
  write fails. Wiring archiving up therefore means either reactivating the archived row instead of
  inserting, or dropping the unique index — it is not just re-adding `ArchiveCategoryMapAsync`.
  Moot now that archiving is not being wired up, but worth keeping for whoever revisits it.

  **Where the work is, and why it is not here.** The change is committed on the local branch
  `finance/drop-dead-holded-columns`: both properties removed, their EF configuration and the two
  assignment sites removed, the doc rows dropped, and one migration **generated** by
  `dotnet ef migrations add` and committed verbatim (two `DropColumn` calls; `Down()` re-adds both).
  Verified before generating: `RawPayload`'s only assignment in all of history is the literal `"{}"`,
  so nothing is lost.

  It is **not** in PR #1374 because it fails `NoDestructiveMigrationOps`, the ratchet that encodes
  [`no-drops-until-prod-verified`](../../../memory/architecture/no-drops-until-prod-verified.md).
  Its baseline header reads: *"When the test reports new violations, fix the code — do not add lines
  to silence it."* Silencing it is exactly what landing this needs, and that is a guardrail decision
  Peter owns, not one a doctor run takes. The same atom also sends hard-storage drops to a separate
  follow-up PR, and its authorized-exception list is maintained per incident by him.

  **Needs Peter, two parts:** (a) does this ship as its own PR, per the separate-PR rule? and
  (b) may the two locators be added to `NoDestructiveMigrationOps.baseline.txt` — the first
  approved-new-drop to go through that file, since its three existing entries are all
  already-shipped history?

  Note while it is open: `IsActive` survives as the other half of the retirement mechanism. Nothing
  ever sets it false, so it is permanently `true` and its three `.Where(m => m.IsActive)` filters are
  no-ops. Dropping it is a bigger change than the two dead columns — it has real read sites — so it
  was not touched.
- [ ] **3. A read interface — "there shouldn't be many folks calling finance in the first place?"**
  Correct: five call sites in four sections (Budget's actuals column, Expenses' controller and push
  service, Holded's controller and nightly sync). Recommendation: **drop this item.** A read/write
  split earns its keep when many consumers hold a write surface they never use; here two of the four
  consumers legitimately write.
- [ ] **4. What is in the tuple?** `(IReadOnlyList<HoldedCreditorAccountRow> Accounts,
  IReadOnlyList<CreditorContactBinding> Unresolved)` — the two halves of one partition of the
  bindings. Expenses takes `.Accounts` and discards `Unresolved`; Finance's own page uses both.
  Agreed a tuple is not itself a problem; the only real question is whether the two halves want
  separate methods, and they do not — they must come from one snapshot.
- [ ] **6. The freshness-trigger conformance rule.** Finance.md asserted at length about Holded's
  ledger service and Budget's controller while its `freshness:triggers` block watched neither, so
  nothing could ever flag those paragraphs stale. The proposed detector: a doc whose prose names
  another section's type but whose trigger globs stay inside its own section. Low value if this was
  a one-off; drop unless it shows up on another section.

## Sweep queue

- `lesson: 2026-08-18 — a new test that does not move the mutants it was written for is not passing, it is not discriminating. Re-run the tool thread against the new tests before the PR; Finance's creditor-block boundary test passed for the wrong reason because the list unions three sources.`
- `lesson: 2026-08-18 — Stryker's --coverage-analysis is config-only, not a CLI flag, and a section-scoped run must exclude Data/Migrations: migration bodies were 292 of 599 surviving mutants and made the score unreadable. Write the config first.`
- `lesson: 2026-08-18 — when the reviewer gate cannot be obtained (here: a session-level instruction against dispatching agents), say so in the commit message as well as the run file. A commit that lands unreviewed should say so where the diff is read, not only where the run is.`
- `lesson: 2026-08-18 — `git log --all` is not blindfold-safe: on a run with a blocked branch set it surfaced a commit subject from that set during an unrelated history check. Scope history checks to a named branch or ref, never --all.`
- `lesson: 2026-08-18 — dotnet ef migrations add/remove with --no-build reads whatever assembly the startup project last built, so it generated an empty migration and then `remove --force` walked back an already-merged one. Always full-build before either command; recover a mis-removal with git checkout of the Migrations folder, never by hand-editing.`
- `lesson: 2026-08-18 — fixing a doc's headline stale claim is not fixing the doc. Finance.md's 23-route table was corrected while eight smaller claims in the same file survived, four of them describing a read-split that had already shipped. When a section doc is opened at all, read it end to end against the code and fix every claim, not the worst one.`
- `lesson: 2026-08-18 — rebuild a section doc's Cross-Section Dependencies from its .csproj project references, not from prose. Finance.md listed a Tickets dependency the section has not had since the controller split, and named IBudgetService (read+write) where the code injects IBudgetServiceRead.`
- `lesson: 2026-08-18 — run `dotnet format whitespace Humans.slnx --verify-no-changes` before the PR, not after CI says so. Two new test files failed code-quality on collection-expression line breaks the local build and the full test run both pass through; a green build is not the formatting gate.`
- `debt: docs/features/global/background-jobs.md and Finance.md both carried POST /Finance/Creditors/Resync, deleted in #1267 — a route can be removed from the controller and survive in every doc that names it, because nothing sweeps route strings. A repo-wide check that every /Route referenced in docs/ resolves to a live action would have caught it (found by /section-doctor on Finance).`
- `debt: Budget's Views/BudgetAdmin/NoActiveYear.cshtml has no links to the three /Finance/* pages that YearDetail.cshtml carries, so with no active budget year the Holded provisioning, unmatched-doc and creditor-accounts pages have no nav path at all (found by /section-doctor on Finance; the view is Budget's).`
