# Feedback — section doctor, 2026-08-31

- **Invocation:** unattended daily run, no arguments. Phase 8 (inline round) skipped.
- **Anchor commit:** `443fe373` (`origin/main`)
- **Branch:** `section-doctor/2026-08-31T071802Z` (cloud run, repo root — no worktree)
- **Budget:** 2.5h, single PR.
- **PR:** peterdrier/Humans#1566

## Assessment summary

First doctor pass over Feedback, the retired report-triage section
(nobodies-collective/Humans#977; reforge 152, loc=1597, cogP95=6, cogMax=14): admin-only
triage over historical `feedback_reports`/`feedback_messages`, a Backdoor API door, GDPR
export/erasure and the account-merge fold. The target shape
([`health.md`](../../../src/Sections/Humans.Feedback/Docs/health.md), written this run
before any scan) finds the structure right as built — the load-bearing weirdness
(email-before-persist, the `FeedbackSource` EF sentinel, the singleton repository) is
deliberate and now pinned by tests.

One real behavior gap (finding 9: three POST actions ignored `ModelState`, so an
unparseable status bound the enum default and silently reset the report to Open, with an
audit row saying so). The bulk of the section's illness was documentation written for a
section that no longer exists: nonexistent symbols, a false resource-location claim
repeated in three places, deletion inventories narrating #977, and comments retelling
fixed bugs. The test suite asserted little of what the section actually promises — the
GDPR erasure contract, the email-before-persist order and every audit write were
unpinned (findings 11–19).

## Ranked findings

Value = bug surface removed, then concepts removed, then words removed.

| # | Finding | Value | Disposition |
|---|---|---|---|
| 1 | **Repository doc-truth cluster:** orphaned/false `ReassignToUserAsync` summary stacked on `EraseForUserAsync` (incl. a "returns total count" claim about a method returning screenshot keys), "tracked report" wording over an `AsNoTracking`+`Attach` reality, a false ordering claim on `GetForUserExportAsync`, FK-remark dedup, "after the Feedback migration lands" clauses, a region banner misnaming GDPR erasure. | med | **worked** |
| 2 | **`Feedback.md` named symbols that do not exist:** `ITeamService.GetTeamNamesByIdsAsync`, `ITeamService`, `INotificationService.SendAsync` → the real `ITeamServiceRead` and `INotificationEmitter` surfaces. | med | **worked** |
| 3 | **`FeedbackResource.cs` + `health.md` claimed `Email_FeedbackResponse_*` keys live in Base's `SharedResource`/Base `EmailRenderer`** — they live in the Email section (`EmailResource`, `EmailRenderer`). Also in `FeedbackPageRenderTests` remarks. All three fixed. | med | **worked** |
| 4 | **`Feedback.md` arch-test paragraph claimed 5 pins; 3 exist.** Rewritten to the real three. | low | **worked** |
| 5 | **`feedback-system.md` truth:** `GuideRoleResolver` doesn't reference FeedbackAdmin; the culture list omitted `ca`; the screenshot-storage claim described a dead `wwwroot` path (only `IFileStorage.DeleteAsync` consumes the keys). | med | **worked** |
| 6 | **Freshness trigger gaps:** `authorization.md` and `data-access.md` had no trigger blocks (the latter also omitted `IFileStorage`); `Feedback.md` missed the Backdoor controller and Base `RoleNames`/`CacheKeys`; `feedback-system.md` missed `Section*.cs` and the Backdoor controller. | med | **worked** |
| 7 | **Narration purge:** #977 deletion inventories and removed-route rosters in the specs, sentinel post-mortem tail, `nameof` origin story, `_ViewImports` stale design-§ pointers, restating JS comments, duplicated audit comments, stale "submit" in the arch test, bare/unqualified issue refs. | low | **worked** |
| 8 | **Dead surface** (reviewer-gated): `FeedbackDetailViewModel.UpdatedAt` (mapped, never rendered) and the inert `[JsonConverter]` on the form-bound status model deleted; `FeedbackController._userService` folded into the base `UserService` property. Reviewer **rejected** deleting `FeedbackListItemViewModel.ReporterUserId` (see finding 27) — kept. | med | **worked** (2 kept) |
| 9 | **`ModelState` ignored in UpdateStatus/SetGitHubIssue/UpdateAssignment** (PostMessage checks it): an unparseable `Status` bound the enum default and silently reset the report to Open — with an audit row recording the accident as an action; garbage `IssueNumber` silently cleared the link. Guards added mirroring PostMessage. | high | **worked** |
| 10 | **`_Detail.cshtml` localization:** status options rendered raw `@s` where Index uses `EnumDisplay`; the heading hardcoded "Category on URL" where `Feedback_CategoryOnPage` exists. Reused existing keys only — no new keys. | med | **worked** |
| 11 | **Email-before-persist unpinned** — an SMTP throw must leave no message row and no `LastAdminMessageAt`. Test added. | high | **worked** |
| 12 | **`EraseForUserAsync` unpinned** — own reports/messages deleted, screenshot keys handed to `IFileStorage.DeleteAsync`, foreign replies kept with authorship detached, triage links nulled. Test added. | high | **worked** |
| 13 | **No audit assertions** for status/assignment/GitHub writes, incl. the null-actor `"API"` fallback the Backdoor door relies on. Tests added. | med | **worked** |
| 14 | **Terminal-status test covered only `Resolved`** — `WontFix` takes the same branch. Now a theory over both. | low | **worked** |
| 15 | **Service-level actionable-count test duplicated the repository test byte-for-byte.** Repointed at what the service adds: the 2-min `FeedbackBadgeCount` cache and its eviction. | low | **worked** |
| 16 | **BurnerName-preference test couldn't fail** — it seeded no distinct BurnerName, so the fallback produced the same string. Now seeds `BurnerName` ≠ `DisplayName`. | med | **worked** |
| 17 | **`UpdateAssignmentAsync`/`SetGitHubIssueNumberAsync` untested** — two Backdoor-reachable verbs. Behavioral tests added (persist both columns, no-op short-circuit, set/clear). | med | **worked** |
| 18 | **`ReassignAsync` (account-merge fold) untested.** Test added: reports re-FK'd with the merge stamp, foreign-report replies re-FK'd. Writing it surfaced finding 26. | med | **worked** |
| 19 | **Reply-path side effects unasserted** — `IEmailService.SendAsync`, the notification dispatch, and the notifier-throw best-effort contract. Asserted. | med | **worked** |
| 20 | **Stray `Humans.Shifts.Contracts` ProjectReference** in the tests csproj; nothing uses it. Removed. | low | **worked** |
| 21 | **Proposed deleting `FeedbackSurface_ExposesNoReportCreationMethod`** as a name-substring absence test. Reviewer **rejected**: it is the sole executable pin of `health.md`'s first invariant ("no creation path") and its lead comment explains the seed-straight-to-DB fixture style. Kept. | low | **no change (reviewer reject)** |
| 22 | **The `debt-ledger.yml` entry "FeedbackService IFormFile/FileStream refactor to IFeedbackScreenshotStorage" (added 2026-06-12) is overtaken:** no `IFormFile`/`FileStream` since #977; only `IFileStorage.DeleteAsync` remains, which is the rule's compliant shape. Runs may not edit existing ledger entries → recommend deletion. | low | **Needs Peter** |
| 23 | **`reportLink` is dead end-to-end in the reply email chain:** `Email_FeedbackResponse_Body` renders `{0}`–`{2}` only; the renderer HTML-encodes and passes the link as an unused `{3}`. Dropping the argument spans `IEmailMessageFactory`/`IEmailRenderer` (Email's surface) plus this caller → sweep queue. | low | **queued** |
| 24 | **Hardcoded admin-facing strings on `/Feedback`**, which is not under `/Admin/*` so [`localization-admin-exempt`](../../../memory/code/localization-admin-exempt.md) is ambiguous: "(inactive)" (controller and `_Detail.cshtml`), the "Admin only" lock-icon title, the "GH #" label. Add keys ×6 cultures, or bless as exempt. | low | **Needs Peter** |
| 25 | **`GetOpenFeedbackIdsForUserAsync` loads full aggregates (incl. messages) to project ids.** Fine at this scale; a narrower query is new repository surface for little gain. Record only. | low | **no change** |
| 26 | **The merge fold moves only `UserId` and `SenderUserId`.** `ReassignToUserAsync` leaves `ResolvedByUserId`/`AssignedToUserId` pointing at the merged-away id — the same two columns erasure explicitly detaches. After a merge they reference a user id that no longer resolves, so the triage labels fall back to raw Guids. Whether the fold should re-point them (like erasure nulls them) is a behavior change → Peter. Surfaced writing finding 18's test. | med | **Needs Peter** |
| 27 | **`Index.cshtml` renders the `ReporterName` string next to a carried `ReporterUserId`** — the exact pair [`burnername-is-the-display-name`](../../../memory/architecture/burnername-is-the-display-name.md) forbids; the compliant fix is `<vc:human>` as `_Detail.cshtml` already does. Raised by the strike reviewer rejecting finding 8's deletion. In-section → `Docs/debt.yml` (created). | med | **ledgered** |

## Worked

Findings 1–20 (less the two reviewer keeps), one commit per strike batch:

- `doctor(Feedback): repository doc truth` — finding 1.
- `doctor(Feedback): section doc truth sweep` — findings 2–6.
- `doctor(Feedback): narration purge` — finding 7.
- `doctor(Feedback): ModelState guards + detail-panel localization` — findings 9, 10.
- `doctor(Feedback): dead-surface trim (reviewer-approved)` — finding 8.
- `doctor(Feedback): pin untested invariants in service tests` — findings 11–20.
- `doctor(Feedback): sweep merged run-file queues` — Phase 5 sweep (Settings/Agent queue items).

Surfaces hit: **localization** — finding 10 reuses existing keys present in all six cultures;
no new keys (finding 24 holds the open question). **Authorization** — no policy change; the
controller-level `AdminOnly` stands, and the new guards only tighten invalid input handling.
**Audit** — no new automated actions; finding 13 pins the existing audit writes. **GDPR** —
no new personal data; finding 12 pins the existing erasure contract. **Invariant doc** —
`Feedback.md` corrected in the same PR as the code it describes. **Migrations** — none; no
schema change (the skill forbids them regardless). **Navigation** — unchanged. **Tests** —
31 passing in `tests/Humans.Feedback.Tests`, up from 22. The `_Detail.cshtml` rendering
change is verified by build only: a cloud run has no live instance or preview DB, so the
runtime-verification gate for finding 10 is the PR's preview deploy.

## Skipped

Findings 21–27 (dispositions above: 21 and 25 no change, 23 to the sweep queue, 27
ledgered in the section's `Docs/debt.yml`, 22/24/26 to Needs Peter).

Sections passed over as blocked: **Budget** (peterdrier/Humans#1565), **Campaigns**
(peterdrier/Humans#1564), **Camps** (peterdrier/Humans#1561).

## Threads

Raw per-thread finding counts before consolidation into the ranked list above.

| Thread | How it ran | Model | Findings |
|---|---|---|---|
| Shape | main | session default (see cost comment) | 7 (S1–S7) → findings 8, 10, 25 |
| Behavior & bugs | main | session default (see cost comment) | 2 (B1, B2) → findings 9, 23 |
| Freshness | subagent doctor-reader | opus (low effort) | 11 → findings 2, 3, 4, 5, 6 |
| Conformance | subagent Explore (ran the yml detectors) | haiku | 0 hits — all 3 rules clean |
| Tests | subagent doctor-reader | opus (low effort) | 13 + invariant matrix → findings 11–21 |
| Prose & surface | subagent Explore | haiku | 4 → findings 10, 24; InspectCode unavailable in the cloud container (skipped-with-reason) |
| History | subagent doctor-reader | opus (low effort) | 18 → finding 7 |
| Comments | subagent doctor-reader | opus (low effort) | 22 → findings 1, 7 |
| Inbox | self-run on main | session default (see cost comment) | 1 → finding 22. Backlog was one ledger item + zero open fork issues — dispatch overhead exceeded the work. In-app issues unreachable from this container (no live instance/API key; per-session limitation, not a rule) |
| Second opinion (finding 8 + 21 deletions) | subagent doctor-reviewer | opus (low effort) | 3 approvals, 2 rejects — both rejects became findings 21 (keep) and 27 (inverse fix, ledgered) |

Independence check: pass (recorded in the ranked list at 3e; reforge contributed no ranked
item of its own — its hot rules are the inherent crosscut dependencies).

## Retro

**What the selector/rubric got wrong:** nothing. Feedback was the median of 22
never-doctored sections; right size for a first pass, finished inside budget.

**Wasted motion:** the Freshness thread attributed the guide page's trigger block to
`feedback-system.md`, which cost one failed edit before re-reading the actual file —
verify a claimed quote against the file before editing it. Building `prs.json` by hand
from the GitHub MCP tools (no `gh` in this container) cost a round of result-file parsing.

**What the assessment missed that striking revealed:** the target shape itself repeated
the section's false `SharedResource` claim (finding 3) until the Freshness thread caught
it — a target written from the section's own docs inherits their lies. Writing finding
18's test surfaced finding 26 (the merge fold's triage-column gap), and the reviewer's
reject of finding 8's `ReporterUserId` deletion surfaced finding 27 — two of this run's
three Needs-Peter/ledger items came from the strike phase, not the scan.

**Target diff:** none possible — first doctor pass; `health.md` was written this run.
Next run gets the first real diff.

One auto-compaction occurred mid-Phase 4; Phase 5's mandatory re-read of Phases 5–7 was
applied after it.

## Needs Peter

- [ ] 22 — delete the overtaken 2026-06-12 debt-ledger entry (IFormFile/FileStream refactor that #977 mooted)?
- [ ] 24 — `/Feedback` hardcoded admin-facing strings ("(inactive)", "Admin only", "GH #"): add keys ×6, or bless as admin-exempt?
- [ ] 26 — should the account-merge fold re-point `ResolvedByUserId`/`AssignedToUserId` to the target user (erasure already detaches them)?

## Sweep queue

- debt: Email — `reportLink` is dead end-to-end in the feedback-reply email: `Email_FeedbackResponse_Body` uses `{0}`–`{2}` in all six cultures and `EmailRenderer.RenderFeedbackResponse` passes the encoded link as an unused `{3}`. Drop the argument from `IEmailMessageFactory.FeedbackResponse` + `IEmailRenderer.RenderFeedbackResponse` + the Feedback caller (finding 23, 2026-08-31-Feedback)
- debt: Calendar — `Docs/Calendar.md:33` names `ITeamService.GetTeamNamesByIdsAsync`, which exists nowhere in the code; the live surface is `ITeamServiceRead.GetTeamsAsync`/`GetTeamAsync` (found fixing the same claim in Feedback, 2026-08-31-Feedback)
- debt: Shifts — `Docs/Shifts.md:322/345/354` name `ITeamService.GetTeamNamesByIdsAsync`, which exists nowhere in the code; the live surface is `ITeamServiceRead.GetTeamsAsync`/`GetTeamAsync` (found fixing the same claim in Feedback, 2026-08-31-Feedback)
- debt: `docs/sections/G5-SECTION-TEMPLATE.md:341` claims `Humans.Infrastructure/Services/EmailRenderer.cs`; the renderer lives in the Email section (found fixing Feedback's copy of the same claim, 2026-08-31-Feedback)
- debt: GoogleIntegration — `Docs/features/workspace-account-provisioning.md:47` lists the supported cultures as en/es/de/fr/it, omitting `ca` (found fixing the same list in Feedback's spec, 2026-08-31-Feedback)

## File coverage

`generated` = excluded from review per the skill.

**Changed:**
`src/Sections/Humans.Feedback/Controllers/FeedbackController.cs` ·
`src/Sections/Humans.Feedback/Data/Configurations/FeedbackReportConfiguration.cs` ·
`src/Sections/Humans.Feedback/Data/FeedbackDbContext.cs` ·
`src/Sections/Humans.Feedback/Data/FeedbackRepository.cs` ·
`src/Sections/Humans.Feedback/Data/IFeedbackRepository.cs` ·
`src/Sections/Humans.Feedback/Docs/Feedback.md` ·
`src/Sections/Humans.Feedback/Docs/authorization.md` ·
`src/Sections/Humans.Feedback/Docs/data-access.md` ·
`src/Sections/Humans.Feedback/Docs/debt.yml` (new) ·
`src/Sections/Humans.Feedback/Docs/features/feedback-system.md` ·
`src/Sections/Humans.Feedback/Docs/health.md` (new) ·
`src/Sections/Humans.Feedback/Domain/FeedbackReport.cs` ·
`src/Sections/Humans.Feedback/FeedbackResource.cs` ·
`src/Sections/Humans.Feedback/Models/FeedbackViewModels.cs` ·
`src/Sections/Humans.Feedback/Section.cs` ·
`src/Sections/Humans.Feedback/Services/AuditEntityTypes.cs` ·
`src/Sections/Humans.Feedback/Services/FeedbackService.cs` ·
`src/Sections/Humans.Feedback/Views/Feedback/Index.cshtml` ·
`src/Sections/Humans.Feedback/Views/Feedback/_Detail.cshtml` ·
`src/Sections/Humans.Feedback/Views/_ViewImports.cshtml` ·
`tests/Humans.Feedback.Tests/Architecture/FeedbackArchitectureTests.cs` ·
`tests/Humans.Feedback.Tests/Humans.Feedback.Tests.csproj` ·
`tests/Humans.Feedback.Tests/Services/FeedbackServiceTests.cs` ·
`tests/Humans.Integration.Tests/Controllers/FeedbackPageRenderTests.cs` ·
`docs/guide/Feedback.md` ·
outside the section (sweep commit): `docs/architecture/debt-ledger.yml` ·
`memory/process/debt-ledger-additions.md` ·
`src/Sections/Humans.Agent/Docs/debt.yml`

**Reviewed:**
`src/Sections/Humans.Backdoor/Controllers/BackdoorFeedbackController.cs` ·
`src/Sections/Humans.Feedback/Contracts/FeedbackCategory.cs` ·
`src/Sections/Humans.Feedback/Contracts/FeedbackDtos.cs` ·
`src/Sections/Humans.Feedback/Contracts/FeedbackStatus.cs` ·
`src/Sections/Humans.Feedback/Contracts/IFeedbackServiceRead.cs` ·
`src/Sections/Humans.Feedback/Contracts/IFeedbackTriage.cs` ·
`src/Sections/Humans.Feedback/Data/Configurations/FeedbackMessageConfiguration.cs` ·
`src/Sections/Humans.Feedback/Data/FeedbackDbContextFactory.cs` ·
`src/Sections/Humans.Feedback/Data/Migrations/20260809125242_BaselineFeedback.cs` ·
`src/Sections/Humans.Feedback/Domain/FeedbackMessage.cs` ·
`src/Sections/Humans.Feedback/Domain/FeedbackSource.cs` ·
`src/Sections/Humans.Feedback/FeedbackResource.resx` (+ `ca`/`de`/`es`/`fr`/`it` variants) ·
`src/Sections/Humans.Feedback/Humans.Feedback.csproj` ·
`src/Sections/Humans.Feedback/Properties/AssemblyInfo.cs` ·
`src/Sections/Humans.Feedback/SectionAdminNav.cs` ·
`src/Sections/Humans.Feedback/SectionAdminTiles.cs` ·
`tests/Humans.Feedback.Tests/Data/FeedbackRepositoryTests.cs` ·
`tests/Humans.Feedback.Tests/Enums/EnumStringStabilityTests.cs`

**Generated:**
`src/Sections/Humans.Feedback/Data/Migrations/20260809125242_BaselineFeedback.Designer.cs` ·
`src/Sections/Humans.Feedback/Data/Migrations/FeedbackDbContextModelSnapshot.cs`
