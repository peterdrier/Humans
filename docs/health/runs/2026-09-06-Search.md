# Search — section doctor, 2026-09-06

- **Invocation:** unattended daily run, no arguments. Phase 8 (inline round) skipped.
- **Anchor commit:** `10199a23` (`origin/main`)
- **Branch:** `section-doctor/2026-09-06T061610Z` (cloud run, repo root — no worktree)
- **Budget:** 2.5h, single PR.
- **PR:** peterdrier/Humans#pending

## Assessment summary

First doctor pass over Search, the orchestrator behind `/Search`: one controller, one
internal service fanning out to five sections' read interfaces, one view plus one partial,
no tables. The target shape
([`health.md`](../../../src/Sections/Humans.Search/Docs/health.md), written this run before
any scan) finds the code already at target — every structural item in §3 exists and nothing
exists beyond it; reforge reports only the five cross-section read edges and one DI
registration, nothing dead.

No behavior bugs. What the section had instead was documentation and comment drift of one
kind: prose written when things were in flight and never brought back to the present tense.
`Search.md` recorded the Shifts read-split as not landed, the nav as a `_Layout` link, and two
rulings that closed as designed as still open; every line-number and count citation had
drifted (findings 1–6, 8, 9). The same drift sat in a 45-line interface summary and a dozen
smaller comments (finding 7). Four small test gaps (10–13), two dead csproj references (13,
14), one precedence-shaped expression (15) and two hardcoded aria-labels (16) rounded out
the strikes.

## Ranked findings

Value = bug surface removed, then concepts removed, then words removed.

| # | Finding | Value | Disposition |
|---|---|---|---|
| 1 | **Docs said the Shifts read-split had not landed.** `Search.md`, `global-search.md`'s diagram and DTO table, and the section csproj comment all named `IShiftManagementService`; the code injects `IShiftManagementServiceRead`. Swept; `IShiftManagementServiceRead.cs` added to `Search.md`'s triggers. | high | **worked** |
| 2 | **nobodies-collective/Humans#993 and #998 framed as open gaps** in `ISearchService`, `SearchController`, `global-search.md` and one line of `Search.md`, while `Search.md` elsewhere already said both closed as designed. One story now, everywhere. | med | **worked** |
| 3 | **Nav claim stale:** `Search.md`, `Contracts/README.md` and `SectionNav`'s own summary said `_Layout` reaches `/Search` by `asp-controller`; the item is the section's `SectionNav : ISectionNav`. Fixed. | med | **worked** |
| 4 | **Dead citations:** a non-existent `memory/feedback_ef_ilike_not_toupper.md` (`ISearchService`, `global-search.md`), a `reforge.surface-score.json` per-section path list that does not exist (`Search.md`), and `ShiftRepositoryRotaSearchVisibilityTests` for what is `ShiftRepositoryRotaSearchTests` (`SearchServiceTests`). Dropped or corrected. | med | **worked** |
| 5 | **Frozen line-number and count citations in `Search.md`** (test counts, `:468`-style anchors, a seven-views enumeration of `<vc:human-search>` callers that is eight today). Dropped per [`no-derived-aggregates-in-docs`](../../../memory/process/no-derived-aggregates-in-docs.md); paths corrected. | med | **worked** |
| 6 | **History narration across the section's prose** — migration steps, deleted-symbol roll-calls, "no longer"/"last holdout" framing, sister-section comparisons, a malformed `nobodies-collective#987` ref. Cut to present tense in `Search.md`, `Contracts/README.md`, both csproj files, `SearchResource`, `_ViewImports`, `SearchArchitectureTests`, `SearchServiceTests` and `global-search.md`. | low | **worked** |
| 7 | **Comment rewrite:** the `ISearchService` summary restated the whole invariant doc; `Section.cs`, `SearchResource`, `SectionNav`, the DTO parameters, the controller and service line comments carried the same drift. Each now says what the thing is and points at `Docs/Search.md` for the rest. | low | **worked** |
| 8 | **`Search.md` test-coverage bullet** was an audit-status narrative. Replaced by a compact map of where each invariant is pinned. | low | **worked** |
| 9 | **`data-access.md` said every bucket comes from a cache.** Shifts is DB-backed (Postgres `ILike`). Fixed. | med | **worked** |
| 10 | **`SearchServiceTests` length-gate and unbounded-cap tests never asserted the Events section.** Both tightened. | med | **worked** |
| 11 | **The nav gate (authenticated, no `AppAccess`) was unpinned in CI.** `SectionNavTests` added: shows to a signed-in principal with no policy, hidden from anonymous. | med | **worked** |
| 12 | **`Index_ProjectsEveryHumanField_OntoTheSharedPartialsViewModel` named a deleted shape.** Deletion proposed and rejected by the second-opinion reviewer: it is the only CI-run pin that the human match context reaches the view untouched (the render test is local-only). Renamed to say what it pins; body unchanged. | low | **worked (rename)** |
| 13 | **Test csproj referenced `NodaTime` and `NodaTime.Testing`** with no usage. Removed; build clean. | low | **worked** |
| 14 | **Section csproj declared implicit usings for `Microsoft.AspNetCore.Http` and `.Routing`** that no symbol needed, plus an empty `ItemGroup` holding only a comment. Removed; build clean. | low | **worked** |
| 15 | **The Events gate read `flag && x is null or Event`** — correct (pattern combinator) but shaped like a precedence bug. Parenthesised; the feature-off test pins it. | low | **worked** |
| 16 | **Two hardcoded English aria-labels on `Index.cshtml`** (query box, filter chip group). The repo localizes aria-labels elsewhere (Calendar, Camps, Cantina). Two keys added to all six cultures; parity test green. | med | **worked** |
| 17 | **View-side half of the `Features:Events` gate is unpinned in CI** — the chip and heading hide, but only the local-only render tests could see it. Recorded in the section's new `Docs/debt.yml`. | low | **queued (section debt.yml)** |
| 18 | **`freshness-catalog.yml` has no Search entry.** Not a gap: the catalog walks `src/Sections/` as an editorial tree and `Search.md` carries its own `freshness:triggers` marker. Nothing to add. | — | **no change** |
| 19 | **Translations of "Camps"/"Teams" left as loanwords in de/fr/it.** Correct for this community's vocabulary. | — | **no change** |
| 20 | **Debt-ledger entry on the rota cold-cache read** reads as Search's but is Shifts-owned and correctly filed. Zero open Search issues on peterdrier/Humans. | — | **no change** |
| 21 | **`SearchPageRenderTests.Shells_nav_still_links_to_the_moved_controller`** narrates the move in its name. Outside the inventory (`Humans.Integration.Tests`). | low | **queued (sweep)** |
| 22 | **`CampControllerTests.Details_NonPublicSeason_AnonymousViewer_IsRefused` is `Skip`'d pending nobodies-collective/Humans#993**, which closed as designed — the skip will never lift. Camps-owned. | low | **queued (sweep)** |
| 23 | **The selector reported Tour with no reforge score and ranked it last.** reforge's compact output has no Tour line at all — it omits a section with nothing to report — and `select-section.py` reads absence as "unscored" and ranks it last with a warning. A section with no debt is the cleanest, not an unknown. Skill defect; see Needs Peter. | low | **Needs Peter** |
| 24 | **Phase 3d lesson:** the Tests thread declared the anonymous redirect unpinned because it read only the section's own test project; `SearchPageRenderTests.An_anonymous_visitor_cannot_reach_search` pins it locally. Proposed edit: the Tests lens greps `tests/Humans.Integration.Tests` for the section name before calling any invariant unpinned, and reports "pinned locally only" as its own column. | — | **Needs Peter** |
| 25 | **Phase 5 lesson:** the 2026-08-28-Settings sweep item "freshness-catalog has no Settings entry" was already satisfied by the catalog's `editorial_trees` walk, so this run's sweep skipped it as present; finding 18 above is the same mistake caught before queueing. Proposed edit: a `debt:` sweep item names the check that proves it absent, not only the file to edit. | — | **Needs Peter** |

## Worked

Findings 1–16, one commit per cluster:

- `doctor(Search): add the target shape (health.md)`.
- `doctor(Search): make the docs true to the code` — findings 1, 2, 3, 4, 5, 6, 8, 9.
- `doctor(Search): cut history narration from comments and the csproj` — findings 2, 3, 4, 6, 7, 14, 15.
- `doctor(Search): pin the nav gate and the Events bucket in tests` — findings 10, 11, 13.
- `doctor(Search): localize the two aria-labels on /Search` — findings 12 (rename), 16.
- `doctor(Search): record the unpinned view-side Events gate in Docs/debt.yml` — finding 17.
- `doctor(Search): sweep queued items from merged run files` — the sweep commit.

Surfaces hit: **localization** — two new keys in all six cultures (finding 16);
`SectionResourceParityTests` green. **Authorization** — no behavior change; finding 11 pins
the existing authenticated-only nav gate. **Audit** — unchanged; the section performs no
actions. **GDPR** — untouched; the section holds no personal data. **Invariant doc** —
rewritten (findings 1–6, 8) and consistent with the code as struck. **Migrations** — none.
**Navigation** — unchanged in behavior; documented correctly now (finding 3). **Tests** —
`tests/Humans.Search.Tests` green, two methods added. The one view change (finding 16) is
build-verified and parity-tested, not rendered live: the container has no database, so
`dotnet run` was not possible. The PR's preview deploy is the place to see the labels.

## Skipped

Findings 17–25 (dispositions above; 17 to the section's `debt.yml`, 21 and 22 to the sweep
queue, 23–25 to Needs Peter).

Sections passed over as blocked (open doctor PR): Auth, Backdoor, Budget, Calendar,
Campaigns, Camps, Consent, Debug, EarlyEntry, Email, Feedback, Gate, GoogleIntegration,
Governance, Holded, Monitor, Shifts, Stripe, Teams, TicketTailor, Tickets. Set aside as
feature-active: AuditLog, Gdpr, Notifications, Rideshare.

## Threads

Raw per-thread finding counts before consolidation into the ranked list above.

| Thread | How it ran | Model | Findings |
|---|---|---|---|
| Shape | main | session default (see cost comment) | 5 → findings 13, 14, 15; two deliberately-not-done notes folded into the target |
| Behavior & bugs | main | session default (see cost comment) | 0 bugs; 5 notes → finding 9 |
| Freshness | subagent (`doctor-reader`) | opus (low effort) | 20 → findings 1–5, 18; did not reach `user-search-overhaul.md` / `person-search.md`, read by main (no finding) |
| Tests | subagent (`doctor-reader`) | opus (low effort) | 8 → findings 10–13, 17; one withdrawn (finding 24) |
| History | subagent (`doctor-reader`) | opus (low effort) | 40 → findings 1, 3, 4, 6 |
| Comments | subagent (`doctor-reader`) | opus (low effort) | 48 → findings 2, 7 |
| Prose & surface + Conformance | subagent (one combined run) | haiku | 9 + conformance table (both detectors clean) → findings 16, 19 |
| Inbox | main | session default (see cost comment) | 1 → finding 20. Fork-only scope: zero open Search issues on peterdrier/Humans; ledger reviewed. In-app issues not reachable from this container |
| Second opinion (finding 12) | subagent (`doctor-reviewer`) | session default (see cost comment) | 1 reject with rename accepted |

Independence check: pass — findings 1, 2, 3, 9, 12 and 15 came from the target and from
reading, not from a scan.

## Retro

**What the selector/rubric got wrong:** it reported Tour as unscored and ranked it last
(finding 23). The pick itself was clean: Search was the median of four never-doctored
sections after setting aside none as feature-active, and the right size for a first pass.

**Wasted motion:** one thread claim cost a verification round — the Tests thread's
"anonymous redirect unpinned" (finding 24). One tool-shaped detour: the clone's `origin`
pointed at nobodies-collective/Humans (peterdrier/Humans#1591, already filed); remotes were
renamed before branching, no new issue.

**What the assessment missed that striking revealed:** finding 18 was queued for the sweep
before checking how the freshness catalog actually covers section docs; the sweep phase
caught the same shape in a prior run's queue (finding 25). Checking the target's mechanism
before queueing an edit to it is cheaper than sweeping a wrong item forever.

**Target diff:** none possible — first doctor pass; `health.md` was written this run.

One auto-compaction occurred at the start of Phase 4; Phase 5's mandatory re-read of Phases
5–7 was applied after it.

## Needs Peter

- [ ] 12 — the human match-context pass-through test stays under its new name per the reviewer; delete it anyway?
- [ ] 17 — pin the view-side `Features:Events` gate with a `Humans.Web.Tests` render case, or leave it to the local-only render tests?
- [ ] 23 — Phase 2: treat a section absent from reforge's compact output as score 0 (cleanest), not "unscored, ranked last"? Filed as an issue on peterdrier/Humans per the routine's instruction (number in the PR body).
- [ ] 24 — Phase 3d: Tests lens greps `tests/Humans.Integration.Tests` for the section before declaring an invariant unpinned, and reports "pinned locally only" as its own column?
- [ ] 25 — Phase 5: a `debt:` sweep item must name the check that proves it absent, not only the target file?

## Sweep queue

- debt: `tests/Humans.Integration.Tests/Controllers/SearchPageRenderTests.cs` — `Shells_nav_still_links_to_the_moved_controller` narrates the move; rename to what it pins (the member nav carries a `/Search` link) (finding 21, 2026-09-06-Search)
- debt: Camps — `tests/Humans.Camps.Tests/Controllers/CampControllerTests.cs` `Details_NonPublicSeason_AnonymousViewer_IsRefused` is `Skip`'d pending nobodies-collective/Humans#993, which closed as designed; the test asserts the ruling's opposite and will never un-skip — delete it or invert it (finding 22, 2026-09-06-Search)

## File coverage

`generated` = excluded from review per the skill; this section has none.

**Changed:**
`src/Sections/Humans.Search/Contracts/README.md` ·
`src/Sections/Humans.Search/Controllers/SearchController.cs` ·
`src/Sections/Humans.Search/Docs/Search.md` ·
`src/Sections/Humans.Search/Docs/data-access.md` ·
`src/Sections/Humans.Search/Docs/debt.yml` (new) ·
`src/Sections/Humans.Search/Docs/health.md` (new) ·
`src/Sections/Humans.Search/Humans.Search.csproj` ·
`src/Sections/Humans.Search/Models/SearchViewModels.cs` ·
`src/Sections/Humans.Search/SearchResource.ca.resx` ·
`src/Sections/Humans.Search/SearchResource.cs` ·
`src/Sections/Humans.Search/SearchResource.de.resx` ·
`src/Sections/Humans.Search/SearchResource.es.resx` ·
`src/Sections/Humans.Search/SearchResource.fr.resx` ·
`src/Sections/Humans.Search/SearchResource.it.resx` ·
`src/Sections/Humans.Search/SearchResource.resx` ·
`src/Sections/Humans.Search/Section.cs` ·
`src/Sections/Humans.Search/SectionNav.cs` ·
`src/Sections/Humans.Search/Services/Dtos/GlobalSearchResults.cs` ·
`src/Sections/Humans.Search/Services/ISearchService.cs` ·
`src/Sections/Humans.Search/Services/SearchService.cs` ·
`src/Sections/Humans.Search/Views/Search/Index.cshtml` ·
`src/Sections/Humans.Search/Views/Search/_GlobalSearchSection.cshtml` ·
`src/Sections/Humans.Search/Views/_ViewImports.cshtml` ·
`tests/Humans.Search.Tests/Architecture/SearchArchitectureTests.cs` ·
`tests/Humans.Search.Tests/Controllers/SearchControllerTests.cs` ·
`tests/Humans.Search.Tests/Humans.Search.Tests.csproj` ·
`tests/Humans.Search.Tests/SectionNavTests.cs` (new) ·
`tests/Humans.Search.Tests/Services/SearchServiceTests.cs` ·
outside the section: `docs/features/global/global-search.md` ·
sweep commit only: `docs/architecture/dependency-graph.md` ·
`memory/process/debt-ledger-additions.md` ·
`src/Sections/Humans.Agent/Docs/debt.yml` ·
`src/Sections/Humans.Users/Docs/debt.yml`

**Reviewed:**
`src/Sections/Humans.Search/Docs/authorization.md` ·
`src/Sections/Humans.Search/Properties/AssemblyInfo.cs`

**Generated:** none.
