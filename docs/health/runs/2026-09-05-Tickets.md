# section-doctor — Tickets — 2026-09-05

- Invocation: unattended daily run (cloud), no arguments; Phase 8 skipped per the stored prompt.
- Anchor commit: `10199a23` (origin/main at branch point); branch `section-doctor/2026-09-05T013332Z`.
- Budget: standard daily (~2.5 h).
- PR: peterdrier/Humans#1589

## Assessment summary

First doctoring of Tickets (never-doctored tier, median surface-score pick). Target shape written
fresh to `Docs/health.md`. The code is in good structural shape — one vendor port, one sync
pipeline, one transfer state machine, repository discipline intact, resx parity and navigation
clean (Prose thread: no dead ends, every localizer key bound). What it carries is *documentation
that describes a section that no longer exists*: `Tickets.md` named controllers and a seeder as
Shell's, cited three test paths that do not exist, listed cross-section symbols that were never
injected, omitted both transfer route groups, and narrated the G5 move throughout
(findings 1–4). The same narration and wrong-location drift sat in the code comments (finding 3).
Tests are strong on the sync and transfer happy paths and thin on the negative-access rows, the
erasure/export paths and the ticket-holding predicates (findings 7, 15). Two behavior questions
(findings 8, 11) and one schema-shaped ledger item (finding 9) go to Peter unchanged.

## Findings (ranked, 3e)

1. **`Tickets.md` stale end to end** — `TicketsGateAdminController` and `GateTerminalAccountSeeder`
   described as Shell's (they are this section's); test paths cited under `Humans.Application.Tests`
   and a nonexistent `TicketSyncArchitectureTests`; routing table missing `/Tickets/Transfers` and
   `/Tickets/Admin/Transfers`; audit list missing `TicketTransferAutoFailed`; Cross-Section
   Dependencies naming `ITeamService.GetActiveMemberUserIdsAsync`,
   `IUserEmailService.GetAllUserEmailLookupEntriesAsync`, `IProfileService.*`,
   `IAccountMergeService.ReassignToUserAsync`, `BackfillParticipationsAsync` — none injected; sync
   "every 5 min" (default 15, `appsettings.json` 5); dead wheat marker to a spec that does not exist;
   triggers block incomplete and self-duplicating; `WelcomeController` placed in `Humans.Web`
   (it is Onboarding's); owning-services list missing `OnsiteRosterService`,
   `GateTerminalAccountSeeder`, `TicketTransferRepository`; a "Phase 2" roadmap sentence in an
   invariant doc; lane-number narration throughout. Found by Freshness (F1–F16, F42, F43) and
   History (H19–H27, H32–H35).
2. **`data-access.md` drift** — `IShiftManagementService` listed for Query/Sync/Import (code:
   `IBurnSettingsService`); `IUserService` for the query service (only `IUserServiceRead`);
   `InvalidateAfterTransfer` "called from ApproveAsync" (every transition); `ProcessTransferAsync`
   "flag-gated" twice (no flag); no freshness triggers block; "now …Stores/" narration.
3. **Stale code comments and remarks** — `Section.cs` placing `TicketSyncJob` in `Contracts/` and
   the health check in Shell; `ITicketSync`/`ITicketVendorMirror`/`ITicketVendorService` move
   narration; `TicketVendorSettings` citing a deleted extensions class; `TicketStubViewComponent` and
   `TicketStubInfo` placing types in Base/Shell; `TicketsDbContext` migrations path; `AssemblyInfo`
   naming a nonexistent `ITicketVendorGateway`; `ITicketRepository`/`TicketRepository` claiming a
   single writer and a "SQL-Server-friendly" trim on a Postgres app; `TicketTransferRequest` summary
   asserting the app never calls the vendor for a transfer and that the sender must be the order's
   buyer; `ITicketTransferService.GetMyAttendeesAsync` doc using order ownership (code:
   `IsCurrentOwner`); `TicketSyncState` pointing at Google/Discord settings; `ITicketServiceRead`
   "fourteen call sites"; bare `#NNN` tags. Comments checklist (D1–D13) plus History H1–H18.
4. **Feature docs, guides, csproj comments** — `event-participation.md` claiming a
   `Ticketed → NoShow` post-event derivation is written (the enum value exists; nothing writes it)
   and a trigger on a file that no longer carries the claim; `ticket-vendor-integration.md` route
   table missing Sync/FullResync/exports/backfill/transfers and "every 5 min";
   `ticket-transfer.md` narrating an earlier engine; `docs/guide/Tickets.md` calling the GateList
   placeholder a working door list and saying Board can view the transfer queue (it is
   `TicketAdminOrAdmin`); `authorization.md` with no triggers; `Humans.Tickets.csproj` stating the
   Budget flow backwards; `Humans.Tickets.Contracts.csproj` claiming two injection sites for the
   port (one, inside Tickets) and naming five Base consumers that no longer exist.
5. **Resx dead keys** — `TicketTransfer_CancelButton` and `TicketTransfer_SendButton` referenced
   nowhere in `src/` or `tests/`, all six cultures (Prose P2, Comments D14). Cut. The
   `Enum_TicketTransferStatus_*` keys are also unreferenced, but `TicketsResource.cs` records a
   prior decision to keep them — Peter's call, not a strike.
6. **DTO → ViewModel field-by-field copies** — `TicketController.Orders/Attendees/Codes/
   SalesAggregates` map service DTOs onto `Models/TicketViewModels.cs` records with identical
   shapes (~150 lines); `TicketTransferAdmin/Detail` and `Onsite` already bind the DTOs directly.
   Collapse candidate, internal types, reviewer-gated. Skipped — see below; recorded in
   `Docs/debt.yml`.
7. **Sync-cursor half of the error invariant untested** —
   `SyncOrdersAndAttendeesAsync_NonTransientError_SetsErrorState` asserted status and message but
   never that `LastSyncAt` survives (Tests T5). Fixed.
8. **`HasEventTicketAsync` counts a buyer-only paid order** (`Data/TicketRepository.cs:404`) — the
   docs (`Tickets.md`, guide, this run's `health.md` invariant) say buyer-only matches never count.
   Sole consumer: Users' `AccountDeletionService` post-event deletion hold, where a finance-relevant
   buyer may be the intent — then the name and the docs are wrong instead. Doc and code disagree
   and the code looks wrong → changed neither.
9. **Central ledger 2026-08-21 (re-sync resurrects erased PII)** — still real: `EraseUserPiiAsync`
   writes tombstones with no marker and the sync upserts `Buyer*`/`Attendee*` unconditionally.
   Contradicts the erasure invariant. Fix is a column or a vendor-side delete; schema → queued
   (Inbox I2).
10. **Pending-transfer projection built twice** — the attendee → pending-request dictionary and
    `IsCurrentOwner` projection appear in `TicketQueryService.GetUserTicketHoldingsAsync` and
    `TicketTransferService.GetMyAttendeesAsync` with the same comment. Collapsing onto
    `ITicketServiceRead.GetUserTicketHoldingsAsync` needs `CheckedInAt` on the public
    `UserTicketHoldingRow` — a surface change.
11. **Email fallback comparer mismatch** — `ComputeUserTicketCountAsync` compares verified emails
    with `OrdinalIgnoreCase`; the sync matches with `NormalizingEmailComparer`. A gmail-dotted
    verified email matches in sync and not in the homepage fallback between verification and the
    next sync. Behavior change.
12. **Two dated design docs** (`Docs/2026-05-13-…contact-import-design.md`,
    `Docs/2026-05-23-…transfer-wizard-design.md`) — false premises (the wizard doc deletes an
    automated writeback that has since been restored; the import doc is Draft/TBD for a shipped
    feature, with every Location a dead pre-G5 namespace) and zero inbound links. History exempts
    dated design records (peterdrier/Humans#1554), so deletion is Peter's; nothing edited.
13. **`/Tickets/GateList` placeholder** — the page says "coming in June"; the door lookup lives at
    `/Scanner/Tickets`. Whether the tab and page go is a nav decision.
14. **Conformance** — `Health/` is a top-level folder outside the section-file-layout allowed set
    (multi-section; Agent's was blessed 2026-08-28). Most resx keys are not `Tickets_`-prefixed —
    standing backlog, recorded only.
15. **Test gaps** (Tests thread matrix) — negative-access rows for Board vs Sync/Export/Decide and
    for the gate-terminal account vs `/Tickets/*` have no test; they belong in the repo-wide
    `Humans.Web.Tests` authorization table (sweep queue). Erasure, export, the ticket-holding
    predicates and the Ticketed-removal path are untested in-section (`Docs/debt.yml`).
16. **Inbox** — central ledger 2026-06-29 (TicketTailor check-in mapping) is fixed in code
    (`GetCheckInsAsync` pages `/v1/check_ins`); sweep queue to remove. The
    `Humans.Tickets.Contracts.csproj` Base-consumer list (Freshness F40) dissolved with finding 4's
    rewrite.

Raised during Phase 4:

17. **`ITicketTransferQueue` has no consumer outside the section** — the leaf interface's one
    method, `CountPendingAsync`, is called only from this section's own `SectionAdminNav.cs`.
    Public surface with no cross-section caller; pulling it off the leaf is a surface change.

## Worked

- Finding 1 — commit `f514dae7` (`Tickets.md` rewritten against the code: triggers, routing rows
  for both transfer route groups, audit list, GDPR erasure trigger, Cross-Section Dependencies
  rebuilt from the actual constructor injections, owning services, test paths).
- Finding 2 — commit `bea7a62b`.
- Finding 4 — commit `e772b4bf`.
- Finding 5 — commit `3a8ff11d` (two keys × six cultures, XML-tool edits, parity tests green).
- Finding 3 — commit `4f7fe395` (one build; 155 section tests green).
- Finding 7 — commit `d38171b9`.
- Sweep — separate commit: Agent 2026-08-28 queue (Agent `debt.yml` entry;
  `debt-ledger-additions` atom clarified for section test projects) and Settings 2026-08-28 queue
  (two central-ledger entries). Every older queue item verified already present in its target.
- Target shape — `Docs/health.md` (new). Two shape rows corrected mid-run: the first draft named
  an `ITicketAttendeeLookup` that does not exist and put `HasEventTicketAsync` on
  `ITicketServiceRead`.

Surfaces hit: **localization** — two dead keys removed from all six cultures, nothing added.
**Authorization** — no behavior change. **Audit** — unchanged. **GDPR** — unchanged; the erasure
path is now documented in `Tickets.md`. **Invariant doc** — rewritten (finding 1). **Migrations** —
none. **Navigation** — unchanged. **Tests** — one assertion added; `tests/Humans.Tickets.Tests`
green; resx parity green.

## Skipped + why

- Finding 6 — UI-affecting (the four views declare the row types in `Func<TRow, IHtmlContent>`
  helpers) and this container has no Postgres, so a render check was impossible. Recorded in
  `Docs/debt.yml` with the implementation note.
- Findings 8–14, 17 — behavior, schema, surface or nav decisions; queued to Needs Peter.
- Finding 15's negative-access rows — repo-wide test file, not this section's; sweep queue.
- Tests thread T13/T14 (duplicate mapping assertions, low) — not ranked; left.
- Sections passed over as blocked (open PRs at branch point): Auth (peterdrier/Humans#1575),
  Backdoor (peterdrier/Humans#1586), Budget (peterdrier/Humans#1565),
  Calendar (peterdrier/Humans#1578), Campaigns (peterdrier/Humans#1564),
  Camps (peterdrier/Humans#1561), Consent (peterdrier/Humans#1572),
  Email (peterdrier/Humans#1587), Feedback (peterdrier/Humans#1566),
  Gate (peterdrier/Humans#1574), Governance (peterdrier/Humans#1580),
  Holded (peterdrier/Humans#1583), Monitor (peterdrier/Humans#1582),
  Stripe (peterdrier/Humans#1588).

## Retro (Phase 6)

- **Selector/rubric:** nothing wrong — the never-doctored-median pick landed on a section whose
  dominant defect class (post-move narration and wrong-location drift) is what the run exists for.
- **Wasted motion:** the Comments thread hung for fifteen minutes on a tool call before being
  stopped; its checklist self-ran on the main thread in two. Finding 6 was assessed before the
  runtime check — the DB-less container was known at Phase 1 and should have gated the assessment.
- **Assessment missed, striking revealed:** Freshness F27 claimed `ParticipationStatus.NoShow`
  does not exist; it does (`= 3`, read by Users' ranking only) — only the derivation is unwritten,
  and the doc fix was narrowed accordingly. The main thread's S5 claimed `ITicketServiceRead` had
  grown past two methods; it has exactly two — only the "fourteen" count was stale. History H2's
  proposed rewrite placed the health check in Shell; it is this section's `Health/`. Three threads'
  proposed texts were wrong in a detail the strike had to re-verify — the run's freshest prose was
  again its wrongest. Striking also surfaced finding 17.
- **Target diff:** first run for this section — no prior target. `health.md` is new.
- Three context compactions occurred (during assessment, after thread dispatch, and during
  finding 7's strike — per the cost report); the Phase 5 re-read rule applied.

Finding raised by the retro:

18. **Phase 4 judgment lesson** — a cloud run has no Postgres, so every UI-affecting strike is
    skipped by the render-verification rule even when the section's own controller tests cover the
    binding and the PR's preview deploy would render it within minutes of the push. Proposed: in a
    cloud run, a reviewer-accepted collapse whose views bind the same fields may ship with the
    section's tests as the gate and the preview deploy named in the PR as the render check.

Finding raised by the review rounds:

19. **Phase 3c has no code-trace gate** — `health.md` was written from the assessment, and Codex
    caught wrong target-shape claims in three consecutive rounds. A trace of the whole doc against
    the code then found more (wrong queue method name, a `SurfaceBudget` that is only a
    `cref`, a Decide action set that does not exist, the comparer's dot-folding, a fallback
    attributed to the wrong method, `GateAdmit` missing from the terminal's policies, paging on
    who-hasn't-bought, the pipeline order, the stub/holdings localization). All corrected in the
    round-3 commit; the same false `SurfaceBudget` claim was carried into `Tickets.md` and
    `data-access.md` and is corrected there too. Skill issue: peterdrier/Humans#1590.

## Needs Peter

- [ ] 5 — keep the four `Enum_TicketTransferStatus_*` keys per the prior note, or delete?
- [ ] 8 — `HasEventTicketAsync` on a buyer-only paid order: fix the predicate, or the docs and the name?
- [ ] 9 — PII resurrection on re-sync: erased-flag column, or vendor-side delete — file the issue?
- [ ] 10 — add `CheckedInAt` to `UserTicketHoldingRow` so the pending-transfer projection collapses to one?
- [ ] 11 — switch the `ComputeUserTicketCountAsync` fallback to `NormalizingEmailComparer`?
- [ ] 12 — delete both dated design docs, or stamp Superseded?
- [ ] 13 — `/Tickets/GateList`: remove tab and page, or keep with corrected copy?
- [ ] 14 — bless `Health/` for Tickets as for Agent?
- [ ] 17 — pull `ITicketTransferQueue` off the Contracts leaf (internal to Tickets)?
- [ ] 18 — Phase 4: allow the cloud-run render-check substitute as proposed?
- [ ] 19 — Phase 3c: add the code-trace gate and drop the boilerplate subtitle (peterdrier/Humans#1590)?

## Sweep queue

- `debt: docs/architecture/debt-ledger.yml — remove the 2026-06-29 entry (TicketTailor inbound check-in detection broken): fixed — TicketTailorService.GetCheckInsAsync pages /v1/check_ins, NetCheckIns joins them, and TicketSyncService.SyncOrdersAndAttendeesAsync consumes the result (finding 16, /section-doctor on Tickets 2026-09-05).`
- `debt: Humans.Web — tests/Humans.Web.Tests/Authorization/EndpointAuthorizationTests.cs pins only Tickets' Onsite and Gate rows; no test denies Board on POST /Tickets/Sync, the two exports, /Tickets/Admin/Transfers/Decide, or the gate-terminal account on any /Tickets/* admin page (finding 15, /section-doctor on Tickets 2026-09-05).`
- `debt: Humans.Budget — src/Sections/Humans.Budget/Docs/Budget.md "Architecture test" bullet cites tests/Humans.Budget.Tests/Architecture/TicketingBudgetArchitectureTests.cs; no Architecture/ folder exists — the bridge is pinned by tests/Humans.Budget.Tests/Services/TicketingBudgetServiceTests.cs. Budget was blocked (peterdrier/Humans#1565) so not edited here (/section-doctor on Tickets 2026-09-05).`
- `debt: Humans.TicketTailor — src/Sections/Humans.TicketTailor/Contracts/README.md:6 and Humans.TicketTailor.csproj:22-26 carry G5 lane narration ("lane 4b-2g", "used to live in Base", "previously avoided"); keep the live acyclicity and deletion-path facts, cut the history (/section-doctor on Tickets 2026-09-05).`

## File coverage

| Path | Disposition |
|---|---|
| `src/Sections/Humans.Tickets.Contracts/Humans.Tickets.Contracts.csproj` | changed |
| `src/Sections/Humans.Tickets.Contracts/ITicketDiscountCodes.cs` | reviewed |
| `src/Sections/Humans.Tickets.Contracts/ITicketServiceRead.cs` | changed |
| `src/Sections/Humans.Tickets.Contracts/ITicketSync.cs` | changed |
| `src/Sections/Humans.Tickets.Contracts/ITicketTransferQueue.cs` | reviewed |
| `src/Sections/Humans.Tickets.Contracts/ITicketVendorMirror.cs` | changed |
| `src/Sections/Humans.Tickets.Contracts/TicketAttendeeStatus.cs` | reviewed |
| `src/Sections/Humans.Tickets.Contracts/TicketConstants.cs` | reviewed |
| `src/Sections/Humans.Tickets.Contracts/TicketDiscountCodeRequest.cs` | reviewed |
| `src/Sections/Humans.Tickets.Contracts/TicketOrderInfo.cs` | reviewed |
| `src/Sections/Humans.Tickets.Contracts/TicketPaymentStatus.cs` | reviewed |
| `src/Sections/Humans.Tickets.Contracts/TicketStubInfo.cs` | changed |
| `src/Sections/Humans.Tickets.Contracts/UserTicketHoldings.cs` | reviewed |
| `src/Sections/Humans.Tickets/Contracts/ITicketVendorService.cs` | changed |
| `src/Sections/Humans.Tickets/Contracts/TicketStubViewComponent.cs` | changed |
| `src/Sections/Humans.Tickets/Contracts/TicketVendorDtos.cs` | reviewed |
| `src/Sections/Humans.Tickets/Contracts/TicketVendorSettings.cs` | changed |
| `src/Sections/Humans.Tickets/Controllers/TicketController.cs` | reviewed |
| `src/Sections/Humans.Tickets/Controllers/TicketTransferAdminController.cs` | reviewed |
| `src/Sections/Humans.Tickets/Controllers/TicketTransferController.cs` | reviewed |
| `src/Sections/Humans.Tickets/Controllers/TicketsContactsAdminController.cs` | reviewed |
| `src/Sections/Humans.Tickets/Controllers/TicketsGateAdminController.cs` | reviewed |
| `src/Sections/Humans.Tickets/Controllers/TicketsOnsiteAdminController.cs` | reviewed |
| `src/Sections/Humans.Tickets/Data/Configurations/TicketAttendeeConfiguration.cs` | reviewed |
| `src/Sections/Humans.Tickets/Data/Configurations/TicketOrderConfiguration.cs` | reviewed |
| `src/Sections/Humans.Tickets/Data/Configurations/TicketSyncStateConfiguration.cs` | reviewed |
| `src/Sections/Humans.Tickets/Data/Configurations/TicketTransferRequestConfiguration.cs` | reviewed |
| `src/Sections/Humans.Tickets/Data/ITicketRepository.cs` | changed |
| `src/Sections/Humans.Tickets/Data/ITicketTransferRepository.cs` | reviewed |
| `src/Sections/Humans.Tickets/Data/Migrations/20260809125159_BaselineTickets.Designer.cs` | generated |
| `src/Sections/Humans.Tickets/Data/Migrations/20260809125159_BaselineTickets.cs` | reviewed |
| `src/Sections/Humans.Tickets/Data/Migrations/TicketsDbContextModelSnapshot.cs` | generated |
| `src/Sections/Humans.Tickets/Data/TicketPiiTombstone.cs` | reviewed |
| `src/Sections/Humans.Tickets/Data/TicketRepository.cs` | changed |
| `src/Sections/Humans.Tickets/Data/TicketTransferRepository.cs` | reviewed |
| `src/Sections/Humans.Tickets/Data/TicketsDbContext.cs` | changed |
| `src/Sections/Humans.Tickets/Data/TicketsDbContextFactory.cs` | reviewed |
| `src/Sections/Humans.Tickets/Docs/2026-05-13-ticket-attendee-contact-import-design.md` | reviewed |
| `src/Sections/Humans.Tickets/Docs/2026-05-23-ticket-transfer-wizard-design.md` | reviewed |
| `src/Sections/Humans.Tickets/Docs/Tickets.md` | changed |
| `src/Sections/Humans.Tickets/Docs/authorization.md` | changed |
| `src/Sections/Humans.Tickets/Docs/data-access.md` | changed |
| `src/Sections/Humans.Tickets/Docs/features/event-participation.md` | changed |
| `src/Sections/Humans.Tickets/Docs/features/ticket-transfer.md` | changed |
| `src/Sections/Humans.Tickets/Docs/features/ticket-vendor-integration.md` | changed |
| `src/Sections/Humans.Tickets/Domain/TicketAttendee.cs` | reviewed |
| `src/Sections/Humans.Tickets/Domain/TicketOrder.cs` | reviewed |
| `src/Sections/Humans.Tickets/Domain/TicketSyncState.cs` | changed |
| `src/Sections/Humans.Tickets/Domain/TicketSyncStatus.cs` | reviewed |
| `src/Sections/Humans.Tickets/Domain/TicketTransferRequest.cs` | changed |
| `src/Sections/Humans.Tickets/Domain/TicketTransferStatus.cs` | reviewed |
| `src/Sections/Humans.Tickets/Domain/TicketTransferVendorResult.cs` | reviewed |
| `src/Sections/Humans.Tickets/Health/TicketVendorHealthCheck.cs` | changed |
| `src/Sections/Humans.Tickets/Humans.Tickets.csproj` | changed |
| `src/Sections/Humans.Tickets/Jobs/TicketSyncJob.cs` | changed |
| `src/Sections/Humans.Tickets/Models/ContactImportPreviewViewModel.cs` | reviewed |
| `src/Sections/Humans.Tickets/Models/OnsiteRosterViewModel.cs` | reviewed |
| `src/Sections/Humans.Tickets/Models/TicketDashboardPageBuilder.cs` | reviewed |
| `src/Sections/Humans.Tickets/Models/TicketStubMapping.cs` | reviewed |
| `src/Sections/Humans.Tickets/Models/TicketTransferViewModels.cs` | reviewed |
| `src/Sections/Humans.Tickets/Models/TicketViewModels.cs` | reviewed |
| `src/Sections/Humans.Tickets/Properties/AssemblyInfo.cs` | changed |
| `src/Sections/Humans.Tickets/Section.cs` | changed |
| `src/Sections/Humans.Tickets/SectionAdminNav.cs` | changed |
| `src/Sections/Humans.Tickets/SectionHealthChecks.cs` | changed |
| `src/Sections/Humans.Tickets/SectionJobs.cs` | reviewed |
| `src/Sections/Humans.Tickets/SectionMemberDashboard.cs` | reviewed |
| `src/Sections/Humans.Tickets/SectionPolicies.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/AttendeeContactImportService.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/Dtos/AttendeeImportDecision.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/Dtos/AttendeeImportOutcome.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/Dtos/AttendeeImportPlan.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/Dtos/AttendeeImportResult.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/Dtos/TicketDashboardDtos.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/Dtos/TicketSyncDtos.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/Dtos/TicketTransferDtos.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/GateTerminalAccountSeeder.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/IAttendeeContactImportService.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/IOnsiteRosterService.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/ITicketCacheInvalidator.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/ITicketService.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/ITicketSyncService.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/ITicketTransferService.cs` | changed |
| `src/Sections/Humans.Tickets/Services/OnsiteRosterService.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/Stores/CachingTicketQueryService.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/TicketAttendeeOwnership.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/TicketQueryService.cs` | changed |
| `src/Sections/Humans.Tickets/Services/TicketSyncService.cs` | reviewed |
| `src/Sections/Humans.Tickets/Services/TicketTransferService.cs` | changed |
| `src/Sections/Humans.Tickets/Services/TicketVendorGateway.cs` | changed |
| `src/Sections/Humans.Tickets/TicketsResource.ca.resx` | changed |
| `src/Sections/Humans.Tickets/TicketsResource.cs` | changed |
| `src/Sections/Humans.Tickets/TicketsResource.de.resx` | changed |
| `src/Sections/Humans.Tickets/TicketsResource.es.resx` | changed |
| `src/Sections/Humans.Tickets/TicketsResource.fr.resx` | changed |
| `src/Sections/Humans.Tickets/TicketsResource.it.resx` | changed |
| `src/Sections/Humans.Tickets/TicketsResource.resx` | changed |
| `src/Sections/Humans.Tickets/ViewComponents/GuestTicketOrdersViewComponent.cs` | reviewed |
| `src/Sections/Humans.Tickets/ViewComponents/MemberTicketStatusViewComponent.cs` | reviewed |
| `src/Sections/Humans.Tickets/ViewComponents/MyTicketStubsViewComponent.cs` | reviewed |
| `src/Sections/Humans.Tickets/ViewComponents/TicketHoldingsViewComponent.cs` | reviewed |
| `src/Sections/Humans.Tickets/Views/Shared/Components/GuestTicketOrders/Default.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Shared/Components/MemberTicketStatus/Default.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Shared/Components/MyTicketStubs/Default.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Shared/Components/TicketHoldings/Default.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Shared/Components/TicketStub/Default.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Ticket/Attendees.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Ticket/Codes.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Ticket/GateList.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Ticket/Index.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Ticket/Orders.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Ticket/ParticipationBackfill.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Ticket/SalesAggregates.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Ticket/WhoHasntBought.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Ticket/_TicketNav.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Ticket/_ViewStart.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/TicketTransfer/Index.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/TicketTransferAdmin/Detail.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/TicketTransferAdmin/Index.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/TicketTransferAdmin/_ViewStart.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Tickets/Admin/Contacts.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Tickets/Admin/Gate.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/Tickets/Admin/Onsite.cshtml` | reviewed |
| `src/Sections/Humans.Tickets/Views/_ViewImports.cshtml` | changed |
| `tests/Humans.Tickets.Tests/Architecture/TicketQueryArchitectureTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Contracts/TicketStubInfoTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Controllers/TicketsContactsAdminControllerTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Controllers/TicketsOnsiteAdminControllerTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Data/TicketRepositoryTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Data/TicketRepository_OrderDriftTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Enums/TicketEnumStringStabilityTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Humans.Tickets.Tests.csproj` | reviewed |
| `tests/Humans.Tickets.Tests/Services/AttendeeContactImportServiceApplyTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/AttendeeContactImportServicePlanTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/AttendeeContactImportServiceSquatterTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/AttendeeImportResultTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/CachingTicketQueryServiceTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/OnsiteRosterServiceTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/TicketAttendeeOwnershipTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/TicketQueryServiceTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/TicketQueryService_HoldingsTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/TicketSyncServiceNullOrderTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/TicketSyncServiceTests.cs` | changed |
| `tests/Humans.Tickets.Tests/Services/TicketSyncService_ReassignCacheTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/TicketTransferServiceTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/Services/TicketTransferService_OnwardTransferTests.cs` | reviewed |
| `tests/Humans.Tickets.Tests/TicketsTestHarness.cs` | reviewed |
| `tests/Humans.Tickets.Tests/UserInfoProjection.cs` | reviewed |
| `tests/Humans.Tickets.Tests/ViewComponents/MyTicketStubsViewComponentTests.cs` | reviewed |
| `docs/guide/Tickets.md` | changed |
| `docs/guide/TicketTransfers.md` | reviewed |
| `src/Sections/Humans.Tickets/Docs/health.md` | changed (new) |
| `src/Sections/Humans.Tickets/Docs/debt.yml` | changed (new) |
| `docs/health/runs/2026-09-05-Tickets.md` | changed (new, this file) |
| `src/Sections/Humans.Agent/Docs/debt.yml` | changed (sweep) |
| `docs/architecture/debt-ledger.yml` | changed (sweep) |
| `memory/process/debt-ledger-additions.md` | changed (sweep) |
| `memory/INDEX.md` | changed (sweep) |

## Threads

| Thread | How | Model | Findings |
|---|---|---|---|
| Inbox | subagent | doctor-reader (opus, low) | 7 |
| Conformance | subagent | haiku | 2 |
| Prose & surface | subagent | haiku | 9 |
| Freshness | subagent | doctor-reader (opus, low) | 43 |
| Tests | subagent | doctor-reader (opus, low) | 18 |
| History | subagent | doctor-reader (opus, low) | 39 |
| Comments | self-run after a hang | (main session) | 14 |

Comments was dispatched as doctor-reader (opus, low) at 01:48, produced no output for fifteen
minutes (stuck on a tool call), and was stopped at 02:03; its checklist was self-run on the main
thread over the same slice (findings D1–D14). Inbox scope was peterdrier/Humans only (the default
run never queries upstream). Prose skipped InspectCode (not installed in the container).
