# section-doctor — Gdpr — 2026-09-23

- Invocation: scheduled unattended daily routine, no arguments (Phase 8 skipped per routine prompt)
- Anchor commit: `481ae1562` (origin/main at branch point); branch `section-doctor/2026-09-23T011627Z`.
- Budget: 2.5h.
- PR: peterdrier/Humans#1811

## Assessment summary

- 2026-09-23 session: cloud container; SDK, dotnet-ef and reforge present. No live render (the section has no views; its one route returns a file or a redirect).
- Scope: re-doctor against `BASE` `6baab17f8`. Since the last run the envelope gained `UserId` and `MergedFromUserIds` (the orchestrator now reads `IUserServiceRead` once per export), `GdprExportSections` was deleted in favour of per-contributor constants, and the failure tests gained a capturing logger.

Gdpr is still at its target structurally: a contracts leaf, one internal orchestrator, one
internal controller and a `Section.cs`. Conformance detectors are clean and reforge's score is
the fan-out seam the target already blesses. What moved is the prose again, this time behind
the envelope change: the section doc said Gdpr names no other section's type, described an
envelope without the account fields, and — with the contract comments and the export spec —
said a missing erasure declaration is *enforced* when the orchestrator logs it and exports the
slice anyway (a test pins exactly that). The vacuous-coverage gap lost its tracker when
nobodies-collective/Humans#1116 closed on a different fix; it is now central debt row CENTRAL-67.

Target diff: the previous target's invariants cited lines the merge-lineage change had moved,
claimed the orchestrator's whole dependency set was contributors, clock and logger, and cited the
erasure-coverage test as enforcing that every *exported* category is declared — the test cannot
see exports; the runtime log line is the only check. The earlier target was wrong on those
points, not the section moving.

Independence check: pass. Findings 1, 2 and 5 came from the target, not a tool: 1 is a
spec-vs-reality delta (the contract says enforced, the code logs), 2 is the target's one
outbound dependency against a doc denying it, 5 is the seam whose tracker closed.

## Findings

1. The contract comments (`IUserDataContributor.cs`, `UserDataSlice.cs`), the section doc's
   freshness flag and the export spec said the orchestrator *enforces* that every exported
   portion carries an erasure declaration. `GdprService` logs an error and still exports the
   slice, and `ExportForUserAsync_LogsAndContinuesWhenExportedSectionHasNoErasureDeclaration`
   pins that. Code, test and the doc's own Triggers section agree, so the prose moved.
2. `Docs/Gdpr.md` Cross-Section Dependencies said outbound is "none at compile time"; Gdpr
   references `Humans.Users.Contracts` and injects `IUserServiceRead`. Its inbound line said
   `Humans.Web` references the leaf "for the two download controllers"; neither controller is
   in Web.
3. `Docs/Gdpr.md` described `GdprExport` as a timestamp and a section bag, omitting
   `UserId` and `MergedFromUserIds`.
4. `IGdprService.cs` said the export "does not resolve a merge chain" and that taking only an
   id keeps the orchestrator free of Users; it now resolves the account for its envelope.
5. The vacuous-coverage gap (a section that never implements the contract is invisible to the
   coverage tests) was cited to nobodies-collective/Humans#1116, which is closed, and to an
   audit label. The gap is still real — both coverage tests enumerate implementers by
   reflection — and was tracked nowhere open. Filed as CENTRAL-67 (central ledger: the fix is a shared cross-section test).
6. `Humans.Gdpr.Contracts.csproj` still listed the deleted `GdprExportSections`.
7. `docs/features/global/gdpr-export.md` said sections with no data are omitted; only `null`
   slices are, and empty collections appear as `[]`.
8. `ExportForUserAsync_EmptyCollectionSerializesToEmptyArray` serialized a hand-copied flatten
   with default options, never the controllers' `ExportJsonOptions` or `BuildExportPayload`,
   and its copy had already drifted (no `UserId`, no `MergedFromUserIds`). It reverses finding
   18 of `docs/health/runs/2026-08-27-Gdpr.md`, which kept the test as "the only test that pins
   the claim at the JSON level": that rationale was false, since it pinned System.Text.Json
   defaults, not product JSON. The JSON-level `[]` claim is now pinned nowhere, as it in effect
   never was; the controller-test gap is GDPR-1.
9. `tests/Humans.Gdpr.Tests/Humans.Gdpr.Tests.csproj` said the test builds `GdprService` over
   fake contributors "and a FakeClock and nothing else"; it also passes a user-read substitute
   and a logger.
10. Comments carrying history or duplication: the `GdprService` summary's retired-`DbContext`
    story, the erasure-ordering comment restating the interface doc, `GuestDataController`'s
    placement argument, and `GdprExport.cs` naming its formatting helper.
11. `Docs/health.md` load-bearing weirdness narrated the prior state of the sequential
    fan-out.
12. nobodies-collective/Humans#1116 is closed but its gap stands (finding 5). Inbox
    recommendation: reopen it, or leave CENTRAL-67 as the tracker.
13. nobodies-collective/Humans#1199 (Calendar `CreatedByUserId` missing from the export) is
    valid; Inbox recommendation: keep, add `section:gdpr`.
14. Off-section, not struck: `src/Sections/Humans.Surveys/Docs/2026-06-03-survey-section-design.md`
    names `GdprExportSections.Survey`; it is a dated design record, so left as history.
15. Behaviour noted, not changed: `GuestDataController`'s `catch (Exception)` also catches a
    client-abort cancellation and logs it as an export failure.

## Debt verified

- GDPR-1 — still-true — `GuestDataController` has no test anywhere; the only hit under `tests/` is the test csproj comment — `tests/Humans.Gdpr.Tests/Humans.Gdpr.Tests.csproj:18`
- GDPR-2 — still-true — no test compares `docs/features/global/gdpr-export.md` with the contributor roster; the only test naming the file is an agent-dispatcher fixture — `tests/Humans.Agent.Tests/AgentToolDispatcherTests.cs:532`
- GDPR-3 — closed — the logged half is now pinned in both fan-outs through `CapturingLogger` — `ExportForUserAsync_PropagatesContributorFailure`, `EraseForUserAsync_PropagatesContributorFailureAndStopsBeforeAccount`
- The cap was not reached; every open row was verified.

## Worked

- 1 — contract comments, freshness flag and export spec say "logged, still exported".
- 2, 3 — `Docs/Gdpr.md` dependencies and envelope rewritten against the csproj and `GdprExport.cs`.
- 4 — `IGdprService` export and erasure summaries.
- 5 — CENTRAL-67 filed (moved from GDPR-4 on Codex review); `Docs/Gdpr.md` known gap and `Docs/health.md` seam point at it.
- 6, 9 — both csproj comments.
- 7 — export spec's omission rule.
- 8 — test deleted; critical-tier reviewer approved with a correction to this record (the reversal of the earlier run's finding, now named in finding 8).
- 10 — comment trims in `GdprService`, `GuestDataController`, `GdprExport`.
- 11 — `Docs/health.md` weirdness line.
- Also: `Docs/Gdpr.md`'s sequential-fan-out invariant cut to its current reason, its design-rules pointer corrected to §8b where the paragraph lives.
- Executor additions kept: none beyond the listed findings.

## Skipped

- 12, 13 — existing issues are read-only to a run; recommendations for Peter.
- 14 — dated design record in another section.
- 15 — behaviour change; the two download routes' failure handling is already a Users ledger row.
- Duplicated export serialization across `GuestDataController` and `ProfileController` — central ledger row stands; adds public surface either way.
- `Section.cs` summary boilerplate — every section carries it; not this section's call.
- Blocked: Settings (open `section-doctor/2026-09-21T011611Z` branch).

## Retro

Selector: right pick. Gdpr's churn since the last run was the merge-lineage envelope and the
section-name registry deletion, both of which left the section's prose behind — exactly what a
re-doctor is for.

Wasted motion: the first reforge run raced the baseline build's package restore and refused to
score an uncompiled solution; it had to be re-run after the build.

Striking revealed: the previous run had kept the serialize test on a rationale nobody checked —
it never touched product serialization. The reviewer caught that the reversal needed saying.

Target diff: the earlier target overstated what the erasure-coverage test enforces and
understated the orchestrator's dependencies; both were the earlier target being wrong, not the
section moving.

## Needs Peter

- [ ] 12 — reopen nobodies-collective/Humans#1116, or leave CENTRAL-67 as the tracker?

## File coverage

| Path | Disposition |
|---|---|
| `src/Sections/Humans.Gdpr.Contracts/GdprExport.cs` | changed |
| `src/Sections/Humans.Gdpr.Contracts/Humans.Gdpr.Contracts.csproj` | changed |
| `src/Sections/Humans.Gdpr.Contracts/IGdprService.cs` | changed |
| `src/Sections/Humans.Gdpr.Contracts/IUserDataContributor.cs` | changed |
| `src/Sections/Humans.Gdpr.Contracts/UserDataSlice.cs` | changed |
| `src/Sections/Humans.Gdpr/Controllers/GuestDataController.cs` | changed |
| `src/Sections/Humans.Gdpr/Docs/Gdpr.md` | changed |
| `src/Sections/Humans.Gdpr/Docs/authorization.md` | reviewed |
| `src/Sections/Humans.Gdpr/Docs/data-access.md` | reviewed |
| `src/Sections/Humans.Gdpr/Docs/debt.yml` | changed |
| `src/Sections/Humans.Gdpr/Docs/health.md` | changed |
| `src/Sections/Humans.Gdpr/Humans.Gdpr.csproj` | reviewed |
| `src/Sections/Humans.Gdpr/Section.cs` | reviewed |
| `src/Sections/Humans.Gdpr/Services/GdprService.cs` | changed |
| `tests/Humans.Gdpr.Tests/Humans.Gdpr.Tests.csproj` | changed |
| `tests/Humans.Gdpr.Tests/Services/GdprServiceTests.cs` | changed |

## Threads

| Thread | How it ran | Model | Findings |
|---|---|---|---|
| Shape | main | main session | 2, 4 |
| Behavior & bugs | main | main session | 1, 15 |
| Freshness | subagent (`doctor-reader`) | opus-low | 1, 2, 3, 5, 6, 7, 14 |
| Conformance | subagent (`general-purpose`) | haiku | none (every detector clean) |
| Tests | subagent (`doctor-reader`) | opus-low | 8, 9 |
| Prose & surface | subagent (`general-purpose`), logged as `Prose-surface`; InspectCode not run (not in this environment) | haiku | Gdpr.md sequential-invariant trim (Worked) |
| History | subagent (`doctor-reader`) | opus-low | 11 |
| Comments | subagent (`doctor-reader`) | opus-low | 1, 4, 10 |
| Inbox | subagent (`doctor-reader`) | opus-low | 5, 12, 13 |

