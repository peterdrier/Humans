# Stripe — section doctor, 2026-09-04

- **Invocation:** unattended daily run, no arguments. Phase 8 (inline round) skipped.
- **Anchor commit:** `10199a23` (`origin/main`)
- **Branch:** `section-doctor/2026-09-04T191601Z` (cloud run, repo root — no worktree)
- **Budget:** 2.5h, single PR.
- **PR:** peterdrier/Humans#pending

## Assessment summary

First doctor pass over Stripe, the payments connector (reforge 65 @ 0.29.0, loc=753,
cogP95=11, cogMax=20): one interface, one implementation, two boot jobs, no tables, no
`DbContext`, no controller, no view, no resx, no route. The target shape
([`health.md`](../../../src/Sections/Humans.Stripe/Docs/health.md), written this run before
any scan) found the structure right as built and named two things it should not have: a
behavioural rule the code broke in one place, and one duplicated listing.

The rule is the section's whole contract — **every read returns `null` for "could not ask
Stripe"; only the write throws.** Finding 1 is the one method that broke it: with
`STRIPE_TICKETS_KEY` unset, `GetPaymentDetailsAsync` threw out of Stripe.NET's `StripeClient`
constructor where its three siblings log and return `null`. Reproduced by execution before
being fixed. It is unreachable in production — the single caller gates on `IsConfigured` and
catches around the call — so no reached path changed behaviour; what changed is that the
contract is now uniform and pinned.

Everything else was prose and coverage. The section carried a TODO for work that shipped
(finding 7), a DI extension type that exists nowhere in the tree (finding 11), a class doc
counting two tests where one exists (finding 3), and a layer of G5-migration provenance —
lane numbers, design-session dates, other sections' names as precedent (finding 10). Nine
test gaps (15–18, 20, 21) sat over the checkout write guards, the signature boundary, the
event switch and both boot jobs' non-blocking contract; `tests/Humans.Stripe.Tests` went
from 21 to 37 tests.

## Ranked findings

Value = bug surface removed, then concepts removed, then words removed.

| # | Finding | Value | Disposition |
|---|---|---|---|
| 1 | **`GetPaymentDetailsAsync` threw where its siblings return `null`.** With `STRIPE_TICKETS_KEY` unset it raised `ArgumentException: API key cannot be the empty string` from `new StripeClient("")` — the only one of the four methods with no configuration guard, against an `IStripeService` doc-comment promising `null`. Confirmed by execution (a throwaway probe test failed with that exact exception, then was deleted). Unreachable today: `TicketSyncService.cs:319` gates on `IsConfigured` and catches around the call. Guard added, doc-comment corrected to cover the unset key it never promised, invariant written into `Stripe.md`. | high | **worked** |
| 2 | **The webhook registrar listed the account's endpoints twice.** `SweepStaleEndpointsAsync` and `DeleteExistingForUrlAsync` each made their own `WebhookEndpointService.ListAsync(Limit = 100)` and deleted from that list on a different criterion. Collapsed to one listing, one pass, two predicates, with the open-PR lookup returning `null` for "unknown" rather than an early return that silently also skipped the own-URL delete. Behaviour on every reached path unchanged; the own-URL delete stays outside try/catch, the cross-PR delete still swallows `StripeException`. Reviewer-gated. | high | **worked** |
| 3 | **`StripeConnectorArchitectureTests.cs:20-22` said "Two tests remain"; one exists** — and the file's own COVERAGE REDUCED note says the other was removed. | med | **worked** |
| 4 | **Same file :31 named the survivor `IStripeService_ExposesNoStripeSdkTypes`;** the method is `IStripeService_ExposesNoStripeSdkTypesOnItsPublicSurface`. | low | **worked** |
| 5 | **`Docs/data-access.md:5` promised "services under `Services/`" and documented one of three.** Both hosted services added. | med | **worked** |
| 6 | **`Docs/Stripe.md`'s freshness trigger covered only `src/Sections/Humans.Stripe/**`,** while the doc asserts a test method name and a test file path — the exact claim that went stale in finding 3. `tests/Humans.Stripe.Tests/**` added to the trigger. | med | **worked** |
| 7 | **`StoreWebhookRegistrationService.cs:31` carried a TODO for shipped work.** "Async-payment events log at Warning until #638 ships" was false on three counts: the work shipped (Store's `TransitionAsyncPaymentAsync`, `Store.md:268`, `ServiceTests.cs:1122`); nothing in that file logs async-payment events; and a bare `#638` violates [`issue-refs-qualified`](../../../memory/process/issue-refs-qualified.md). | med | **worked** |
| 8 | **`Humans.Stripe.Tests.csproj:5` carried an unused `Humans.Shifts.Contracts` ProjectReference** — no `.cs` under the project names a Shifts type. | med | **worked** |
| 9 | **`Properties/AssemblyInfo.cs:3` claimed the InternalsVisibleTo attribute serves "NSubstitute in Humans.Stripe.Tests";** nothing there uses NSubstitute. The attribute is a 40-section convention — kept, false rationale cut. | low | **worked** |
| 10 | **G5-migration provenance across four files.** `Humans.Stripe.csproj`, `Section.cs`, `Docs/Stripe.md` and the architecture test carried lane numbers, design-session dates, "TicketTailor's shape", "Scanner's shape", "MailerLite's case", "ruling 1". Cut; the live constraints kept (the NU1010/CPM landmine, why-not-Razor, both COVERAGE-REDUCED gaps with their restore condition, the namespace hazard). | med | **worked** |
| 11 | **`Section.cs:10-11` named `StripeInfrastructureExtensions`,** a type that exists nowhere in the tree. | med | **worked** |
| 12 | **The refunds-are-dashboard-manual rule was restated three times in-section** (`StripeService.cs:17-18`, `Section.cs:27`, `StripeStartupSmokeService.cs:21`), each a copy of [`refunds-manual-via-dashboard`](../../../memory/architecture/refunds-manual-via-dashboard.md). One kept. | low | **worked** |
| 13 | **`StripeService.cs:176` and `:183` were section-header comments restating the next lines.** | low | **worked** |
| 14 | **`StripeService.cs:121`'s "see the 2026-06-04 reconciliation design" resolves.** The History thread reported it unresolvable; `src/Sections/Humans.Store/Docs/2026-06-04-store-stripe-payment-reconciliation-design.md` exists. Kept, and the pointer now names the path. | low | **no change → corrected pointer** |
| 15 | **The checkout write guards were unpinned** — `CreateCheckoutSessionAsync` rejecting amount ≤ 0 and an unset Store key are the two rejections standing between a bad caller and a live charge. Theory + fact added. | high | **worked** |
| 16 | **`ParseStoreCheckoutEvent`'s security boundary was unpinned** — returning `null` for an unset signing secret and for an invalid signature. `StripeSignatureSanityTest` exercises the SDK's `EventUtility`, never `StripeService`. Two tests added. | high | **worked** |
| 17 | **Event categorisation and `MapSession`'s projection were unpinned.** A mis-mapped `humans_store_order_id` silently orphans a real payment. Five-row theory over the switch, plus projection and unusable-metadata tests. | med | **worked** |
| 18 | **`ListStoreCheckoutSessionsAsync`'s null-vs-empty distinction was unpinned,** though `Stripe.md` spells out the exact bug it prevents (false-flagging recorded payments as orphans). Test added. | med | **worked** |
| 19 | **This run's own target over-claimed.** health.md §4 said "EUR ↔ minor units rounds half away from zero"; that is true outbound only — `FromStripeMinorUnits` divides a `long` by `100m`, which cannot round. Target wording corrected, inverse and round-trip pinned. | med | **worked** |
| 20 | **Neither boot job's non-blocking contract was pinned.** A future `await` in `StartAsync` would delay every boot with nothing noticing. Both pinned. | med | **worked** |
| 21 | **The registrar's two refusal gates were unpinned** — no registrar key, and a host that is not `*.n.burn.camp`. Both pinned, plus the missing-base-URL gate. | med | **worked** |
| 22 | **Inbox: nothing to review.** No open `section:stripe` issue on `peterdrier/Humans`, and no open issue whose text matches "stripe". Reach proved by reading `peterdrier/Humans#1494` before trusting the zero result. Scope: fork only, which is the complete review. | — | **no action** |
| 23 | **Conformance: clean.** Both applicable rules (`section-file-layout`, `resource-key-prefix`) pass. | — | **no action** |
| 24 | **The History thread reported `docs/sections/G5-SECTION-TEMPLATE.md` as absent from the tree. It is present.** Verified before acting; no edit made on that basis, and the test csproj's Stripe.net comment now points at it. | — | **no change** |
| 25 | **`memory/code/stripe-restricted-keys.md:10` points scope documentation at `docs/sections/<Section>.md`,** which holds only templates post-G5 — real section docs live at `src/Sections/Humans.<Section>/Docs/<Section>.md`. The same line uses `STRIPE_BUSSES_KEY`, an env var that exists nowhere, as its canonical naming example. Shared file → sweep queue. | med | **queued** |
| 26 | **`memory/architecture/refunds-manual-via-dashboard.md:12` says the app "only handles `checkout.session.completed`".** Four events are handled. The rule's conclusion still holds; its premise is stale. Shared file → sweep queue. | low | **queued** |
| 27 | **Both atoms cite a `Store.md` heading "Stripe Configuration"; the heading is `## Stripe Connector`.** Shared files → sweep queue. | low | **queued** |
| 28 | **`src/Sections/Humans.Tickets/Docs/Tickets.md:160,213` still calls Stripe "Infrastructure" / "Infrastructure-only".** It has been its own section project since the G5 carve-out. Another section's doc → sweep queue. | low | **queued** |
| 29 | **`src/Sections/Humans.Store/Docs/Store.md:259` says the webhook is for "`checkout.session.completed` ingestion";** `:243` in the same file says all four events. Another section's doc → sweep queue. | low | **queued** |

## Worked

Findings 1–13 and 15–21, one commit per strike:

- `doctor(Stripe): derive the target shape` — `health.md`, written before any scan.
- `doctor(Stripe): cut prose that is false, dead, or pure provenance` — findings 3, 4, 7, 9, 10, 11, 12, 13, 14.
- `doctor(Stripe): drop the unused Shifts.Contracts reference from the test project` — finding 8.
- `doctor(Stripe): document the two hosted services; widen the freshness trigger` — findings 5, 6.
- `doctor(Stripe): guard GetPaymentDetailsAsync on an unset Tickets key, and pin the read/write contract` — findings 1, 15, 16, 17, 18, 19.
- `doctor(Stripe): collapse the registrar's two endpoint listings into one pass` — findings 2, 20, 21.

Surfaces hit: **localization** — none applies; the section has no view, no route and no resx,
and every string it emits is a log line. **Authorization** — no behavior change; the section
has no controller and no endpoint, and its only gates are configuration gates (findings 20,
21 pin two of them). **Audit** — unchanged; the section takes no action on a member's behalf,
it translates one API. **GDPR** — untouched; it persists nothing, and the personal data it
passes through belongs to Store and Tickets. **Invariant doc** — `Stripe.md` updated in the
same commits as the behaviour it describes (findings 1, 2, 5, 6, 10). **Migrations** — none;
the section owns no tables. **Navigation** — not applicable; nothing is reachable from a page.
**Tests** — 37 passing in `tests/Humans.Stripe.Tests`, up from 21; full `dotnet test
Humans.slnx` green (Integration self-skipped, as designed).

## Skipped

Findings 22–24 needed no action (dispositions above). Findings 25–29 are off-section shared
files this run may not touch → sweep queue.

Sections passed over as blocked (open `doctor(...)` PR): **Auth** (peterdrier/Humans#1575),
**Backdoor** (#1586), **Budget** (#1565), **Calendar** (#1578), **Campaigns** (#1564),
**Camps** (#1561), **Consent** (#1572), **Email** (#1587), **Feedback** (#1566), **Gate**
(#1574), **Governance** (#1580), **Holded** (#1583), **Monitor** (#1582). Down-ranked as
feature-active: AuditLog, Gdpr, Notifications, Rideshare.

## Threads

Raw per-thread finding counts before consolidation into the ranked list above.

| Thread | How it ran | Model | Findings |
|---|---|---|---|
| Shape (target) | main | session default | 3 → findings 1, 2, 19 (all three from the target, before any scan) |
| Behavior & bugs | main, tool-free | session default | 1 → finding 1, then confirmed by execution |
| Freshness | subagent | opus (low effort) | 9 → findings 5, 6, 25, 26, 27, 28, 29 |
| Tests | subagent | opus (low effort) | 12 → findings 15, 16, 17, 18, 20, 21; independently re-found finding 1 |
| History | subagent | opus (low effort) | 11 → finding 10; two claims rejected on verification (findings 14, 24) |
| Comments | subagent | opus (low effort) | 14 → findings 7, 9, 11, 12, 13 |
| Prose & surface + Conformance | subagent (one combined run) | haiku | 6 + conformance table → findings 3, 4, 23 |
| Inbox | main (no GitHub tool in the subagent) | session default | 1 → finding 22 |
| Second opinion (finding 1, guard + contract tests) | subagent | opus (high effort) | APPROVE; one correction accepted — the pre-change doc-comment promised `null` for a missing scope, not for an unset key, so the guard is a contract-uniformity fix, not a restored promise |
| Second opinion (finding 2, registrar collapse) | subagent | fable (high effort) | APPROVE; it established that no reached path changes and the exception asymmetry survives, and correctly refused to credit the new boot-job tests as coverage of the collapse (see Retro) |

Independence check: pass.

## Retro

**What the selector/rubric got wrong:** nothing. Stripe was the median of 12 never-doctored
sections by reforge score; 13 of 43 were blocked by open doctor PRs, which is the highest
blocked count any run has faced and worth watching — the never-doctored tier is down to
eleven sections and the pool is thinning faster than PRs merge.

**Wasted motion:** two subagent claims cost verification rounds and produced nothing. The
History thread reported `docs/sections/G5-SECTION-TEMPLATE.md` absent (it is present) and the
`2026-06-04` reconciliation-design pointer unresolvable (it resolves). Both were "this
reference is stale" claims about files outside the section, which is exactly where a
section-scoped grep returns a false negative. Neither produced an edit; each cost a check.

**What the assessment missed that striking revealed:** the guard for finding 1 is not what
the assessment said it was. The ranked entry read "restores the documented `null`" — but the
pre-change doc-comment promised `null` only for a missing *scope*, never for an unset key.
The reviewer caught it; the commit message and `Stripe.md` say the accurate thing (the guard
makes the four methods' contract uniform, and the doc-comment now covers the case it never
did). Second: the boot-job tests written alongside finding 2 do not cover finding 2. Every
path they exercise returns before the first network call, and `WebhookEndpointService` is
constructed inline from a `StripeClient`, so the deletion pass is unreachable from a test as
written. The reviewer refused to credit it, correctly; the collapse ships behind a reviewer
gate and a reasoned equivalence argument, not behind tests. That is worth naming plainly:
this section's two boot jobs are untestable past their configuration gates without a seam
none of them has, which is finding 30 below.

**Target diff:** none possible — first doctor pass, `health.md` written this run. One
correction landed on the target itself mid-run (finding 19: the round-trip claim was true
outbound only), and one section of it moved from "should change" to "reached this shape"
when finding 2 shipped. Next run gets the first real diff.

One auto-compaction occurred mid-Phase 4, between the reviewer gate on finding 2 being
dispatched and its verdict arriving. Phase 5's mandatory re-read of Phases 5–7 is what
recovered the run-file block list; the compaction summary had kept the goal and shed the
mechanics, exactly as that rule predicts.

**30** — [process] The registrar and the smoke probe both construct their Stripe service
objects inline from a `StripeClient` built in the method body, so nothing past the
configuration gates is reachable from a test. Every behavioural claim about the deletion pass
— which endpoints it deletes, which exceptions it swallows — rests on reading, not running.
Introducing a seam is new surface and a design call, so it is Peter's; recording the gap is
this run's job.

**31** — [skill] Phase 4 step 4's reviewer gate says "reject → rework once; second reject →
revert". It does not say what to do with an APPROVE that carries a material correction, which
is what both of this run's gates returned. Proposed edit to **Phase 4**: an APPROVE with a
correction is applied to the finding's description and the commit message before committing,
and the correction is named in the run file's retro — it is not a silent approval.

## Needs Peter

- [ ] 25 — `memory/code/stripe-restricted-keys.md` points at `docs/sections/<Section>.md` and names a nonexistent `STRIPE_BUSSES_KEY`: fix both in the sweep as queued, or reword the atom yourself?
- [ ] 30 — Stripe's two boot jobs are untestable past their config gates (no seam on `WebhookEndpointService`/`PaymentIntentService`). Add a seam, or accept read-only assurance there?
- [ ] 31 — Phase 4 of the skill: should an APPROVE-with-correction be an explicit outcome, applied to the finding and named in the retro?

## Sweep queue

- memory: `code/stripe-restricted-keys` — point scope documentation at `src/Sections/Humans.<Section>/Docs/<Section>.md`, not `docs/sections/<Section>.md` (templates only post-G5), and replace the nonexistent `STRIPE_BUSSES_KEY` example with a real key (finding 25, 2026-09-04-Stripe)
- memory: `architecture/refunds-manual-via-dashboard` — the app handles all four `checkout.session.*` events, not only `checkout.session.completed`; the rule's conclusion stands, its premise is stale (finding 26, 2026-09-04-Stripe)
- memory: `code/stripe-restricted-keys` + `architecture/refunds-manual-via-dashboard` — both cite a `Store.md` heading "Stripe Configuration"; the heading is `## Stripe Connector` (finding 27, 2026-09-04-Stripe)
- debt: `src/Sections/Humans.Tickets/Docs/Tickets.md:160,213` calls Stripe "Infrastructure" / "Infrastructure-only"; it has been its own section project since the G5 carve-out (finding 28, 2026-09-04-Stripe)
- debt: `src/Sections/Humans.Store/Docs/Store.md:259` says the webhook is for "`checkout.session.completed` ingestion" while `:243` in the same file says all four events (finding 29, 2026-09-04-Stripe)

## File coverage

The section owns no data, so there are no `generated` paths.

**Changed:**
`src/Sections/Humans.Stripe/Contracts/IStripeService.cs` ·
`src/Sections/Humans.Stripe/Docs/Stripe.md` ·
`src/Sections/Humans.Stripe/Docs/data-access.md` ·
`src/Sections/Humans.Stripe/Docs/health.md` (new) ·
`src/Sections/Humans.Stripe/Humans.Stripe.csproj` ·
`src/Sections/Humans.Stripe/Properties/AssemblyInfo.cs` ·
`src/Sections/Humans.Stripe/Section.cs` ·
`src/Sections/Humans.Stripe/Services/StoreWebhookRegistrationService.cs` ·
`src/Sections/Humans.Stripe/Services/StripeService.cs` ·
`src/Sections/Humans.Stripe/Services/StripeStartupSmokeService.cs` ·
`tests/Humans.Stripe.Tests/Architecture/StripeConnectorArchitectureTests.cs` ·
`tests/Humans.Stripe.Tests/Humans.Stripe.Tests.csproj` ·
`tests/Humans.Stripe.Tests/Services/StripeBootJobTests.cs` (new) ·
`tests/Humans.Stripe.Tests/Services/StripeCheckoutAmountTests.cs` ·
`tests/Humans.Stripe.Tests/Services/StripeServiceContractTests.cs` (new)

**Reviewed:**
`tests/Humans.Stripe.Tests/Services/StripeSignatureSanityTest.cs`

Read but not in the 3a inventory (other sections' files, consulted to settle a claim and left
untouched): `src/Sections/Humans.Tickets/Services/TicketSyncService.cs` ·
`src/Sections/Humans.Store/Docs/Store.md` ·
`src/Sections/Humans.Store/Docs/2026-06-04-store-stripe-payment-reconciliation-design.md`
