# Freshness Sweep Report

**Date:** 2026-08-03
**Mode:** diff
**Previous anchor:** `39a9fa32` · **New anchor:** `upstream/main` `5a9bbe198`
**Worktree base:** `origin/main` `c2ff51423`
**Dirty set:** 10 of 11 mechanical entries (8 prompt + 2 script) + 62 editorial docs + 0 unmarked docs
**Outcome:** 20 editorial docs drift-fixed · 8 mechanical regens updated (+3 verified clean) · 13 husks pruned (−4,694 lines, within the 4,770 cap) · 10 wheat migrations into 8 living docs · 8 inbound-ref rewrites · 0 errors

Range themes (upstream `39a9fa32..5a9bbe198`, ~60 commits): the **per-section DbContext split** (nobodies-collective/Humans#858 — SystemSettings, Containers, Agent, Expenses, Finance, Surveys and EventGuide each peeled onto their own context, factory and `__EFMigrationsHistory_<Section>` table, plus guard-rail analyzers #1140 and the new marker-only `Humans.Interfaces` project #1120); **persistent login sessions** on a 14-day sliding cookie (#1138); a **Board/Admin team roster page** at `/Teams/{slug}/Roster` (#1137); a batch of **Agent** hardening (never end a turn empty #1144, synthesize on tool-cap #1107, self-correcting doc-fetch misses #949, conversation paging #930, 9 more reachable sections #1134); **Surveys `LoggedInSince` audience** (#894) with status-walled accounts excluded (#1099); **Google sync** stops retrying terminal Drive permission failures (#1147); consent notifications repointed to `/Consent` (#954); Store catalog year derived from the active event (#924).

## Updated automatically (mechanical)

- `development-stats.md` — script: +5 daily rows (table now 135 data rows); class/interface source reforge=5 days, regex-fallback=0
- `reforge-history.csv` — script: 124 rows, ok=5 fail=0
- `authorization-inventory.md` — new `PolicyNames.TeamsAdminOrAdmin` (TeamsAdmin, Admin) registered but wired to no controller — currently referenced only from `Team/Details.cshtml`'s "Open store" boolean; added to §3/§5. `TeamAdminController.Roster` (action-level `BoardOrAdmin`, narrowing the class-level coordinator-reachable surface) added to §1 with its matching `authorize-policy` link row in §2. §6 call-site line numbers corrected for `StoreController.cs` and `TeamController.cs`
- `controller-architecture-audit.md` — 89 controllers re-audited; `TeamAdminController.Roster` (GET `/Teams/{slug}/Roster`) was in code but undocumented — added; date → 2026-08-03
- `architecture/dependency-graph.md` — added the missing `Cantina → BurnSettings` edge; removed the stale `AdminLegalDocumentService` node (merged into `LegalDocumentSyncService`, #751/PR #1155). All 85 Application service constructors re-walked; every other edge and lazy-edge confirmed
- `architecture/service-data-access-map.md` — rewritten methodology: **there is no longer a single `HumansDbContext`.** Added the eight-context ownership table (which context owns which tables, incl. the deliberate exclusion of the Shifts-owned `event_settings` / `event_participations` from `EventGuideDbContext`), the explicit-configuration-application rule, and per-section "**DbContext:**" annotations. Notes the new `Humans.Interfaces` marker project; date → 2026-08-03
- `About/Index.cshtml` — all 47 production packages cross-checked against `Directory.Packages.props`; package set unchanged, Hangfire.Core + Hangfire.AspNetCore 1.8.23 → 1.8.24 (lockstep pin)
- `docs/README.md` — file sets match exactly (no adds/removes/renames); 2 stale descriptions fixed (Expenses no longer generates SEPA batches — it reads paid/unpaid back from the Holded ledger; the Membership Status Partition is no longer consumed by Volunteers-team sync, which computes eligibility itself). All 148 relative links verified to resolve on disk
- Verified clean (dirty but no drift): `guid-reservations.md` (all 7 GUID blocks and sources already correct), `architecture/data-model.md` (107 Domain entities cross-checked 1:1 against Infrastructure configurations and against every section doc's Data Model — no additions, removals or reassignments post-#858; the `#dbcontext-ownership` anchor that `design-rules.md:261` points at resolves), `seed-data.md`
- Not triggered: `code-analysis-suppressions`

## Updated (editorial drift fixes — 20 docs)

**Architecture (4):** `design-rules.md` — the layer table's Infrastructure row now names the application contexts (`HumansDbContext` **plus** the per-section `<Section>DbContext`s, §2b) instead of a single context; the project-reference sentence now includes `Humans.Interfaces.csproj`, and a new paragraph documents `Humans.Interfaces` as the lowest-level project (no project references, no packages; holds the role markers and architecture attributes, namespaces deliberately left as `Humans.Application.*` so call sites and the analyzer full-name constants are unaffected by the move) · `roslyn-analysis.md` — the "DbSet write ownership should become one analyzer family" wishlist item is now marked **SHIPPED** as `HUM0025` (`SingleRepositoryPerTableAnalyzer`), which since the split matches DbSets on **every** application context via `Internal/SectionDbContexts.cs`, not just `HumansDbContext`; the bespoke AuditLog/Events/Notifications ratchets it replaced are gone; the repository-implementation bullet now says `IDbContextFactory<TContext>` and names the two arch-test rules that already generalize it · `conventions.md` — fetch() exceptions table said `Notification/Index.cshtml`; the actual view folder is `Notifications/` · `code-review-rules.md` / `coding-rules.md`: no contradictions found.

**Agent (2):** `sections/Agent.md` — tool whitelist was missing `fetch_community_faq` (and the fact that it resolves against a *separate* `IGuideContentSource` bound to the `nobodies-collective/knowledge-base` repo, not the Humans repo); tool-loop bound now describes the cap-hit behaviour (error result telling the model to answer now, then a `ToolChoice: none` call so it must synthesize rather than dead-end, #1107); new invariant 11 "a turn never ends with an empty assistant reply" (#1144), with the `route_to_issue` fallback persisted-not-streamed vs the generic fallback streamed-and-warned distinction; Tier2 preload corrected 14 → 23 sections; admin conversation view gained Older/Newer paging · `features/agent/agent-section.md` — `/Agent/Conversations` acceptance now distinguishes the user view (fixed most-recent window, no paging) from the admin view (zero-based `page` param preserving filters); turn workflow updated for cap-synthesis, empty-reply fallback, and turn-wide token totals across loop iterations.

**Auth (2):** `features/auth/authentication.md` — Security Considerations said only "ASP.NET Core Identity handles session tokens"; now states every sign-in path uses `isPersistent: true` with the 14-day sliding `ExpireTimeSpan` · `guide/SigningIn.md` — had no mention of session persistence at all even though the behaviour changed (session cookies used to die on browser close); added an end-user "Staying signed in" section. `sections/Auth.md`, `sections/Users.md` and `features/scanner/gate-terminal-login.md` were already correct — fixed inside the anchor range by the feature PRs themselves; re-verified line-by-line against `Program.cs` and the controllers.

**Teams/Camps (2):** `features/teams/teams.md` — new US-6.13 for the Board/Admin per-team Roster page (route, `BoardOrAdmin` gate, legal-name + burner-name + join-date columns, default sort, sidebar link) · `guide/Teams.md` — added a distinguishing bullet and subsection so the new per-team legal-name roster isn't confused with the pre-existing cross-team `/Teams/Roster` role-slot view. `sections/Teams.md` already carried the route row correctly. Verified no-drift: `global-search.md` / `user-search-overhaul.md` already state the real field scope (Humans: Name|Bio incl. city/interests/pronouns/CV/contact/public-email; Teams/Camps/Shifts: name-only; Events: title+description) — #1148 corrected the *code comments* to match docs that were already right.

**Google (2):** `features/google-integration/google-integration.md` — the permission-filtering comment claimed direct-permission detection excludes only *fully*-inherited permissions (`All(d => d.Inherited)`); the real semantics are `Any`, so a permission mixing direct and inherited detail entries is excluded too (Drive 403s deleting it at this level). Error-scenarios row rewritten to the real flow: excluded before the delete is attempted; if Drive still 403s on a list/delete race it is classified **terminal**, logged once, and not retried until the next reconciliation pass (#945/#1147) · `sections/GoogleIntegration.md` — same any-inherited-component correction plus the terminal/no-retry classification.

**Sections — per-section DbContext (5):** `sections/Finance.md` — the Migrations line still named the pre-split `HumansDbContext` chain (`HoldedActuals`, `HoldedCreditorData`, `HoldedCreditorContact`, `HoldedLedgerSingleSource`); now `20260715103643_BaselineFinance` on `FinanceDbContext` with its own `__EFMigrationsHistory_Finance`, noting the old chain was squashed into it · `sections/Survey.md` — repository factory corrected `IDbContextFactory<HumansDbContext>` → `IDbContextFactory<SurveysDbContext>`; new negative rule that the `LoggedInSince` audience excludes GDPR-anonymized, deletion-pending, merged, and `Rejected`/`Suspended`/`AdminSuspended` accounts (#1099) · `sections/Expenses.md`, `sections/Containers.md`, `sections/Events.md` — **orchestrator fix.** These three still described data access as generic `DbContext` with no mention of their own context, while the peer sections (Agent, Survey, Finance) already named theirs. Added a **DbContext** bullet to each naming the context, its owned tables, its `__EFMigrationsHistory_<Section>` table and `Migrations/<Section>/` folder — and, for `EventGuideDbContext`, the deliberate exclusion of the Shifts-owned `event_settings` / `event_participations` (hence `EventGuideSettings.EventSettingsId` staying a bare cross-context FK). Every claim verified against the contexts' own XML docs.

**Mailer (1):** `sections/Mailer.md` — `MailerLiteClient` 429 handling documented: up to 2 retries (3 attempts), honouring `Retry-After` clamped to 0–90s, defaulting to 60s when absent or unparsable (#1103).

**Debug/CI (3):** `features/debug/http-errors.md` — the aborted-requests-record-as-499 bullet omitted the new exception; added a 4th special case (authenticated aborts under `/Profile/Picture` are not recorded; anonymous aborts still are, #1111) · `features/debug/client-stats.md` — same skip added to the error-response workflow · `features/test-system-reliability.md` — two references to `.github/workflows/build.yml:36` were stale after #1132 pushed the integration-test filter down; corrected to `:50`.

**Indexes (1):** `sections/_Index.md` — `GuideController` was mis-filed under the "Event Guide" row (festival events); split out into its own **Guide** row. The "Finance (Holded)" row conflated two distinct section docs; split into **Finance** and **Holded**. Finance's table list named `holded_payments` / `holded_creditor_balances`, neither of which exists (Finance.md is explicit that balance and payments are *derived* from ledger lines) and omitted `holded_ledger_lines` / `holded_creditor_contacts`, which do — corrected against the `ToTable()` calls. **Surveys** had no row at all; added.

**Verified no-drift:** the remaining 42 triggered docs. Notably `sections/LegalAndConsent.md` + `features/legal-and-consent/legal-documents-consent.md` + `guide/LegalAndConsent.md` (no `/Legal/Consent` reference survives anywhere in `docs/`; every `AdminLegalDocumentService` mention is already correctly framed as consolidated into `LegalDocumentSyncService`), `sections/Store.md` + `features/store/store.md` + `guide/Store.md` (no doc asserts a hardcoded 2026 catalog year), `sections/Guide.md`, `sections/Camps.md` / `features/camps/camps.md` / `guide/Camps.md` (none describes carousel autoplay or `/Barrios` filter-param handling at the level #1146/#926 touched), `seed-data.md`.

## Pruned — 13 husks, −4,694 lines (target 3,407 = 5%; cap 4,770 = 7%)

Doc corpus 68,143 → 63,660 lines. Three batches, oldest-eligible-first: plans older than 30 days (before 2026-07-04) and specs older than 60 days (before 2026-06-04), by filename date prefix.

### Wheat migrated (10 blocks, each code-verified)

| Source §section → Destination | Verified by |
|---|---|
| store-team-orders-design §Out of scope → `sections/Store.md` (why `StoreOrder` is polymorphic, not a `store_team_orders` table) | `StoreOrder.cs` — `CampSeasonId`/`TeamId` both nullable on one entity; no second table exists |
| holded-finance-integration-design §1 → `sections/Holded.md` (Holded REST **API v1 only**; v2 403s on every endpoint without a registered developer OAuth app) | `HoldedClient.cs` — every path `/api/{invoicing,accounting}/v1/…`, `key` header only, no Bearer/OAuth |
| holded-finance-integration-design §3 → `sections/Finance.md` (member ↔ creditor link resolves via `supplierRecord.num`, never name matching; `/Finance/Creditors/Bind` is the manual fallback) | `ExpenseReportService.cs:949-969`; `HoldedClient.cs:205,220`; `HoldedFinanceService.BindCreditor` |
| early-entry-camps-design §Authorization → `sections/Camps.md` (EE grant/revoke reuses `ResolveCampManagementAsync`; there is **no** `CampOperation.SetEarlyEntry`) | `CampController.SetMemberEarlyEntry`; grep for `SetEarlyEntry` across both Authorization trees → 0 hits |
| camp-detail-roles-roster-design §Part A → `sections/Camps.md` (Roles/Roster card visibility matrix; both sourced from `BuildPanelAsync`, same data as `/Edit/Members`) | `CampController.PopulateDetailCardsAsync` + `CanSeeFullCamp` (:1295); `_CampRolesCard.cshtml`, `_CampRosterCard.cshtml` |
| early-entry-roster-design §Consumers → `sections/Shifts.md` (`GET /Shifts/Admin/EarlyEntry` cross-source roster; orchestrator page, deliberately not its own section) | `EarlyEntryRosterController` — `[Route("Shifts/Admin/EarlyEntry")]`, `[Authorize(Policy = ShiftDashboardAccess)]` |
| early-entry-roster-design §Consumers → `sections/Tickets.md` (the EE pill is **always the viewer's own** date via the single `TicketStubInfo.From` mapper — a transfer cannot leak the sender's EE) | `TicketTransferDtos.cs:107-115` (`holderEarlyEntry` is the viewer's, supplied by the caller); `MyTicketStubsViewComponent`, `TicketHoldingsViewComponent`, `ScannerController:82`, `TicketTransferWizardViewModel.HolderEarlyEntry` |
| team-rotas-coordinator-message-design §Routes → `sections/Shifts.md` (`GET`/`POST /Teams/{slug}/Shifts/Email`) | `ShiftAdminController.EmailTeamRotas` :406/:427 under `[Route("Teams/{slug}/Shifts")]` |
| team-rotas-coordinator-message-design §Audience → `sections/Shifts.md` (Pending-or-Confirmed, rota included on **end** not start, one email per recipient grouped by rota, `Reply-To` coordinator / `From` shared) | `RotaCoordinatorMessageService.cs:215` (`GetAbsoluteEnd(rotaEs) > now`), `:231` (status filter), `:182` doc comment |
| team-rotas-coordinator-message-design §Audit → `sections/Shifts.md` (one `CoordinatorTeamRotasMessageSent` per dispatch, not per recipient) | `RotaCoordinatorMessageService.cs:138-144`; enum value confirmed in `AuditAction.cs` |

**One manifest claim rejected rather than applied.** The camps/early-entry analysis proposed recording that "the transfer wizard never populates `EarlyEntryDate`, so a recipient cannot see the sender's EE status." The code contradicts the premise — `TicketTransferWizardViewModel.HolderEarlyEntry` explicitly stamps it, and `TicketTransferViewModels.cs:24` says so. The actual invariant is stronger and was migrated instead (row 7 above): the value is always the *viewer's* own EE, so it can neither leak across a transfer nor go missing on one surface.

### Husks deleted

| Batch | Files | Lines |
|---|---|---|
| Mailer + ticket transfer | `specs/2026-05-12-mailer-section-inbound-import-design.md` (554), `specs/2026-05-14-mailer-outbound-audiences-design.md` (391), `specs/2026-05-12-ticket-transfer-ui-history-design.md` (370) | 1,315 |
| Store + Finance | `plans/2026-05-18-store-summary-aggregates.md` (987), `plans/2026-05-27-store-team-orders.md` (936), `specs/2026-05-27-store-team-orders-design.md` (160), `specs/2026-05-25-holded-finance-integration-design.md` (139), `specs/2026-05-18-store-summary-aggregates-design.md` (138) | 2,360 |
| Camps + early entry + teams | `specs/2026-05-27-team-rotas-coordinator-message-design.md` (284), `specs/2026-06-02-team-early-entry-design.md` (274), `specs/2026-05-25-early-entry-roster-design.md` (186), `specs/2026-05-10-early-entry-camps-design.md` (151), `specs/2026-05-20-camp-detail-roles-roster-and-lead-decouple-design.md` (124) | 1,019 |

All-chaff drops with nothing to migrate: both mailer specs (superseded by `sections/Mailer.md`, which already documents Website-group-only import scoping, the Reset pass and the `MailerAudienceBase` opt-out exclusion — none of which the specs mention; their §9.1 "process-everyone-or-fail-loud" reliability invariants were never implemented, and the ML date-format and rate-limit quirks are self-documented in `MailerLiteDateConverter` / `MailerLiteClient` XML docs), the ticket-transfer-UI spec (explicitly superseded by the wizard spec; the whole `VendorStepsJson` timeline / retry-issue / Send-page engine it describes is gone, and its one surviving idea — `TicketAttendeeOwnership.IsCurrentOwner` — is already in `sections/Tickets.md`), both store plans and the store-summary design (pure implementation task lists; the shipped shape has since evolved past them and is current in `sections/Store.md`), and `2026-06-02-team-early-entry-design.md` (every decision — the `Art: {ProjectName}` source label, `EETeamAdmin` superseding the proposed `EarlyEntryArtAdmin`, the grants table, disable-keeps-grants, GDPR/merge coverage, all routes incl. `LookupTicket` — already present and verified in `sections/Teams.md`).

### Inbound refs rewritten (8)

- `specs/2026-05-23-ticket-transfer-wizard-design.md:7` — "Supersedes … 2026-05-12-ticket-transfer-ui-history-design.md" → annotated historical, pointing at `sections/Tickets.md` + `features/tickets/ticket-transfer.md`
- `plans/2026-05-25-holded-finance-feature1-actuals.md:11` and `plans/2026-05-25-holded-finance-feature2-creditor.md:11` — spec pointer → historical, pointing at `sections/Holded.md` (§1 API finding) and `sections/Finance.md` (§3 creditor link)
- `plans/2026-05-20-camp-roster-roles.md:11` → historical, pointing at `sections/Camps.md`
- `plans/2026-05-25-early-entry-roster.md:15` → historical, pointing at `sections/Shifts.md` + `sections/Tickets.md`
- `plans/2026-06-02-team-early-entry.md:25` → historical, pointing at `sections/Teams.md`

**Left as-is (deliberate):** two occurrences of a deleted filename survive inside *quoted historical command text* — a `gh pr create --body "…"` string in `plans/2026-05-20-camp-roster-roles.md:873` and a completed checklist step in `plans/2026-05-25-holded-finance-feature1-actuals.md:962`. Neither is a markdown link; both are transcripts of actions already taken, and editing them would falsify the record.

### Not eligible this sweep

- `architecture/tech-debt-2026-04-23.md` — re-checked; still carries genuinely-open items (`BudgetRepository` `ResponsibleTeam` `.Include`, ViewComponent `IMemoryCache` injection, `HumansMetricsService` migration)
- `specs/2026-04-25-freshness-sweep-design.md` — the active spec for this skill
- Specs dated 2026-06-04 and later (not yet 60 days old); `docs/superpowers/probes/**` (not on the prune allowlist)
- Budget candidates deferred to the next sweep (analysis not started — a budget decision, not a punt): the remaining large `superpowers/plans/*` husks, led by `2026-05-23-volunteer-tracking-export.md` (2,761), `2026-05-25-holded-finance-feature2-creditor.md` (1,642), `2026-05-25-dietary-prompt-tightening.md` (1,562), `2026-06-10-table-component.md` (1,482), `2026-06-09-ical-feed.md` (1,410)

## Flagged for human review

- `architecture/conventions.md` — the "current exceptions" `fetch()` table omits ~10 files that genuinely use `fetch()` (`TeamAdmin/Roles.cshtml`, `Guest/CommunicationPreferences.cshtml`, `Issues/Index.cshtml`, `Profile/CommunicationPreferences.cshtml`, `Profile/Edit.cshtml`, the Feedback and Help widget components, `wwwroot/js/shifts.js`, `wwwroot/js/agent/widget.js`, `wwwroot/js/city-planning/**`). All predate the anchor by a wide margin (oldest traces to PR #150/#395-398) — pre-existing drift outside this sweep's changed-file scope, not edited. **Resolved (Peter, 2026-08-03): leave.** Criterion was "fix only if it blocks an in-progress migration" — it does not. Checked: no analyzer in `src/Humans.Analyzers/` and no test enforces the list (it is review-enforced convention, per the closing line "if a page has no entry here, it must be server-rendered"), and no `debt-ledger.yml` entry or `design-rules.md` §15i item touches client-side `fetch()`. It is a view-layer convention, orthogonal to the §15 service migration, the #858 DbContext split, the nav strip, and the section read-splits.
- `features/auth/authentication.md` — the role-catalog table is missing `StoreAdmin` and `EETeamAdmin`, both present in `sections/Auth.md` and `RoleNames.cs`. `RoleNames.cs` did not change in range; pre-existing, not edited.

## Questions — all four resolved by Peter inline, 2026-08-03

1. **`sections/_Index.md` and `admin-shell.md`** — the doc existed with no index row, because the index's own note says `/Admin/*` is not a section. **Resolved: add the row.** Peter's framing: Admin is a concept that will move into framework plumbing, and is the logical holder for the admin-type bits each section contributes, with a dynamically built nav in the long-term vision. Applied — an **Admin Shell** row was added to the *Cross-cutting concerns* table (`AdminController` `/Admin` tile only; `AdminNavTree` / `AdminSidebarViewComponent` / `AdminBreadcrumbViewComponent`; owns no tables), and the "`/Admin/*` is not a section" note was reworded to "is not a **vertical** section", now pointing at the Admin Shell row and recording the long-term direction so a future sweep doesn't delete the row as drift.

2. **`generate-stats.sh` stashes the working tree** — reverted two in-flight subagent outputs and the regenerated CSV mid-sweep (all recovered from the leftover stash; no work lost). **Resolved: file an issue.** Filed as nobodies-collective/Humans#971 (`bug`, `section:infra`, `size:S`, `db:no`, `tier:lightweight`), covering both scripts: the silent stash-clobber, the fact that `generate-reforge-history.sh` also leaves the tree detached at whatever commit its loop last visited, and three suggested fixes (drop the stash if unnecessary → run in a throwaway worktree → else refuse on a dirty tree rather than stashing it).

3. **`reforge-history.csv` vs `development-stats.md` ref mismatch** — `generate-reforge-history.sh` walks the local `main` ref (recorded `5a9bbe198`) while `generate-stats.sh` walks `HEAD` (recorded `c2ff51423`) for the same day. **Resolved: leave.** Rows join by date, so nothing is currently wrong.

4. **`architecture/conventions.md` fetch()-exceptions backlog** — **Resolved: leave**, having confirmed it blocks no in-progress migration. See the verification under *Flagged for human review*.

## Post-review corrections (Codex + claude-review on PR #1160)

Eight review findings, all verified against source and all valid. Six were errors this sweep introduced — worth recording, because four of them are the same failure mode: **an agent generalised a rule from a subset of call sites without checking the outliers.**

| # | Finding | Correction |
|---|---|---|
| 1 | `sections/Tickets.md` — the migrated EE-pill rule listed the Scanner gate card among the surfaces going through `TicketStubInfo.From`. `ScannerController` constructs the stub *directly* from `IEarlyEntryService.GetForUserAsync(hit.MatchedUserId)`, so gate staff see the **scanned attendee's** EE, by design | Split the rule: holder-facing surfaces are viewer-scoped; the Scanner is the deliberate staff-facing exception, with a "don't consolidate this onto the shared mapper" warning |
| 2 | `guide/SigningIn.md` — "each visit resets that 14-day clock" is false; ASP.NET Core `SlidingExpiration` renews only after >50% of `ExpireTimeSpan` has elapsed. (`Program.cs:146`'s comment says "any visit inside it extends it", which is where the claim came from) | Reworded for end users: a visit more than a week after the last one extends the window by 14 days |
| 3 | The prune inbound-ref scan covered `docs/` and `memory/` only, so it missed `tests/Humans.Application.Tests/Services/Store/StoreServiceTeamOrdersTests.cs:22`, whose class summary cited a deleted design doc | Repointed to `sections/Store.md`. Re-ran the scan across `src/`, `tests/`, `scripts/` and `.claude/` for all 13 deleted husks — this was the only hit |
| 4 | `sections/Camps.md` — claimed both detail cards project `CampRoleAssignment` via `BuildPanelAsync`. Only **Roles** does; **Roster** is a `CampMember` projection off `season.ActiveMembers` | Rewritten to give each card its real source and to say they can legitimately disagree. Visibility detail also sharpened: a signed-in non-member gets the Roles card retitled **"Leads"** and filtered to the Lead role's filled assignees (`_CampRolesCard.cshtml`) |
| 5 | `architecture/service-data-access-map.md` — the regen invented a thread-safety rationale for sequential fan-out. Post-split the two contributors use *different* contexts from separate factories, and independent factory-created contexts are safe concurrently | Rationale corrected. **The same stale claim was found and fixed in `design-rules.md` §8b**, which this sweep should have caught on its own (design-rules was a triggered doc and #858 is in range) |
| 6 | `sections/Agent.md` — the Tier2 count was fixed 14 → 23 but the adjacent wheat `NOTE` still explained the ITPM budget in terms of 14 sections | Note updated to 23, naming `AgentPreloadCorpusBuilder.Tier2Sections` as the source of truth and explaining why the growth stays under the tier ceiling |
| 7 | `sections/Finance.md` — the migrated wheat repeated the code's claim that a null `supplierRecord.num` is "backfilled on the paid poll". No such retry exists: `GetContactAsync` has exactly one call site, and `SetCreditorAccountNumAsync` fires only when that first lookup succeeded | Doc now states the real behaviour (one best-effort attempt, no retry, manual `POST /Finance/Creditors/Bind` required after a miss). The stale code comment and its two log messages are a genuine defect — filed as nobodies-collective/Humans#972 |
| 8 | claude-review WARN — the sweep did not update `docs/architecture/maintenance-log.md`, per `memory/process/maintenance-log-update.md` | Row bumped. **Root cause fixed too:** the skill's own Constraints said "does not update maintenance-log.md (hand-maintained)", contradicting `CLAUDE.md` and the memory atom — `.claude/skills/freshness-sweep/SKILL.md` now requires the sweep to bump its own row and leave the rest of the file alone |

Worth encoding in the drift-fix subagent prompt next time: when a doc claim takes the form "every X does Y", the agent must enumerate *all* call sites and name the exceptions rather than asserting the rule from the majority. Findings 1, 4, 5 and 7 are all that shape.

## Proposed for review

None — all prune candidates resolved this sweep (migrate / drop / verify-clean). No uncertain wheat was queued for a future pass.

## Skipped (errors)

None. Five subagents went idle without delivering their result payload (`authorization-inventory`, `editorial-agent`, `data-model-index`, and both prune batches); two prune batches and the data-model entry were re-dispatched and completed, and the other two had already written their edits, which were read back from the worktree diff and verified against source. No entry was skipped.
