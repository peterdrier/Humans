# Freshness Sweep Report — 2026-05-27

- **Mode:** diff (batch)
- **Previous anchor:** `7de0d0d2` (set by sweep #794, 2026-05-26)
- **New anchor:** `1c4c6ad2` (upstream/main HEAD)
- **Diff range:** `7de0d0d2..upstream/main` — 106 files changed (60 src/, 11 tests/, ~35 docs/superpowers/ + assorted root)
- **Mechanical entries dirty:** 10 of 11 (only `code-analysis-suppressions` didn't fire — no `Directory.Build.props` / `BannedSymbols.txt` change)
- **Mechanical entries updated:** 6 (`dev-stats`, `reforge-history`, `controller-architecture-audit`, `authorization-inventory`, `service-data-access-map`, `data-model-index`); 4 regenerated byte-identical or no-op (`about-page-packages`, `docs-readme-index`, `dependency-graph`, `guid-reservations`).
- **Pruned:** 10 historical husks deleted (−6,056 lines), 1 wheat migration into `sections/Auth.md`.
- **Docs tree:** 135,798 → 129,757 lines (**−6,041, 4.45%** — just under the 5% soft target; well under the 7% hard cap).
- **Editorial docs flagged for drift review:** 26 (all `flag-on-change`; no `freshness:auto` blocks fired).

---

## Updated automatically

- **dev-stats** — appended one daily row via `generate-stats.sh`.
- **reforge-history** — refreshed `docs/reforge-history.csv` via `generate-reforge-history.sh`.
- **controller-architecture-audit** — added new `ShiftAdminController.EmailTeamRotas` (GET + POST) actions at `/Teams/{slug}/Shifts/Email` from PR #795 (team-level coordinator rota message). Verified action name against `conventions.md §Action Naming` — passes. Header date bumped to 2026-05-27.
- **authorization-inventory** — bumped Refreshed date to 2026-05-27; fixed §6 description for `ProfileController.cs:1826` (TicketAdminBoardOrAdmin onsite-chip visibility gate, not a shift-history viewer gate). Verified all 11 resource-based handlers + 4 composite handlers + 27 PolicyNames constants. **Carryover:** the 4 Events Guide controllers (`EventsAdminController`, `EventsDashboardController`, `EventsExportController`, `EventsModerationController`) still use `[Authorize(Roles = RoleGroups.EventsAdminOrAdmin)]` — Phase-1 deviation from the in-progress `docs/plans/2026-04-03-first-class-authorization-transition.md`.
- **service-data-access-map** — refreshed against 4 commits since anchor (PR #802, #804, #801, #795). Key change: `TicketRepository` no longer reads `UserEmails` (PR #802 retired the cross-section `UserEmail` projection, routing ticket email matching through `IUserServiceRead.GetAllUserInfosAsync` in `TicketSyncService.BuildEmailLookupAsync`). Cross-section violations table now shows only `UserRepository` reading `UserEmails` for the `UserInfo` unified read-model bridge (HUM0025 grandfathering per PR #804). Rewrote Pattern #4 ("Tickets ↔ Profiles email lookup retired"). Updated `RotaCoordinatorMessageService` to reflect PR #795 — added `IShiftManagementRepository` injection, `EventSettings` read, and team-level `SendTeamRotasMessageAsync` dispatch.
- **data-model-index** — verified the entity-index table against `src/Humans.Domain/Entities/` and `src/Humans.Infrastructure/Data/Configurations/` — all 90+ configured entities have correct owning-section links. One correction: `AccountMergeRequest` note now points to the actual folder `Humans.Application.Services.Profiles/` (plural) instead of the non-existent singular `Profile/` form.

**Unchanged (regenerated, byte-identical / no edit needed):**
- **about-page-packages** — all production package versions already match `Directory.Packages.props`; no card adds, removes, or version bumps.
- **docs-readme-index** — index already lists every `.md` under `docs/features/` (62), `docs/sections/` (33), and `docs/guide/` (25) with correct one-line descriptions; the four touched section docs (Campaigns, Email, Expenses, Finance) were content edits, not adds/removes.
- **dependency-graph** — only Application/Services change since anchor was `TicketSyncService` adding `IUserServiceRead`; folds onto the existing `TicketSync → User` edge per the read-split convention. No Mermaid edges added, removed, or relabeled.
- **guid-reservations** — table already lists the 5 deterministic GUID blocks (`0001`, `0002`, `0003`, `0010`, `0026`) with correct sources; no new literals discovered in `Constants/` or `Configurations/`.

---

## Pruned

This sweep cleared the **auth / governance / feedback / volunteer-management** cohort that the previous sweep (2026-05-26) explicitly deferred. All ten source docs are now operationalized in section invariants, feature docs, or constitution-level rules — no durable wheat remained except a single OAuth remote-failure note.

**Migration (wheat preserved):**

- `docs/superpowers/specs/2026-03-23-session1-auth-google-sync-design.md` → `docs/sections/Auth.md` §Invariants: OAuth correlation-cookie failures (missing/expired cookie, user-cancelled consent, browser cookie restrictions) are caught by an `OnRemoteFailure` handler in `Program.cs` that logs at Warning and redirects to `/Account/Login?error=sign-in-failed`. The unhandled `CorrelationException` would otherwise surface as a 500. *(provenance comment added)*

**Refs retargeted:** none. Living-doc mentions of "profileless" and the husked plans are conceptual references (e.g., "profileless accounts can do X") — they do not link to the deleted filenames. The only literal filename references were the husks' own front-matter self-references (deleted with the file) and the previous report's deferred-cohort paragraph (overwritten by this report).

**Husks deleted (all chaff — task lists, code samples now in src/, ADRs already migrated to section invariants):**

| File | Lines | Evidence |
|------|------:|----------|
| `plans/2026-03-23-feedback-upgrade.md` | 1,523 | Operationalized in `sections/Feedback.md` (FeedbackMessage aggregate, status flow, needs-reply derivation, AdminNotes-migration, nav badges) + `features/feedback/feedback-system.md`. |
| `specs/2026-03-23-feedback-upgrade-design.md` | 180 | All ADRs (timestamp-based unread tracking, master-detail layout, admin→reporter email only) now in `sections/Feedback.md`. |
| `plans/2026-03-23-session1-auth-google-sync.md` | 1,557 | Sync-mode invariants, EnsureTeamGroupAsync collision handling, settings remediation, AllGroups view documented in `sections/Auth.md` + `sections/GoogleIntegration.md`. The googlemail/NormalizeForComparison rationale lives in the source XML doc on `EmailNormalization.cs`. |
| `specs/2026-03-23-session1-auth-google-sync-design.md` | 174 | OnRemoteFailure wheat migrated. Group-link dedup, group-settings remediation, AllGroups view documented in `GoogleIntegration.md` routing table. Gmail/googlemail equivalence in `Users.md` + `Profiles.md`. |
| `plans/2026-03-23-session3-shift-polish-legal-section.md` | 1,031 | UI text changes are resx strings; `VoluntellRange` is in `Shifts.md` routing; `ILegalDocumentService` extraction + LegalController fully captured in `LegalAndConsent.md` (Statutes `/Legal` route, anonymous, GitHub-fetched, in-memory cache, separate from `legal_documents`). |
| `specs/2026-03-23-session3-shift-polish-legal-section.md` | 215 | VoluntellRangeAsync semantics (block-id, confirmed, EnrolledByUserId, skip-already-signed, overlap check) match `Shifts.md` state machine. Add-new-doc steps are operational (Documents array in source). |
| `plans/2026-03-19-vol-management.md` | 632 | Abandoned `/Vol` prototype (PR #12 parallel-rollout experiment). No `VolController.cs`, no `Views/Vol/`, no `/Vol` routes registered in src/. `/Shifts` is the surviving design. |
| `plans/2026-03-19-vol-management-gaps.md` | 108 | Gap-tracking checklist for the same abandoned `/Vol` prototype. |
| `specs/2026-03-19-vol-volunteer-management-design.md` | 309 | Design spec for the abandoned `/Vol` prototype. Genuinely durable shift concepts (period grouping, urgency scoring, voluntell semantics, range signups) already in `sections/Shifts.md`. |
| `plans/profileless-accounts-rip.md` | 327 | All 5 ADRs operationalized: ADR-1 (GuestController + MembershipRequiredFilter redirect) → `Onboarding.md` + `Auth.md`; ADR-2 (HasProfile claim nav filter) → `Auth.md`; ADR-3 (`FindOrCreateUserByEmailAsync` + ContactSource layering) → `Users.md` §Account Provisioning + Triggers; ADR-4 (GDPR deletion with event hold) → `Users.md` + `Profiles.md`; ADR-5 (Create-Profile via existing `ProfileController.Edit`) → `Onboarding.md`. UpdateSource="Guest" documented in `Profiles.md`. |
| **Total** | **6,056** | |

**Still load-bearing (not touched):**

- `docs/plans/2026-04-03-first-class-authorization-transition.md` (187 lines) — Events Guide controllers (`EventsAdminController`, `EventsDashboardController`, `EventsExportController`, `EventsModerationController`) still carry `[Authorize(Roles = RoleGroups.EventsAdminOrAdmin)]` despite `PolicyNames.EventsAdminOrAdmin` being registered. The transition plan must remain until Events Guide migrates to `[Authorize(Policy = ...)]`. The Phase-3 tombstone in that plan still carries the cycle-crash history (`225ac14`, `1626098`, `bbbe508` — service-layer authorization DI loop `TeamService → RoleAssignmentService → IAuthorizationService → TeamAuthorizationHandler → ITeamService`) that the previous sweep flagged for migration into `design-rules.md §11` before eventual pruning.

---

## Flagged for human review

### High-signal findings (from mechanical regeneration)

**Authorization (`authorization-inventory`):**

- **Events Guide controllers still use raw role strings** (carryover, third sweep in a row). `EventsAdminController`, `EventsDashboardController`, `EventsExportController`, `EventsModerationController` carry `[Authorize(Roles = RoleGroups.EventsAdminOrAdmin)]` even though `PolicyNames.EventsAdminOrAdmin` is registered. → Migrate to `[Authorize(Policy = ...)]` so the auth-transition plan above can finally be pruned.

**Service data-access (`service-data-access-map`):**

- **`Tickets ↔ Profiles email lookup`** has been retired (PR #802) — `TicketRepository` no longer reads `UserEmails`. The only remaining cross-section read of `UserEmails` is from `UserRepository` for the unified `UserInfo` read-model bridge, which is grandfathered under HUM0025 (PR #804).
- **`RotaCoordinatorMessageService` (PR #795)** now reads `EventSettings` via `IShiftManagementRepository` and dispatches team-level coordinator emails (`SendTeamRotasMessageAsync`).

### Editorial drift flags (`freshness:flag-on-change` markers fired by trigger changes)

26 editorial docs had their flag-on-change markers fire. None had `freshness:auto` blocks, so the flags are for human review only — the underlying triggers changed, the listed concerns *may* have drifted.

**Sections (7):**
- `docs/sections/AuditLog.md` — AuditAction enum surface (new actions added).
- `docs/sections/Email.md` — Outbox queue/retry semantics, pause flag ownership.
- `docs/sections/Expenses.md` — Lifecycle, IBAN access rules, SEPA generation, Holded sync, resource-based authorization.
- `docs/sections/Finance.md` — Holded sync, creditor balance, payments (PR #785 brought feature 2).
- `docs/sections/Holded.md` — same Holded changes.
- `docs/sections/Shifts.md` — Shift entities, urgency scoring, dashboard panels, coordinator/admin auth — reconcile `RotaCoordinatorMessageService` PR #795.
- `docs/sections/Tickets.md` — Manual-processing model, lifecycle states, email notification points.

**Features (13):**
- `docs/features/active-user-metrics.md` — Window set, in-memory-only guarantee, middleware position.
- `docs/features/email/email-outbox.md` — Outbox queue/process/cleanup behavior.
- `docs/features/global/administration.md` — Admin/Board/Profile/Google route tables, role catalog.
- `docs/features/global/background-jobs.md` — Job catalog and schedules (HoldedSyncJob, ExpensePaidPollingJob added/changed).
- `docs/features/google-integration/google-removal-notifications.md` — Variant selection logic, suppression cases, MessageCategory routing.
- `docs/features/google-integration/workspace-account-provisioning.md` — Provisioning step ordering, post-provisioning admin tooling.
- `docs/features/onboarding/onboarding-pipeline.md` — End-to-end pipeline, parallel consent + profile-review tracks.
- `docs/features/onboarding/volunteer-status.md` — Volunteer status state machine, ActiveMember claim.
- `docs/features/shifts/coordinator-roles.md` — Coordinator capabilities, OnboardingReview authorization.
- `docs/features/shifts/email-a-rota.md` — Email template shape, recipient selection rules — directly impacted by PR #795 (team rotas).
- `docs/features/shifts/shift-management.md` — Shift entities, state machine, urgency scoring.
- `docs/features/shifts/shift-signup-visibility.md` — Privileged-viewer signup display, `includeSignups` flag.
- `docs/features/tickets/ticket-transfer.md` — Manual-processing model, lifecycle states.

**Guide (5):**
- `docs/guide/Admin.md` — Global control panel, audit log, notifications, sync settings.
- `docs/guide/Email.md` — Email provisioning, group address mechanics, Profile Emails surface.
- `docs/guide/Expenses.md` — Expense lifecycle, IBAN access, SEPA, Holded sync.
- `docs/guide/Onboarding.md` — Sign-up paths, profile wizard, consent gate.
- `docs/guide/Shifts.md` — Shift browsing/signup, rota management, event settings.

**Other (1):**
- `docs/seed-data.md` — Holded sync / Finance-related triggers fired.

---

## Questions

None.

## Skipped (errors)

None.
