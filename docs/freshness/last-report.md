# Freshness Sweep Report — 2026-04-25 22:10:57 UTC (extended through 2026-04-26)

**Anchor:** upstream/main @ 8f6a2556 (previous: none)
**Mode:** full
**Entries dirty:** 11
**Entries updated:** 9 (initial sweep) + comprehensive follow-up sweep
**Entries flagged:** originally 7 — all addressed
**Questions accumulated:** none unresolved
**Editorial docs reviewed for accuracy:** 21 (every section + Admin guide)

This report covers the original sweep on 2026-04-25 plus an immediate
follow-up sweep on 2026-04-26 that addressed every flagged item, added
inline freshness markers to all editorial docs, built a correctness
test harness, replaced brittle bash counting with Roslyn-derived
counts, re-snapshotted all historical reforge-history rows, audited
About-page package versions, verified diff-mode logic, and ran one
agent per section to scrub each section's invariants doc and user
guide against current code.

## Updated automatically (initial sweep)

- `about-page-packages` — Bumped 5 Microsoft framework packages from 10.0.6 to 10.0.7 and added 5 missing production packages (verified clean by the follow-up audit; all 43 production packages present and versions match Directory.Packages.props exactly).
- `docs-readme-index` — Regenerated indexed tables for `features/`, `sections/`, and `guide/`.
- `authorization-inventory` — Refreshed controller authorization map and added resource-based handler section for the RoleGroups → PolicyNames migration.
- `controller-architecture-audit` — Regenerated for current 46 controllers.
- `dependency-graph` — Regenerated Mermaid dependency graph from current service constructors.
- `service-data-access-map` — Regenerated by walking each service's repository deps to its EF DbSet usage and cache-key surface.
- `data-model-index` — Added ProfileLanguage row, removed TeamPage row.
- `guid-reservations` — Fixed stale source paths and added co-source.
- `code-analysis-suppressions` — Added missing CS1591, VSTHRD200, xUnit1051 entries.

## Follow-up sweep (2026-04-26)

### Item A — Editorial-tree marker coverage (the foundational gap)

Added inline `<!-- freshness:triggers -->` and
`<!-- freshness:flag-on-change -->` blocks to **80 editorial docs**
(20 sections + 40 features + 15 guide + 4 architecture rule docs +
seed-data). Total of **775 trigger globs**, every one verified to
match at least one real file. Architecture rule docs (cross-cutting
principles) carry only flag-on-change reason text; everything else
has precise per-doc trigger globs. Without these markers, diff-mode
treated every editorial doc as `src/**` (broad-net) and flooded the
report; with them, only the docs whose dependencies actually changed
are flagged.

### Item B — Correctness tests

Added `docs/scripts/freshness-checks/<entry-id>.sh` for each of the
11 mechanical catalog entries plus a `run-all.sh` aggregator. Each
check counts authoritative items in `src/` and asserts the regenerated
doc has them all. Final state: **11 of 11 PASS**.

### Item C — Roslyn-backed dev-stats

Replaced the brittle xargs/wc/grep counting in
`docs/scripts/generate-stats.sh` with a hybrid that consumes
reforge-snapshot's Roslyn-semantic counts for loc_prod / loc_test /
files_prod / files_test / classes / interfaces and keeps simple
file-extension counting in bash for the supplementary columns. Re-
backfilled all 72 daily rows in `docs/development-stats.md`; the
classes/interfaces columns now reflect Roslyn semantic accuracy
(records, partials, file-scoped declarations all counted properly).

### Item D — Diff-mode test harness

Added `docs/scripts/freshness-checks/diff-mode.sh` exercising Phase 3
(catalog/marker discovery) and Phase 4 (trigger glob matching) with
synthetic diffs. Caught 3 stale catalog globs (removed
`src/**/*.razor`, fixed `Domain/Authorization/**` →
`Application/Authorization/**`, fixed `ServiceCollectionExtensions/**`
→ `Extensions/Sections/**`). Final state: **6 of 6 PASS**.

### Item E — Original sweep flags (all 7 addressed)

1. `authorization-inventory.md` view section — re-scanned all
   `src/Humans.Web/Views/` and rewrote §2 from scratch. Discovered
   the legacy `RoleChecks.*` / `ShiftRoleChecks.*` helpers are
   no longer invoked from any view at all (Phase 1 retirement
   complete); replaced with the four current patterns
   (`authorize-policy="X"` TagHelper, `(await
   AuthService.AuthorizeAsync(...)).Succeeded`, scattered
   `User.IsInRole`, and `Model.CanX` view-model properties). Every
   line number now matches HEAD.
2. `authorization-inventory.md` transition plan reference — updated
   intro to reflect Phase 1 complete, Phase 2 first vertical slices
   shipped, Phase 3 cancelled.
3. `dependency-graph.md` prose — rebuilt cycles list and fan-in
   hotspot table by parsing the actual diagram. Removed 2 spurious
   cycles (Shifts↔Role, UserEmail↔AccountMerge — one-way edges, not
   cycles); added 2 real ones (Team↔TeamResource, GoogleWorkspaceSync
   ↔TeamResource). Corrected every fan-in count.
4. `service-data-access-map.md` TicketDashboardStats — documented as
   a "ghost cache key" — the populator
   (`TicketQueryService.GetDashboardStatsAsync`) does not actually
   read through the cache; the InvalidateTicketDashboardStats hook is
   reserved for a future caching wrapper.
5. `service-data-access-map.md` SystemSettings owner — resolved
   per-key. `IsEmailSendingPaused` → owned by Email
   (EmailOutboxRepository); `DriveActivityMonitor:LastRunAt` → owned
   by Google Integration (DriveActivityMonitorRepository). Keys are
   disjoint, so no cross-section access — removed the violation
   framing.
6. `service-data-access-map.md` Appendix A — re-audited every cited
   controller. Of 10, **8 no longer have direct DB access** post-§15
   (Profile, Board, Budget, CampAdmin, Guest, Unsubscribe, Email,
   Google) — REMOVED. Only AdminController and DevLoginController
   remain.
7. `conventions.md` Action Naming — added a §"Action Naming" subsection
   codifying the heuristics the controller-architecture-audit applies
   (Index = listing; no controller-name prefix repetition; no bare
   plural collisions; no generic verbs; conventional form-handler
   verbs). Updated the catalog prompt to point at the new subsection.

### Item F — dev-stats top sections

Refreshed Quick Summary, Language Breakdown, Highlights from the
latest 2026-04-26 row (was 2026-03-31). Codebase now at 180k lines
across 1,177 files with 1,043 classes / 119 interfaces / 46
controllers in 81 days of development.

### Item G — About-page package audit

Independently verified every production package (43 total)
appears on the About page with versions matching
`Directory.Packages.props` exactly. Zero discrepancies — the
original sweep's regeneration was clean.

### Item H — Historical reforge-history re-snapshot

Re-ran `reforge snapshot` against every historical commit in
`docs/reforge-history.csv` (rows 1–53). Compared each new row to
existing one byte-by-byte. **Zero substantive metric drift** — every
loc_prod, classes, reach, fanout, cyclomatic, class_loc value
reproduces exactly. The only delta was cosmetic (7-char → 8-char
SHA prefix as the repo grew). Plus a side-finding: pre-March-16
commits pin SDK 10.0.102 in their global.json, which would block a
clean `--full` re-run today; worth a script-level rollForward
override if `--full` is needed against pre-March-16 history.

### Item I — Per-section accuracy review (sections + guides)

Dispatched **20 background agents** (one per section) to review both
the section invariants doc and the paired user guide doc against
current code. Each agent read the relevant `src/**` files and fixed
every concrete-claim drift in place. Plus a 21st pass for the
standalone `docs/guide/Admin.md`.

Highlights of the corrections (deeply non-exhaustive):

- **Calendar**: status flipped (C) Pre-migration → (A) Migrated (the
  §15 work shipped 2026-04-23).
- **Tickets**: status flipped (B) Partially migrated → (A) Fully
  migrated. Removed entire stale "Current violations" section
  citing line numbers in a deleted file. Rewrote
  EventParticipation derivation rules — the previous "ticket
  purchase overrides NotAttending" claim was wrong; sync never
  touches NotAttending.
- **GoogleIntegration**: status flipped (B) → (A). Removed 18
  cross-domain `.Include()` claims and ~41 cross-section direct DB
  reads at line numbers in a deleted file.
- **LegalAndConsent**: status flipped (B) → (A). ConsentService now
  in Application with zero DbContext usage.
- **Notifications**: doc was substantially fictional. Replaced
  invented fields (`Category`, `DeepLinkUrl`,
  `RelatedEntityKind`/`Id`) with real surface (Title, Body, ActionUrl,
  ActionLabel, Priority, Source, Class, TargetGroupName,
  CreatedAt, ResolvedAt, ResolvedByUserId). Fixed composite PK on
  NotificationRecipient. Documented the real 27-value
  NotificationSource enum.
- **AuditLog**: largely fictional. Table name wrong
  (audit_log_entries → audit_log), fields wrong (EntityType not
  EntityKind, Description not Summary, OccurredAt not CreatedAt;
  added missing Google-sync columns). Replaced fabricated
  AuditAction enum members with all 81 real values, grouped by
  section. Added the named DB triggers prevent_audit_log_update /
  prevent_audit_log_delete.
- **Users**: largely fictional. Removed invented columns
  (IsArchived, SuspendedAt, Birthday, IcalToken mis-cased — Birthday
  lives on Profile, not User). Added missing real columns
  (PreferredLanguage, ProfilePictureUrl, CreatedAt,
  LastConsentReminderSentAt, ICalToken correct casing, etc.).
  Corrected Identity table names (AspNetUsers → users, etc.). Fixed
  the unsubscribe flow (it doesn't flip a User boolean — it updates
  per-category CommunicationPreference via Profile's service).

For the comprehensive agent-by-agent breakdown of changes per
section, see the per-batch commits in this PR.

## Open product questions surfaced (not addressed; require Peter's input)

The section reviews surfaced **20+ open product questions** about
real code-vs-doc gaps that are not pure doc bugs — they suggest
either a missing feature, a dormant code path, or a deliberate
incompleteness. Highlights:

- **Onboarding**: `ConsentCoordinator` policy permits Reject
  (currently `ConsentCoordinatorBoardOrAdmin`) — was that intent or
  should the route be tightened to `BoardOrAdmin`?
- **Onboarding**: auto-clear path silently adds the user to
  Volunteers without dispatching the welcome notification (only the
  manual ApproveVolunteerAsync path notifies). Product gap or
  intended?
- **Camps**: `CampLead.Primary` enum value exists but no production
  code path assigns it (CreateCampAsync / AddLeadAsync both assign
  CoLead). UI to transfer Primary doesn't exist. Dormant or remove?
- **Camps**: `SetSeasonFullAsync` implemented but no controller
  exposes it — wire up or remove?
- **Governance**: `RequestMoreInfo` Application trigger exists end-
  to-end (entity method, enum value, state machine reentry,
  localization) but no caller invokes it.
- **Governance**: Finalize endpoint allows `BoardOrAdmin` but UI
  gates the Finalize form to `Admin`-only — tighten one or widen
  the other.
- **Governance**: Asociado-must-first-be-Colaborador rule is in the
  docs but not in the code.
- **Tickets**: `TicketDashboardViewModel.AveragePrice` (net) is
  computed but never displayed; the dashboard card labelled
  "Avg. Gross Price" binds to a different field. Naming foot-gun.
- **Email**: `IEmailBodyComposer` interface is in Application but
  the impl (`BrandedEmailBodyComposer`) is in Infrastructure. Move
  impl, or accept the layering split?
- **Notifications**: dispatch methods don't `try/catch` —
  exceptions propagate. The doc previously claimed "always
  swallowed". Is fire-and-forget the intent, or should a guard
  land?
- **Calendar**: `CanEdit` on the view model is hard-coded `true` in
  the controller. Deliberate seam awaiting a future tier check, or
  vestigial?
- **Campaigns**: `User.UnsubscribedFromCampaigns` boolean still on
  the entity but only referenced in GDPR export. Drop?
- **Campaigns**: `ImportCodesAsync` has no service-side Draft guard
  (the view hides the form, but a direct POST on Completed would
  succeed).
- **Users**: `EventParticipation` is not exported in GDPR. Code gap
  or intended scope?

These should each get an issue or a code change separately — I left
the docs reflecting current code state rather than aspirational
state.

## Skipped (errors)

- (none in the follow-up sweep — both `dev-stats` and
  `reforge-history` errors from the original report were resolved).
