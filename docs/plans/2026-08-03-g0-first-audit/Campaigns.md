# Campaigns — G0 First Audit

**Kind:** vertical · **Audited:** 2026-08-03 @ `5a9bbe198`

## G1 — Ownership

| # | Predicate | Result | Evidence |
|---|-----------|--------|----------|
| 1 | Every owned table read/written by exactly one repository | **PASS** | `reforge ownership-violations --owner Campaigns --tables Campaigns,CampaignCodes,CampaignGrants` → 0 hits. `CampaignRepository.cs` header comment: "The only non-test file that touches `DbContext.Campaigns`, `DbContext.CampaignCodes`, or `DbContext.CampaignGrants`... after the Campaigns migration lands." |
| 2 | One writer-service per table | **PASS** | Only `CampaignService` (via `CampaignRepository`) writes `Campaigns`/`CampaignCodes`/`CampaignGrants`. No interceptor/second-writer pattern found. |
| 3 | No EF entity leaks across the boundary | **PASS** | All 19 members of `ICampaignService` return custom DTOs (`CampaignCreateResult`, `CampaignEditSnapshot`, `CampaignListSummary`, `CampaignDetailPageDto`, `CampaignGrantSummary`, etc.) — no raw `Campaign`/`CampaignCode`/`CampaignGrant` entity types in the public signature. Cross-section consumers confirmed via `reforge injected`/grep: `Humans.Application/Services/Tickets/TicketSyncService.cs` (calls `MarkGrantsRedeemedAsync`, a **write**) and `Humans.Infrastructure/Jobs/ProcessEmailOutboxJob.cs` (calls `UpdateGrantEmailStatusAsync` ×2, also **writes**). Both are legitimate cross-section state-changing calls through the public service interface per the hard rules ("calls between sections may happen at the services layer"); neither pulls a read leak. Note: Campaigns has **no** `ICampaignServiceRead` — acceptable today since the only external consumers need writes, not reads, but flagged below since it means the full mutation surface (19 methods) is exposed to two other sections rather than a minimal contract. |
| 4 | No cross-section EF joins (zero baseline entries) | **FAIL** | Grep of all 5 `Architecture/Baselines/*.txt` files for `campaign` (case-insensitive) → 1 hit, in `DisplaySortInControllers.baseline.txt` (unrelated rule — display-sort-in-repository, not cross-section-EF-join; see note below). No cross-section-EF-join baseline-*file* entries, but see predicate 5 — 2 configs carry an active `[Grandfathered("HUM0024", ...)]` marker, the attribute-based form of this analyzer's allowlist. |
| 5 | No `[Obsolete]` cross-section navs / `[Grandfathered]` / baseline rows without a queued G2 item | **FAIL** | **Missed in the original pass** (grep only covered `CampaignController.cs`, not `Infrastructure/Data/Configurations/`): `CampaignConfiguration.cs:8` and `CampaignGrantConfiguration.cs:8` both carry `[Grandfathered(ruleId: "HUM0024", justification: "Pre-existing cross-section EF navigation join; migrating to bare FK + service-level stitching.", since: "2026-05-25", issueRef: "docs/architecture/roslyn-analysis.md#hum0024")]` — the same marker found on 4 other sections' configs in this audit pass. These correspond to the `[Obsolete]`-marked `Campaign.CreatedByUser`/`CampaignGrant.User` navs the section doc itself documents as kept "solely so EF can model the FK constraint." One **stale** baseline row also found: `DisplaySortInControllers.baseline.txt:9` lists `CampaignRepository.cs:OrderByDescending#1`, but the actual code (`CampaignRepository.cs:304`) already carries the `// arch:db-sort-ok` allow-marker on the same line — likely dead baseline weight (see gap list). |
| 6 | Controllers thin, no HUM0031 grandfathers | **PASS** | `grep -n "HUM0031\|Grandfathered" CampaignController.cs` → no matches. `CampaignController(ICampaignService, IUserServiceRead)` — clean primary-constructor DI, no repository injection. |
| 7 | `docs/sections/Campaigns.md` exists and matches reality | PASS | **Correction:** `docs/sections/Campaigns.md` DOES exist (a prior glob call with brace-expansion syntax returned a false negative — confirmed via `ls` and direct `Read`; it's a 151-line, detailed doc: wave allocation, code/grant uniqueness constraints, unsubscribe token handling, HTML-encoding on template substitution). No drift found — it correctly documents both `Campaign.CreatedByUser`/`CampaignGrant.User` as `[Obsolete]`-marked-for-FK-only and the absence of a dedicated `CampaignArchitectureTests.cs`. It does not mention the HUM0024 grandfathers (predicate 5), a minor omission shared by every section audited in this pass. |

## G3 — Tests

| # | Predicate | Result | Evidence |
|---|-----------|--------|----------|
| 1 | Repository tests use real Postgres, zero EF-InMemory | **FAIL (worse: no coverage)** | No `CampaignRepositoryTests.cs` exists anywhere under `tests/` (`Application.Tests/Repositories/`, `Integration.Tests/Repositories/` both checked). `CampaignRepository` — the sole writer of 3 tables including account-merge fold logic (`ReassignGrantsToUserAsync`) — has **zero direct repository-level test coverage**; it's only exercised indirectly through `CampaignServiceTests.cs` against an in-memory context. |
| 2 | Service tests mock repo/`I…ServiceRead`, zero `HumansDbContext` | **FAIL** | `CampaignServiceTests.cs:28` constructs the **real** `CampaignRepository(DbFactory)` (not a mock) where `DbFactory` comes from the shared `ServiceTestHarness` base, which is EF-InMemory-backed (`ServiceTestHarness.cs:54,71` — `.UseInMemoryDatabase(...)`). This is exactly the pattern G3.2 forbids: service tests should mock `ICampaignRepository`, not exercise the concrete repository against an in-memory `HumansDbContext`. This is the shared harness used repo-wide — in scope of tracked issue #766, not unique to Campaigns, but Campaigns is a clear example of the anti-pattern. |
| 3 | Section invariants/triggers have a test (spot-check against `docs/features/campaigns/campaigns.md`) | **PASS (spot-checked)** | Wave-send exclusion rules (already-granted humans, opted-out via `ICommunicationPreferenceService.IsOptedOutAsync`) — heavily covered, 17 references to `SendWave`/`WaveSend` in `CampaignServiceTests.cs`. Legacy-token unsubscribe behavior (flips `CommunicationPreference`, **not** `User.UnsubscribedFromCampaigns`) — covered in `tests/Humans.Application.Tests/Services/UnsubscribeServiceTests.cs`. Admin-only route gating — covered by `Web.Tests/Controllers/CampaignControllerTests.cs` (not deep-inspected this pass, file exists and is named correctly). |
| 4 | No skipped tests without an issue ref | **PASS** | `grep "Skip\s*="` across `CampaignServiceTests.cs`, `CampaignControllerTests.cs` → 0 hits. |
| 5 | Tests grouped under the section | **PASS (minor nit)** | `Architecture/CampaignsArchitectureTests.cs`, `Services/CampaignServiceTests.cs`, `Web.Tests/Controllers/CampaignControllerTests.cs` all clearly named/locatable. Nit: `CampaignServiceTests.cs` sits directly under `Services/` rather than a `Services/Campaigns/` subfolder (same pattern gap as several other sections — not singling out Campaigns). |

## G1 Gap List

1. **2 HUM0024 cross-section EF join grandfathers** (`CampaignConfiguration`, `CampaignGrantConfiguration`) — where: `src/Humans.Infrastructure/Data/Configurations/Campaigns/*.cs:8`. No queued G2 items beyond the generic doc anchor. No-migration-needed: y (pending liveness verification).
2. **Live `DisplaySortInControllers` baseline row.** Where: `tests/Humans.Application.Tests/Architecture/Baselines/DisplaySortInControllers.baseline.txt:9` (`CampaignRepository.cs:OrderByDescending#1`). ~~Initially assessed as stale~~ — corrected 2026-08-03 during the stretch pass: `#1` is the *first* occurrence, `CampaignRepository.cs:72` (`.OrderByDescending(c => c.CreatedAt)`), which carries **no** `arch:db-sort-ok` marker (the markers are on the *other* occurrences, lines 134/304). The row is live: fix by moving the ordering to the presentation layer (or marking it `arch:db-sort-ok` if it qualifies as a pagination tie-breaker/top-N per the display-sort atom), then delete the row. No-migration-needed: y.
3. **No `ICampaignServiceRead`.** Where: `src/Humans.Application/Interfaces/Campaigns/`. Not a violation today (cross-section consumers only need writes), but if any future cross-section caller needs read access to campaign/grant data, it should get a purpose-scoped read interface rather than the full 19-method `ICampaignService`. No action now — advisory only. No-migration-needed: y.

## G3 Gap List (feeding G3)

1. **No repository-level tests for `CampaignRepository` at all.** Fix: add `tests/.../Repositories/CampaignRepositoryTests.cs` against the real-Postgres shared fixture (not InMemory) — highest-priority gap of the two sections in this batch, since there's currently zero direct coverage of a repository that owns account-merge fold logic. No-migration-needed: y.
2. **`CampaignServiceTests.cs` instantiates the real repository against InMemory instead of mocking `ICampaignRepository`.** Fix: convert to NSubstitute mock of `ICampaignRepository`, following the same shape already used correctly for `ITeamService`/`IUserEmailService` mocks in the same file. In scope of #766. No-migration-needed: y.

## G2 Queue Notes (light)

- Q3 plan lane note: Campaigns sits in the independent "`#524 → #204 (legal gate) → #218`" newsletter/marketing-opt-in/segmentation lane — no legal-gate coupling observed in the current code (current `CampaignCodes` message-category opt-out via `ICommunicationPreferenceService` is already the compliant pattern the lane presumably builds on). Nothing blocking G2 schema work found.
- No dead columns/tables spotted in `Campaign`/`CampaignCode`/`CampaignGrant` this pass.
- `ReassignGrantsToUserAsync` (account-merge fold) has a `_ = updatedAt;` unused-parameter workaround with a comment explaining `CampaignGrant` has no `UpdatedAt` column — not a defect, just noted for context if the merge-fold pattern gets revisited.

## Verdict

`G1: 3 gaps (2 HUM0024 grandfathers counted as one item; live DisplaySortInControllers baseline row at CampaignRepository.cs:72; no ICampaignServiceRead is advisory-only, not counted) · G3: 2 gaps`
