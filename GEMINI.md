# GEMINI

Repo-local instructions for Gemini sessions in this repository.

## Project Context

See `CLAUDE.md` for project overview, architecture (including the ongoing per-section project split, nobodies-collective/Humans#866), build commands, and git workflow.

## Related Rules

- See `docs/architecture/design-rules.md` for architecture detail behind the hard rules in `docs/architecture/peters-hard-rules.md`.
- See `memory/INDEX.md` for atomic project rules (one rule per file under `memory/<bucket>/`). Scan INDEX descriptions; fetch the atom body when a description's trigger matches the task at hand.
