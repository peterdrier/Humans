<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <InternalsVisibleTo Include="Humans.Stripe.Tests" />
    <!-- The one assembly allowed to see many sections (design §5). -->
    <InternalsVisibleTo Include="Humans.Integration.Tests" />
  </ItemGroup>

  <ItemGroup>
    <!--
      The payments connector section. Stripe.net stops here: Contracts/IStripeService.cs is
      the §15i seam and no SDK type crosses it. No tables, no controllers and no views, so
      this is plain Microsoft.NET.Sdk rather than Sdk.Razor. Base is the only reference —
      it supplies ILogger.TimeOperation and the GitHubSettings the webhook registrar reads.
    -->
    <ProjectReference Include="..\..\Humans.Base\Humans.Base.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      No AspNetCore package here: those ship with no PackageVersion on purpose and would
      fail the build with NU1010 under Central Package Management. IHostedService and
      friends arrive through the framework reference below (design §15 step 1).
    -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
    <PackageReference Include="NodaTime" />
    <PackageReference Include="Octokit" />
    <PackageReference Include="Stripe.net" />
  </ItemGroup>

  <ItemGroup>
    <!-- The section owns its documentation (design §7a); keep it visible in Solution Explorer. -->
    <None Include="**\*.md" />
  </ItemGroup>

</Project>
