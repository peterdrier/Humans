# Shifts — target shape

Regenerated by every `/section-doctor` run before any scan; the diff against the previous copy
is signal (the section moved, or the earlier target was wrong).

## 1. What the section does

Runs the volunteer schedule for one event at a time. Coordinators describe the work as rotas
(a named container of shifts belonging to a department or sub-team, in the Build, Event or
Strike period) and shifts (a slot with a day, an optional time, a duration and a headcount).
Volunteers browse the open rotas, sign up for a slot or a run of all-day slots, see their own
schedule, declare which build days they are around, save which kinds of work they prefer, and
keep a small shift-matching profile (skills, quirks, languages). Coordinators approve, refuse,
voluntell, bail, mark no-shows and remove people, one slot or a whole run at a time; they can
email everyone booked on a rota or on the team's rotas. Admins set the event calendar and the
sign-up policy for the year. A dashboard tells the cross-department coordinators where the
schedule is thin, who is waiting on approval, how much each person is carrying, and — for the
build weeks — who is on site each day, who is off, and who has gone to camp set-up. After the
event it reports completion and no-show rates. Everything a person does to the schedule is
audited and the affected coordinators are notified.

## 2. The shapes

| Shape | Members | External callers |
|---|---|---|
| **Describe the work** — rota/shift CRUD, bulk generation, tag labels, move rota between departments | `ShiftAdminController` (`/Teams/{slug}/Shifts/*`), `IShiftManagementService` create/update/delete/generate/tag/move, `IShiftSeeding` | Development seeder only (`IShiftSeeding`) |
| **Take a slot** — self sign-up (single, range, per-day toggle) and bail; policy gates: browsing open, EE freeze, name/dietary completeness, capacity, overlap | `ShiftsController` `ToggleDay`/`Bail`/`BailRange`, `IShiftSignups.SignUp*`/`Bail`, `ShiftSignupService` | Onboarding wizard, Agent |
| **Review a slot** — approve/refuse/no-show/remove/voluntell, single and block | `ShiftAdminController` signups/voluntell routes, `ShiftDashboardController.Voluntell`, `IShiftSignupService` review methods, `IShiftSignups.Voluntell/Refuse` | Agent (voluntell) |
| **Look at the schedule** — browse, mine, urgent, staffing snapshot, summary-by-camp, per-user/per-rota projections, search hit | `GET /Shifts`, `/Shifts/Mine`, `/Shifts/Summary*`, `IShiftManagementServiceRead`, `IShiftView`/`IShiftRowView`, `<vc:shifts-search-result>`, `<vc:dashboard-shifts>`, `<vc:shifts-gallery>`, `<vc:shift-signups>`, `<vc:onboarding-shifts-list>` | Search, Teams, Tickets, Store, Cantina, Gate, MailerLite, Camps, Events, Users, Scanner, Surveys |
| **The event calendar** — active event settings, build sub-periods, EE close, browsing toggle | `GET/POST /Shifts/Settings`, `IBurnSettingsService`/`BurnSettingsInfo`, `BuildSubPeriodClassifier` | Every section that needs "which year / which day offset is today" (Events, Camps, Tickets, Notifications, Settings, …) |
| **Who I am as a volunteer** — shift profile, tag preferences, general availability | `ShiftProfileController` (`/Profile/Me/ShiftInfo`), `POST /Shifts/Preferences/Tags`, `POST /Shifts/Mine/Availability`, `IShiftVolunteerProfiles`, `IVolunteerTrackingService.SetAvailabilityAsync` | Users (profile page), Onboarding |
| **Coordinator analytics** — overview, activity, trends, heatmap, daily staffing, duration breakdown, coverage pies, post-event stats, workload | `ShiftDashboardController`, `ShiftWorkloadAdminController`, `IShiftManagementService` analytics methods, `IWorkloadService` | none |
| **Build-week tracking** — availability grid, day-off, camp-set-up marker, xlsx export, profile build strip, EE derivation | `VolunteerTrackingController`, `IVolunteerTrackingService`, `IVolunteerTrackingExportService`, `IVolunteerTrackingServiceRead`, `IEarlyEntryProvider` | Users (build strip), EarlyEntry (provider) |
| **Tell people** — rota / team-rota coordinator email | `ShiftAdminController` `Email` routes, `IRotaCoordinatorMessageService` | none |
| **Housekeeping** — GDPR export/delete, account merge re-FK, iCal feed, cache eviction, orphan scan | `IUserDataContributor`, `IUserMerge`, `ICalendarFeedContributor`, `IShiftViewInvalidator`, `IShiftAuthorizationInvalidator`, `GET /Shifts/OrphanSignups` | Gdpr, Users (merge, deletion), Calendar, Teams/Users (auth eviction) |

## 3. Structure

The shapes imply:

- One repository per table family, both already present: `IShiftManagementRepository` (rotas,
  shifts, signups, event settings, tags, profiles) and `IVolunteerTrackingRepository`
  (availability, build status).
- Three owning services, each one shape family: **management** (describe the work + the event
  calendar + analytics), **signups** (take + review a slot), **tracking** (build-week tracking).
  Cross-section reads through `.Contracts` leaf interfaces only; the full interfaces stay
  `internal`.
- Analytics is the largest shape with no external caller. Its natural home is a fourth
  internal service (`WorkloadService` is already that shape for one report) so
  `ShiftManagementService` stops being the section's junk drawer. This is a direction, not a
  strike item — see §6.
- Controllers translate only; page assembly lives in `*PageBuilder` classes (browse, admin,
  dashboard) and the volunteer search builder.
- One resource set, one `Section.cs`, the Shell seams (`SectionNav`, `SectionAdminNav`,
  `SectionAdminTiles`, `SectionChrome`, `SectionMemberDashboard`, `SectionPolicies`,
  `SectionThingsToDo`).
- Docs: `Shifts.md` (invariants), `authorization.md`, `data-access.md`, `features/*` (one per
  shipped feature), `health.md` (this), `debt.yml`. Dated design docs are history: their
  surviving constraints belong in `Shifts.md`, the rest is provenance.

## 4. Invariants

- Signup state machine on the entity: Pending → Confirmed/Refused/Bailed/Cancelled; Confirmed →
  Bailed/NoShow/Cancelled. Any other transition throws.
- `MaxVolunteers` is a hard ceiling on every confirming path (sign-up, approve, voluntell,
  approve-range). `MinVolunteers ≤ MaxVolunteers` on every create/update/generate path.
- A block (`SignupBlockId`) is bailed / approved / refused as one.
- Exactly one active `EventSettings`; rotas are created only against it and only on non-system
  teams; a rota moves only to a top-level non-system department.
- Regular volunteers see only visible rotas and non-`AdminOnly` shifts; after
  `EarlyEntryClose` they cannot touch Build-period signups; when browsing is closed they see
  only what they already hold.
- Overlap: a confirming path rejects a shift whose absolute window intersects an existing
  Confirmed signup of the same user; all-day means 08:00–18:00 local.
- Every signup state change writes one audit entry and one coordinator notification; a
  bail/remove that drops a shift below `MinVolunteers` raises a coverage-gap notification.
- Medical data is gated at every render, never stripped in a service; only Admin/NoInfoAdmin
  see it.
- Deleting a rota or shift with a Confirmed signup is refused; pending signups are Cancelled.
- Every mutation evicts the per-user / per-rota view cache and, for signups, the dashboard
  aggregates.

## 5. Seams

- **Event settings cutover** (nobodies-collective/Humans#1104): the app-wide event values are
  moving to the Settings section; `IBurnSettingsService`/`BurnSettingsInfo` is the surface
  every consumer holds until then. Do not widen it, do not split it early.
- **Per-user reads still on `IShiftSignupService`** (`GetByUserAsync`, `GetTeamProbeAsync`):
  the constraint is that `IShiftView` stays the only cached per-user projection, so do not add
  a second cache in front of these; move callers onto `IShiftView` when they are touched.
- **Unused VEP dietary/medical columns** await a post-prod-soak drop
  (`no-drops-until-prod-verified`).

## 6. Deliberately not done

- **No `Team.HexColor` column** — export colours derive from the team id (`TeamPalette`).
- **No per-shift `IsCancelled`** — cancellation is a signup state plus cascade delete.
- **No caching decorator over `ShiftManagementService`** — it holds two small caches (auth
  60 s, dashboard 5 min) directly; a decorator would have to know the invalidation keys anyway.
  The one decorator in the section is `CachingShiftViewService` over `IShiftView`/`IShiftRowView`.
- **No dashboard cache key fan-out for sub-period** — sub-period filtered calls bypass the
  cache by design.
- **No analytics-service split this run** — it is the right direction but a structural move
  across 2 k lines with no behaviour change to pin it; it needs Peter's go and its own PR.
- **No `IShiftSignupService` read/write split** — the leaf `IShiftSignups` already is the
  cross-section face; the full interface is internal.
- **No test asserting absences** (`no-tests-for-absences`).

## Load-bearing weirdness

- `ShiftProfileController` routes under `/Profile`, not `/Shifts`: the URL predates the
  section split and is bookmarked/embedded; keep it.
- `IShiftView` is a **singleton** over a keyed-scoped inner and implements `IHostedService`
  with no-op start/stop for symmetry with the sibling caches — the registration looks
  redundant and is not.
- `GetRotaAsync` on the read leaf applies **no visibility filter**: the id comes from a
  filtered search and the destination page enforces access.
- `ExportXlsx` writes **no** audit entry on purpose (burner names only).
- Sub-team managers manage only their sub-team; department coordinators manage the whole
  subtree; `IsPromotedToDirectory` sub-teams get their own pie and filter bucket. Three
  different scoping rules that look like one.
- `SetDayAvailabilityAsync` guards only `dayOffset >= 0`; an out-of-window offset is stored
  but never rendered (documented write-path asymmetry).
- Voluntell is allowed on past shifts (retroactive correction); the volunteer notification is
  suppressed for past shifts, the audit entry never is.
- `ShiftsResource` leaves seven `ShiftsSummary_` keys and `Medical_Badge` in `SharedResource`
  because the partials that bind them live in Teams/UI and a rebind would be a reference cycle.

## History

| Run | Date | Headline | PR |
|---|---|---|---|
| 1 | 2026-09-05 | a coordinator could delete any team's rota or shift by id; docs, comments and member-facing copy brought back to the tree | [peterdrier/Humans#1596](https://github.com/peterdrier/Humans/pull/1596) |
