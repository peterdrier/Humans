@* Reachable only as the SaveSettings POST's model-invalid re-render (peterdrier/Humans#1634)
   — GET here redirects to /Settings#event-guide, which uses the same partial. *@
@model Humans.Events.Models.GuideSettingsViewModel
@{
    ViewData["Title"] = "Guide Settings";
}

<h2>Guide Settings</h2>

<partial name="_GuideSettingsForm" model="Model" />
