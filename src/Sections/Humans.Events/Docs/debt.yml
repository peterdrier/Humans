# Section debt ledger for Humans.Events. Shape + routing: memory/process/debt-ledger-additions.md.
# /debt-sweep pools every src/Sections/*/Docs/debt.yml into the same inbox as the central ledger.
version: 1
inbox:
  - added: 2026-08-24
    what: "Three types carry three hand-mirrored copies of GetOccurrenceInstants — Domain/Event.cs, Services/Dtos/EventInfo.cs and Humans.Events.Contracts/ApprovedEventView.cs — each labelled 'Mirrors …' in its own doc comment. The null-guard around them (gateOpeningDate.HasValue && tz != null ? e.GetOccurrenceInstants(gate, tz) : [e.StartAt]) is then written out at every call site (EventsApiController, EventsExportController, EventsDashboardController, EventsController, EventOccurrenceExpander, the Services/Service.cs calendar feed) and cannot be deduped mechanically because the three types share no shape. Collapsing needs a shared interface or a common base on public DTOs, so it is Peter's call, not a run's. Found by /section-doctor on Events 2026-08-24; call-site list refreshed 2026-09-09."
    review: panel
  - added: 2026-08-24
    what: "EventService (Services/Service.cs) is over the section's maxClassLoc of 740 and holds the section's two highest-complexity methods — ValidateBulkRows and BulkImportAsync — plus the long BuildBulkUploadTemplateAsync. The bulk-import trio is one cohesive concern (parse, validate all-or-nothing, write the template back out) and splits cleanly out of the service without touching the other five question-shapes. Found by /section-doctor on Events 2026-08-24."
    review: panel
  - added: 2026-09-09
    what: "Four invariants in Docs/health.md are unpinned at the enforcement site: (4) favourites/preferences self-scoping — no test touches the [Authorize] API actions (GetFavourites/AddFavourite/RemoveFavourite/GetPreferences/UpdatePreferences) or their Unauthorized path; (5) the submission window — pinned only as the EventGuideSettings.IsSubmissionOpenAt predicate, none of EventsController's closed-window guards is tested; barrio-event camp authorization (CampOperationRequirement.SubmitEvent via ResolveCampEventManagementAsync) and owner-or-admin on barrio Edit/Update have no deny test; (8) EventsApiController's day-offset path always runs with tz == null because every API test stubs guide settings to null. Found by /section-doctor on Events 2026-09-09."
    review: light
  - added: 2026-09-09
    what: "Member-facing views ship hardcoded English: Views/Events/IndividualEventForm.cshtml (no Localizer reference at all), Views/Events/BarrioEventForm.cshtml (a single Localizer reference; every other label, hint and button is a literal) and Views/Shared/Components/EventsCard/Default.cshtml ('Events' header, 'Recurring event' tooltip). Browse, Schedule and MySubmissions are fully localized; the two submit forms never were. Fix is a resx backfill across all six cultures plus the view edits — its own PR, not a side effect. Found by /section-doctor on Events 2026-09-09."
    review: light
