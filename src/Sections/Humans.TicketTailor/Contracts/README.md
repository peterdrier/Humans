# `Contracts/` — deliberately empty

This section publishes nothing: it is an adapter, and the port it implements
(`ITicketVendorService`, namespace `Humans.Tickets.Contracts`) is owned by `Humans.Tickets`.
The folder stays so the section carries the G5 adapter shape.
Invariants: [`../Docs/TicketTailor.md`](../Docs/TicketTailor.md).
