# Section debt ledger for Humans.Tickets. Shape + routing: memory/process/debt-ledger-additions.md.
# /debt-sweep pools every src/Sections/*/Docs/debt.yml into the same inbox as the central ledger.
version: 1
next_id: 9
inbox:
  - added: 2026-09-05
    id: TICKETS-1
    what: "Test gaps from the invariant matrix (finding 15, /section-doctor on Tickets 2026-09-05): (a) TicketQueryService.EraseForUserAsync has no behavior test (only the DI sweep); (b) GetUserTicketExportDataAsync is untested; (c) TicketQueryService.GetMatchedTicketUserIdsAsync and GetMatchedUserIdsForEmailsAsync have no test anywhere; (d) 'sync removes Ticketed when no valid ticket remains' is unpinned — the participation tests cover add/flip only."
    review: light
  - added: 2026-09-05
    id: TICKETS-2
    what: "TicketController.Orders/Attendees/Codes/SalesAggregates copy service DTOs into Models/TicketViewModels.cs field-by-field (TicketDashboardDtos.cs mirrored, ~150 lines; the Attendees VIP split IsVip/TaxableAmount/VipDonation is computed in the controller). Collapse: bind the DTOs directly as TicketTransferAdmin/Detail and Onsite already do, moving the VIP split onto the AttendeeRow DTO or into the view; the views declare the row types in Func<TicketOrderRow, IHtmlContent> helpers, so each needs a render check. Skipped by the 2026-09-05 run (finding 6) because the cloud container has no Postgres for that check."
    review: panel
  - added: 2026-09-11
    id: TICKETS-4
    what: "TicketTransferService.ProcessTransferAsync / ApproveAsync / RejectAsync guard on request.Status == Pending with a plain read-then-write, so two overlapping Decide POSTs on the same request both pass the check and can both enter the vendor void/reissue flow (the Decide form has no submit guard either). no-concurrency-tokens rules out a row version; the fix is an idempotent guard in the service (e.g. a Processing status written before the vendor call) or a disabled-on-submit form. Raised by Codex on peterdrier/Humans#1589; health.md records it as a gap."
    review: panel
  - added: 2026-09-14
    id: TICKETS-5
    what: "Docs/features/ticket-transfer.md's freshness triggers omit src/Sections/Humans.TicketTailor/** while the spec asserts the vendor void+reissue writeback. Docs/Tickets.md credits tests/Humans.TicketTailor.Tests/Architecture/TicketVendorArchitectureTests.cs with pinning \"the two adapters\"; that is tests/Humans.Web.Tests/Architecture/TicketVendorPortArchitectureTests.cs (finding 19, /section-doctor on TicketTailor 2026-09-05; swept in by /section-doctor on Issues 2026-09-14)."
    review: light
  - added: 2026-09-14
    id: TICKETS-6
    what: "Contracts/ITicketVendorService.cs says CreateCheckInAsync is \"Safe to retry\", and src/Sections/Humans.Tickets.Contracts/ITicketVendorMirror.cs repeats it. The vendor call creates a record per POST, which is why Gate's GateVendorCheckInJob runs with Attempts = 0. Both should say \"not idempotent; never retried\" (finding 22, /section-doctor on TicketTailor 2026-09-05; swept in by /section-doctor on Issues 2026-09-14)."
    review: light
  - added: 2026-09-22
    id: TICKETS-7
    what: "Services/Stores/CachingTicketVendorService.cs caches GetEventSummaryAsync with a hand-rolled 15-minute TTL EventSummaryCache instead of the standard TrackedCache decorator pattern (design-rules §15). Replace it with the standard caching pattern (Peter, freshness sweep 2026-09-22)."
    review: panel
  - added: 2026-09-24
    id: TICKETS-8
    what: "Docs/data-access.md (the cache-key paragraph and the cache table row) and Docs/Tickets.md's caching invariant still describe a TicketEventSummary:{eventId} cache entry, while Docs/Tickets.md's CachingTicketVendorService paragraph says the key string is no longer used — the decorator keys its TrackedCache by raw vendor event id (finding 13, /section-doctor on TicketTailor 2026-09-24)."
    review: light
    root: CENTRAL-57
