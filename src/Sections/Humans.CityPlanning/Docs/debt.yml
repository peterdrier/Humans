# Section debt ledger for Humans.CityPlanning. Shape + routing: memory/process/debt-ledger-additions.md.
# /debt-sweep pools every src/Sections/*/Docs/debt.yml into the same inbox as the central ledger.
version: 1
next_id: 4
inbox:
  - added: 2026-08-26
    id: CITY-1
    what: "The container CRUD posts on CityPlanningController (CreateBarrioContainer line 374, EditContainer line 414, DeleteContainer line 451, and their ModelState-invalid branches) redirect to Containers with settings.Year — which is CampSettings.PublicYear — instead of the {year} route value the admin was on. An admin curating containers for a non-public year is bounced to the public year's list after every create, edit and delete, silently losing their place. The private TryCreate/TryUpdate helpers already thread the correct year through; only the callers pass the wrong one. Deferred by Peter (2026-08-26): the year-specific work is being taken together in the winter months, not now. Found by /section-doctor on CityPlanning 2026-08-26."
    review: light
  - added: 2026-09-25
    id: CITY-2
    what: "CityPlanningApiController.SaveCampPolygon validates GeoJSON with its own private IsValidJson, a copy of CityPlanningService.IsValidJson, and the service then validates the same text again and throws ArgumentException. The rule lives twice; folding it into the service needs a result-returning save instead of the throw, which changes ICityPlanningService. Found by /section-doctor on CityPlanning 2026-09-25."
    review: panel
  - added: 2026-09-25
    id: CITY-3
    what: "RestoreCampPolygon with a historyId that does not belong to the season surfaces as a 500: CityPlanningService.RestoreCampPolygonVersionAsync throws InvalidOperationException and CityPlanningApiController does not map it to NotFound. Fixing it changes the endpoint's response, so it is a behaviour change for Peter to accept. Found by /section-doctor on CityPlanning 2026-09-25."
    review: light
