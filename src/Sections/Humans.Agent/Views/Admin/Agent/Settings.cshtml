@model AdminAgentSettingsViewModel
@{ ViewData["Title"] = "Agent — Settings"; }

<h2>Agent Settings</h2>

@* Success/info/error messages are rendered by the shared admin layout's
   toast container via TempDataKeys; SetSuccess() in the controller wires
   into that. No per-view status block needed. *@

<partial name="_AgentSettingsForm" model="Model" />
