@model Humans.Users.Models.HumanSearchViewModel

@{
    ViewData["Title"] = SharedLocalizer["Search_Title"].Value;
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-controller="Team" asp-action="Index">@SharedLocalizer["Teams_Title"]</a></li>
        <li class="breadcrumb-item active">@SharedLocalizer["Search_Title"]</li>
    </ol>
</nav>

<h2>@SharedLocalizer["Search_Title"]</h2>

<form method="get" class="mb-4">
    <div class="input-group" style="max-width: 500px;">
        <input type="text" name="q" class="form-control" value="@Model.Query"
               placeholder="@SharedLocalizer["Search_Placeholder"]" autofocus />
        <button type="submit" class="btn btn-primary">
            <i class="fa-solid fa-search"></i>
        </button>
    </div>
</form>

@if (!string.IsNullOrWhiteSpace(Model.Query))
{
    if (Model.Query.Length < 2)
    {
        <div class="alert alert-info">@SharedLocalizer["Search_MinChars"]</div>
    }
    else if (Model.Results.Count == 0)
    {
        <div class="alert alert-info">@SharedLocalizer["Search_NoResults"]</div>
    }
    else
    {
        <div class="list-group">
            @foreach (var human in Model.Results)
            {
                <vc:user-search-result user-id="@human.UserId"
                                       match-field="@human.MatchField"
                                       match-snippet="@human.MatchSnippet"
                                       matched-email="@human.MatchedEmail" />
            }
        </div>
    }
}
