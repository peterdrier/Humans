<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Mailer's whole cross-section surface: one method, driven by one Hangfire job.

    That job — MailerAudienceSyncJob — was the Base consumer this leaf existed for, and it
    moved into Humans.Mailer/Contracts/ at G5 lane 5b-5 (nobodies-collective/Humans#866).
    Re-derived there: **IMailerAudienceSync now has no consumer outside this section at
    all.** Shell names only the job's concrete type, which is a Humans.Mailer type. So the
    leaf could fold into a Contracts/ folder, or the interface could follow the job inward
    and the project disappear. Left standing deliberately — collapsing a leaf or shrinking
    interface surface is Peter's call, not a job move's side effect (lane 5b-1's open item
    24 answered "leave" on the four in the same position).

    Nothing else leaves. The dashboard's stats, the import plan/apply pair, the whole
    IMailerLiteService read+write surface and the ten audience classes have no consumer
    outside the section and stay internal behind the assembly boundary.

    References Humans.Interfaces alone: the contract returns an int and takes a
    CancellationToken, so no DTO and no Base vocabulary crosses.
  -->

  <ItemGroup>
    <ProjectReference Include="..\..\Humans.Interfaces\Humans.Interfaces.csproj" />
  </ItemGroup>

</Project>
