<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Gate's cross-section surface. This leaf was carved out for exactly one Base consumer,
    GateRetentionJob, and that job moved into Humans.Gate/Contracts/ at G5 lane 5b-3
    (nobodies-collective/Humans#866). Nothing outside Humans.Gate references this project or
    its one type (IGateScanRetention) any more, so the reason it exists is spent: it can fold
    into the section's own Contracts/ folder and be deleted. Left standing here because
    retiring a project is Peter's call, not a jobs lane's.

    References Humans.Interfaces only — the bottom of the graph.
  -->

  <ItemGroup>
    <ProjectReference Include="..\..\Humans.Interfaces\Humans.Interfaces.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="NodaTime" />
  </ItemGroup>

</Project>
